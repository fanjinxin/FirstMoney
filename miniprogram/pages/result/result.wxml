<view class="page-wrap" style="{{themeStyle}}" wx:if="{{result}}">
  <nav-header currentPath="/{{testId}}"></nav-header>
  <view class="container">
  <view class="actions">
    <navigator url="/pages/index/index" class="link">← 返回首页</navigator>
    <view class="retake" bindtap="onRetake">重新测试</view>
  </view>

  <!-- SCL90 完整报告 - 移植自 SCL90Result.tsx -->
  <view wx:if="{{testId === 'scl90' && scl90Report}}" class="scl90-report-wrap">
    <view class="scl90-report-paper">
      <!-- Report Header -->
      <view class="scl90-header">
        <view class="scl90-header-inner">
          <view class="scl90-header-sub">Psychological Assessment Report</view>
          <view class="scl90-header-title">90项症状自评量表（SCL-90）</view>
          <view class="scl90-header-desc">心理健康筛查评估报告</view>
        </view>
      </view>
      <!-- Report Meta -->
      <view class="scl90-meta">
        <view class="scl90-meta-grid">
          <view class="scl90-meta-item"><view class="scl90-meta-label">报告日期</view><view class="scl90-meta-val">{{scl90Report.formatDate}}</view></view>
          <view class="scl90-meta-item"><view class="scl90-meta-label">评估时段</view><view class="scl90-meta-val">最近一周</view></view>
          <view class="scl90-meta-item"><view class="scl90-meta-label">量表题数</view><view class="scl90-meta-val">90 题</view></view>
          <view class="scl90-meta-item"><view class="scl90-meta-label">计分范围</view><view class="scl90-meta-val">1（无）~ 5（严重）</view></view>
        </view>
      </view>
      <!-- Report Body -->
      <view class="scl90-body">
        <!-- 心理健康画像 - 三张独立卡片，严格还原原图 -->
        <!-- 卡片1：画像 + 核心结论 -->
        <view class="scl90-card">
          <view class="scl90-card-header">
            <view class="scl90-dot"></view>
            <text class="scl90-card-title">心理健康画像</text>
          </view>
          <view class="scl90-main-result">{{result.basic.totalLevel}}</view>
          <view class="scl90-desc">{{scl90Report.portraitDesc}}</view>
          <view class="scl90-core-chip">
            <text class="scl90-core-lbl">核心结论</text>
            <text class="scl90-core-val">{{result.basic.totalLevel}}</text>
          </view>
        </view>

        <!-- 卡片2：突出维度（名称+分数横向排列） -->
        <view class="scl90-card scl90-dim-card" wx:if="{{result.top3Factors.length}}">
          <view class="scl90-dim-row">
            <text class="scl90-dim-label">突出维度</text>
            <view class="scl90-dim-items">
              <view wx:for="{{result.top3Factors}}" wx:key="id" class="scl90-dim-item">
                <text class="scl90-dim-name">{{item.name}}</text>
                <text class="scl90-dim-score">{{item.scoreStr}}</text>
              </view>
            </view>
          </view>
        </view>

        <!-- 卡片3：测试总分 -->
        <view class="scl90-card scl90-score-card">
          <view class="scl90-score-top">
            <view class="scl90-score-left">
              <text class="scl90-score-label">测试总分</text>
              <view class="scl90-score-main">
                <text class="scl90-score-num">{{result.basic.totalScore}}</text>
                <text class="scl90-score-max">/ 450</text>
              </view>
            </view>
            <view class="scl90-bar-icon"><view class="scl90-bar-1"></view><view class="scl90-bar-2"></view><view class="scl90-bar-3"></view></view>
          </view>
          <view class="scl90-score-grid">
            <view class="scl90-score-cell"><text class="scl90-cell-lbl">总均分</text><text class="scl90-cell-val">{{result.basic.avgTotalStr}}</text></view>
            <view class="scl90-score-cell"><text class="scl90-cell-lbl">阳性项目</text><text class="scl90-cell-val">{{result.basic.positiveCount}} 项</text></view>
          </view>
          <view class="scl90-top-dim" wx:if="{{result.top3Factors[0]}}">
            <view class="scl90-top-dim-row">
              <text class="scl90-top-dim-label">最高维度: {{result.top3Factors[0].name}}</text>
              <view class="scl90-top-dim-bar-wrap">
                <view class="scl90-top-dim-bar"><view class="scl90-top-dim-fill" style="width: {{(result.top3Factors[0].score/5)*100}}%"></view></view>
                <text class="scl90-top-dim-val">{{result.top3Factors[0].scoreStr2}}</text>
              </view>
            </view>
          </view>
        </view>

        <view class="scl90-divider"></view>

        <!-- 1 可视化分析：雷达图 + 饼图 + 柱状图 -->
        <view class="scl90-section">
          <view class="scl90-section-title"><view class="scl90-section-num">1</view>可视化分析</view>
          <view class="scl90-section-desc">通过图表快速了解十因子整体轮廓、症状分布与因子强弱排序。</view>
          <view class="scl90-charts-row">
            <view class="scl90-chart-left"><canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas></view>
            <view class="scl90-chart-right"><canvas canvas-id="result-pie" class="result-chart-canvas" style="width:100%;height:260px;"></canvas><view class="scl90-pie-legend"><view wx:for="{{scl90Report.symptomDist}}" wx:key="name" class="scl90-legend-item"><view class="scl90-legend-dot" style="background:{{['#7FBFE1','#68D4DB','#98E6E2','#2C6F7A','#0F4C5C'][index % 5]}}"></view><text>{{item.name}}</text></view></view></view>
          </view>
          <view class="scl90-bar-wrap"><canvas canvas-id="result-bar" class="result-chart-canvas" style="width:100%;height:400px;"></canvas></view>
        </view>

        <view class="scl90-divider"></view>

        <!-- 2 十因子分析 -->
        <view class="scl90-section">
          <view class="scl90-section-title"><view class="scl90-section-num">2</view>十因子分析</view>
          <view class="scl90-section-desc">SCL-90 量表从十个维度评估近期心理症状。因子均分 &lt; 2 为正常，2–3 为轻度，3–4 为中度，≥ 4 为重度。</view>
          <view class="scl90-factor-table">
            <view class="scl90-table-row scl90-table-head"><text>序号</text><text>因子维度</text><text>题数</text><text>因子均分</text><text>严重程度</text></view>
            <view wx:for="{{result.factors}}" wx:key="id" class="scl90-table-row {{index % 2 === 1 ? 'scl90-table-alt' : ''}}">
              <text>{{index + 1}}</text><text>{{item.name}}</text><text>{{item.count}}</text><text class="scl90-score-{{item.level}}">{{item.scoreStr}}</text><view class="scl90-level-badge scl90-level-{{item.level}}">{{item.levelLabel}}</view>
            </view>
          </view>
        </view>

        <view class="scl90-divider"></view>

        <!-- 3 总体评价 -->
        <view class="scl90-section">
          <view class="scl90-section-title"><view class="scl90-section-num">3</view>总体评价</view>
          <view class="scl90-section-text">本次评估总分为 <text class="scl90-bold">{{result.basic.totalScore}} 分</text>，总均分 <text class="scl90-bold">{{result.basic.avgTotalStr}}</text>。在 90 个项目中，有 <text class="scl90-bold">{{result.basic.positiveCount}} 项（{{scl90Report.positiveRatio}}%）</text> 达到阳性标准（评分 ≥ 2），阳性症状均分 <text class="scl90-bold">{{result.basic.positiveAvgStr}}</text>。</view>
          <view class="scl90-total-conclusion scl90-conclusion-{{scl90Report.conclusionClass}}">
            <view class="scl90-conclusion-row"><text class="scl90-conclusion-label">综合评估结论：</text><text class="scl90-conclusion-val">{{result.basic.totalLevel}}</text></view>
            <view class="scl90-conclusion-desc">{{scl90Report.conclusionDesc}}</view>
          </view>
          <view class="scl90-indicator-table">
            <view class="scl90-table-row scl90-table-head"><text>指标</text><text>数值</text><text>说明</text></view>
            <view class="scl90-table-row"><text>总分</text><text>{{result.basic.totalScore}}</text><text>90项评分之和（范围 90–450）</text></view>
            <view class="scl90-table-row"><text>总均分</text><text>{{result.basic.avgTotalStr}}</text><text>总分 ÷ 90</text></view>
            <view class="scl90-table-row"><text>阳性项目数</text><text>{{result.basic.positiveCount}}</text><text>评分 ≥ 2 的项目数（占比 {{scl90Report.positiveRatio}}%）</text></view>
            <view class="scl90-table-row"><text>阴性项目数</text><text>{{result.basic.negativeCount}}</text><text>评分 = 1 的项目数</text></view>
            <view class="scl90-table-row"><text>阳性症状均分</text><text>{{result.basic.positiveAvgStr}}</text><text>(总分 − 阴性项目数) ÷ 阳性项目数</text></view>
          </view>
          <view class="scl90-judge-note">* 判断标准：总分 &lt; 160 正常；160–250 轻度困扰；250–350 中度困扰；≥ 350 重度困扰</view>
        </view>

        <view class="scl90-divider"></view>

        <!-- 4 主要困扰维度 -->
        <view class="scl90-section">
          <view class="scl90-section-title"><view class="scl90-section-num">4</view>主要困扰维度</view>
          <view class="scl90-section-desc">以下三个维度得分最高，是近期最突出的心理困扰来源，建议优先关注与干预。</view>
          <view class="scl90-top3-card">
            <view wx:for="{{result.top3Factors}}" wx:key="id" class="scl90-top3-item">
              <view class="scl90-top3-divider" wx:if="{{index > 0}}"></view>
              <view class="scl90-top3-row">
                <view class="scl90-top3-num">{{index + 1}}</view>
                <view class="scl90-top3-content">
                  <view class="scl90-top3-head"><text class="scl90-top3-name">{{item.name}}</text><view class="scl90-level-badge scl90-level-{{item.level}}">{{item.scoreStr2}} 分 · {{item.levelLabel}}</view></view>
                  <view class="scl90-top3-desc">主要表现：{{item.description}}</view>
                  <view class="scl90-top3-detail">{{item.detail}}</view>
                </view>
              </view>
            </view>
          </view>
        </view>

        <view class="scl90-divider"></view>

        <!-- 5 各维度详细解读 -->
        <view class="scl90-section">
          <view class="scl90-section-title"><view class="scl90-section-num">5</view>各维度详细解读</view>
          <view wx:for="{{result.factors}}" wx:key="id" class="scl90-factor-detail">
            <view class="scl90-factor-detail-row"><view class="scl90-factor-detail-head"><text>{{index + 1}}. {{item.name}}</text><view class="scl90-level-badge scl90-level-{{item.level}}">{{item.levelLabel}}</view></view><text class="scl90-factor-detail-score scl90-score-{{item.level}}">{{item.scoreStr}}</text></view>
            <view class="scl90-factor-detail-text">{{item.detail}}</view>
            <view class="scl90-factor-detail-bar"><view class="scl90-factor-bar-fill scl90-bar-{{item.level}}" style="width: {{(item.score/5)*100}}%"></view></view>
          </view>
        </view>

        <view class="scl90-divider"></view>

        <!-- 6 综合建议 -->
        <view class="scl90-section">
          <view class="scl90-section-title"><view class="scl90-section-num">6</view>综合建议</view>
          <view class="scl90-suggest-row">
            <text class="scl90-suggest-label">建议干预等级：</text>
            <view class="scl90-suggest-badges">
              <view class="scl90-sugg-badge {{scl90Report.suggestionLevel === 'normal' ? 'active' : ''}}">自我调节</view>
              <view class="scl90-sugg-badge {{scl90Report.suggestionLevel === 'mild' ? 'active' : ''}}">心理疏导</view>
              <view class="scl90-sugg-badge {{scl90Report.suggestionLevel === 'moderate' ? 'active' : ''}}">心理咨询</view>
              <view class="scl90-sugg-badge {{scl90Report.suggestionLevel === 'severe' ? 'active' : ''}}">专业评估</view>
            </view>
          </view>
          <view class="scl90-suggest-list">
            <view class="scl90-sugg-item"><view class="scl90-sugg-num">1</view><view><text class="scl90-sugg-title">生活方式调整：</text>保持规律作息，保证充足睡眠；适度运动（每周 3–5 次，每次 30 分钟以上）；减少熬夜与过度信息摄入。</view></view>
            <view class="scl90-sugg-item"><view class="scl90-sugg-num">2</view><view><text class="scl90-sugg-title">情绪管理：</text>学习放松技巧（如渐进式肌肉放松、腹式呼吸）；尝试正念冥想；适当倾诉与表达。</view></view>
            <view class="scl90-sugg-item"><view class="scl90-sugg-num">3</view><view><text class="scl90-sugg-title">社会支持：</text>增加积极的人际互动，减少社交孤立；向信任的朋友或家人寻求支持。</view></view>
            <view class="scl90-sugg-item"><view class="scl90-sugg-num">4</view><view><text class="scl90-sugg-title">专业帮助：</text>{{scl90Report.professionalHelp}}</view></view>
          </view>
        </view>
      </view>

      <!-- Report Footer -->
      <view class="scl90-footer">
        <view class="scl90-footer-title">重要声明</view>
        <view class="scl90-footer-text">1. 本报告依据被试自评结果生成，仅作为心理健康筛查参考，不构成任何精神或心理疾病的临床诊断。</view>
        <view class="scl90-footer-text">2. 测评结果受被试当时主观状态、理解能力及配合程度影响，可能与客观情况存在偏差。</view>
        <view class="scl90-footer-text">3. 本报告不能替代精神科医师、心理治疗师或心理咨询师的专业面诊评估。</view>
        <view class="scl90-footer-text">4. 若出现自伤、自杀想法或严重情绪失控等紧急状况，请立即拨打心理援助热线或前往最近的医疗机构急诊。</view>
        <view class="scl90-footer-meta">报告生成时间：{{scl90Report.formatDate}} · SCL-90 心理健康筛查评估</view>
        <view class="scl90-footer-links"><navigator url="/pages/scl90-test/scl90-test" class="scl90-footer-link">返回测试</navigator><navigator url="/pages/index/index" class="scl90-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>

  <!-- AAT 学习适应性测验完整报告 - 移植自 AATResult.tsx -->
  <view wx:if="{{testId === 'aat' && result && aatReport}}" class="aat-report-wrap">
    <view class="aat-report-paper">
      <view class="aat-header">
        <view class="aat-header-inner">
          <view class="aat-header-circle-wrap">
            <view class="aat-circle-outer">
              <view class="aat-circle-inner">{{result.totalPercent}}<text class="aat-circle-unit">%</text></view>
            </view>
          </view>
          <view class="aat-header-text">
            <view class="aat-header-sub">AAT 学习适应性测验</view>
            <view class="aat-header-title">综合适应指数</view>
            <view class="aat-level-badge aat-level-{{aatReport.totalLevelClass}}">{{aatReport.totalLevel.label}}</view>
            <view class="aat-header-desc">{{aatReport.totalLevel.desc}}</view>
            <view class="aat-meta">已完成 {{result.answeredCount}}/{{result.totalQuestions}} 题</view>
          </view>
        </view>
        <view class="aat-stats-row">
          <view class="aat-stat-card"><view class="aat-stat-lbl">优势因子</view><view class="aat-stat-val">{{aatReport.goodCount}}</view><view class="aat-stat-hint">表现较好</view></view>
          <view class="aat-stat-card"><view class="aat-stat-lbl">待改进</view><view class="aat-stat-val">{{aatReport.poorCount}}</view><view class="aat-stat-hint">需关注</view></view>
          <view class="aat-stat-card aat-stat-wide"><view class="aat-stat-lbl">优先改进</view><view class="aat-stat-val">{{aatReport.bottom3[0] ? aatReport.bottom3[0].name : '-'}} {{aatReport.bottom3[0] ? aatReport.bottom3[0].percent : 0}}%</view><view class="aat-stat-hint">建议从该因子着手</view></view>
        </view>
        <view class="aat-groups-row">
          <view wx:for="{{aatReport.groupScores}}" wx:key="id" class="aat-group-cell">
            <view class="aat-group-name">{{item.name}}</view>
            <view class="aat-group-avg">{{item.avg}}%</view>
            <view class="aat-group-bar"><view class="aat-group-fill" style="width: {{item.avg}}%"></view></view>
          </view>
        </view>
      </view>
      <view class="aat-body">
        <view class="aat-section">
          <view class="aat-section-title">可视化分析</view>
          <view class="aat-charts-row">
            <view class="aat-radar-wrap"><view class="aat-chart-title">十二因子雷达图</view><canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas></view>
            <view class="aat-bar-wrap"><view class="aat-chart-title">因子得分排序（由高到低）</view><canvas canvas-id="result-bar" class="result-chart-canvas" style="width:100%;height:400px;"></canvas></view>
          </view>
        </view>
        <view class="aat-divider"></view>
        <view class="aat-section">
          <view class="aat-section-title">四维分组概览</view>
          <view class="aat-group-cards">
            <view wx:for="{{aatReport.groupScores}}" wx:key="id" class="aat-group-card">
              <view class="aat-group-card-head"><view><view class="aat-group-card-name">{{item.name}}</view><view class="aat-group-card-hint">{{item.hint}}</view></view><view class="aat-group-card-avg">{{item.avg}}%</view></view>
              <view class="aat-group-card-bar"><view class="aat-group-card-fill" style="width: {{item.avg}}%"></view></view>
              <view class="aat-group-tags"><view wx:for="{{item.items}}" wx:key="id" wx:for-item="f" class="aat-factor-tag aat-level-{{f.level}}">{{f.name}} {{f.percent}}%</view></view>
            </view>
          </view>
        </view>
        <view class="aat-divider"></view>
        <view class="aat-section">
          <view class="aat-section-title">优势与待改进因子</view>
          <view class="aat-compare-row">
            <view class="aat-top3-block">
              <view class="aat-block-title">表现较好 · 可继续保持</view>
              <view class="aat-top3-cards">
                <view wx:for="{{aatReport.top3}}" wx:key="id" class="aat-top3-item {{index === 0 ? 'aat-top3-first' : ''}}">
                  <view class="aat-top3-rank">{{index + 1}}</view>
                  <view class="aat-top3-name">{{item.name}}</view>
                  <view class="aat-top3-pct">{{item.percent}}%</view>
                </view>
              </view>
            </view>
            <view class="aat-bottom3-block">
              <view class="aat-block-title">建议优先改进</view>
              <view wx:if="{{aatReport.bottom3[0]}}" class="aat-priority-card">
                <view class="aat-priority-head"><view class="aat-priority-name">{{aatReport.bottom3[0].name}}</view><view class="aat-priority-pct">{{aatReport.bottom3[0].percent}}%</view></view>
                <view wx:if="{{aatReport.factorImprove[aatReport.bottom3[0].id]}}" class="aat-priority-tip">💡 {{aatReport.factorImprove[aatReport.bottom3[0].id]}}</view>
              </view>
              <view class="aat-bottom3-list">
                <view wx:for="{{aatReport.bottom3}}" wx:key="id" wx:for-item="b" wx:if="{{index > 0}}" class="aat-bottom3-item">
                  <view class="aat-bottom3-rank">{{index + 1}}</view>
                  <view class="aat-bottom3-name">{{b.name}}</view>
                  <view class="aat-bottom3-pct">{{b.percent}}%</view>
                </view>
              </view>
            </view>
          </view>
        </view>
        <view class="aat-divider"></view>
        <view class="aat-section">
          <view class="aat-section-title">十二因子得分与解读</view>
          <view wx:for="{{result.factorScores}}" wx:key="id" class="aat-factor-detail aat-level-{{item.level}}">
            <view class="aat-factor-detail-head"><view class="aat-factor-detail-name">{{item.name}}</view><view class="aat-factor-detail-meta">{{item.percent}}% · {{item.levelLabel}}</view></view>
            <view class="aat-factor-detail-bar"><view class="aat-factor-bar-fill" style="width: {{item.percent}}%"></view></view>
            <view class="aat-factor-detail-desc">{{aatReport.factorInsights[item.id] || '暂无解读'}}</view>
            <view wx:if="{{(item.level === 'poor' || item.level === 'veryPoor') && aatReport.factorImprove[item.id]}}" class="aat-factor-improve">改进建议：{{aatReport.factorImprove[item.id]}}</view>
          </view>
        </view>
      </view>
      <view class="aat-footer">
        <view class="aat-footer-text">报告生成时间：{{aatReport.formatDate}} · AAT 学习适应性测验</view>
        <view class="aat-footer-note">本测验仅供自我觉察与学习参考，不构成专业评估。结果仅供参考，不用于区分优劣。如有学习困扰，建议寻求专业辅导。</view>
        <view class="aat-footer-links"><navigator url="/pages/test/test?testId=aat" class="aat-footer-link">返回测验</navigator><navigator url="/pages/index/index" class="aat-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>

  <!-- 心理年龄测验结果（严格参照 web 版布局） -->
  <view wx:if="{{testId === 'psych-age' && result && psychAgeReport}}" class="psych-age-report-wrap">
    <view class="psych-age-report-paper">
      <view class="psych-age-header">
        <view class="psych-age-header-row">
          <view class="psych-age-left">
            <view class="psych-age-ring-wrap">
              <view class="psych-age-ring-outer"></view>
              <view class="psych-age-ring-fill" style="background: conic-gradient(from -90deg, var(--xia-teal) 0deg, var(--xia-teal) {{psychAgeReport.ringProgress}}deg, rgba(var(--xia-sky-rgb), 0.35) {{psychAgeReport.ringProgress}}deg, rgba(var(--xia-sky-rgb), 0.35) 360deg);"></view>
              <view class="psych-age-ring-inner">
                <image wx:if="{{psychAgeReport.ageImagePath}}" class="psych-age-icon-img" src="{{psychAgeReport.ageImagePath}}" mode="aspectFill" />
              </view>
            </view>
            <view class="psych-age-left-text">
              <view class="psych-age-label">心理年龄</view>
              <view class="psych-age-score">总分 {{result.totalScore}}/{{result.maxTotalScore}}</view>
            </view>
          </view>
          <view class="psych-age-right">
            <view class="psych-age-right-sub">心理年龄测验</view>
            <view class="psych-age-right-title">{{result.psychAgeRange}}</view>
            <view class="psych-age-right-desc">根据七维度综合评估，你的心理特征倾向于此年龄区间的特质。</view>
            <view wx:if="{{psychAgeReport.youngestDim && psychAgeReport.oldestDim}}" class="psych-age-tags">
              <view class="psych-age-tag psych-age-trend-{{psychAgeReport.youngestDim.trend}}"><text class="psych-age-tag-lbl">最年轻</text><text class="psych-age-tag-val">{{psychAgeReport.youngestDim.name}}</text></view>
              <view class="psych-age-tag psych-age-trend-{{psychAgeReport.oldestDim.trend}}"><text class="psych-age-tag-lbl">最成熟</text><text class="psych-age-tag-val">{{psychAgeReport.oldestDim.name}}</text></view>
            </view>
          </view>
        </view>
      </view>
      <view class="psych-age-body">
        <view class="psych-age-section">
          <view class="psych-age-section-title">心理年龄区间解读</view>
          <view class="psych-age-range-desc">{{psychAgeReport.rangeInsight}}</view>
        </view>
        <view class="psych-age-divider"></view>
        <view class="psych-age-section">
          <view class="psych-age-section-title">可视化分析</view>
          <view class="psych-age-chart-row">
            <view class="psych-age-radar-wrap"><view class="psych-age-chart-title">七维雷达图</view><canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas></view>
            <view class="psych-age-bar-wrap"><view class="psych-age-chart-title">维度得分排序</view><canvas canvas-id="result-bar" class="result-chart-canvas" style="width:100%;height:400px;"></canvas></view>
          </view>
        </view>
        <view class="psych-age-divider"></view>
        <view class="psych-age-section">
          <view class="psych-age-section-title">七维度解读</view>
          <view wx:for="{{result.dimensionScores}}" wx:key="id" class="psych-age-dim-item psych-age-trend-{{item.trend}}">
            <view class="psych-age-dim-head"><view class="psych-age-dim-name">{{item.name}}</view><view class="psych-age-dim-meta">{{item.percent}}% · {{item.trendLabel}}</view></view>
            <view class="psych-age-dim-desc">{{psychAgeReport.dimInsights[item.id] || '暂无解读'}}</view>
          </view>
        </view>
        <view class="psych-age-divider"></view>
        <view class="psych-age-section psych-age-guidance">
          <view class="psych-age-section-title">专业指导</view>
          <view class="psych-age-guidance-inner">
            <view class="psych-age-guidance-block">
              <view class="psych-age-guidance-label">以下情况建议寻求专业帮助</view>
              <view class="psych-age-guidance-list">
                <view wx:for="{{psychAgeReport.professionalGuidance.whenToSeekHelp}}" wx:key="*this" class="psych-age-guidance-item">{{item}}</view>
              </view>
            </view>
            <view class="psych-age-guidance-block">
              <view class="psych-age-guidance-label">自我调节建议</view>
              <view class="psych-age-guidance-list">
                <view wx:for="{{psychAgeReport.professionalGuidance.selfCareTips}}" wx:key="*this" class="psych-age-guidance-item">{{item}}</view>
              </view>
            </view>
            <view class="psych-age-guidance-note">{{psychAgeReport.professionalGuidance.note}}</view>
          </view>
        </view>
      </view>
      <view class="psych-age-footer">
        <view class="psych-age-footer-text">报告生成时间：{{psychAgeReport.formatDate}} · 心理年龄测验</view>
        <view class="psych-age-footer-links"><navigator url="/pages/test/test?testId=psych-age" class="psych-age-footer-link">返回测验</navigator><navigator url="/pages/index/index" class="psych-age-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>

  <!-- APT 天赋潜能结果（严格参照 web 版布局） -->
  <view wx:if="{{testId === 'apt' && result && aptReport}}" class="apt-report-wrap">
    <view class="apt-report-paper">
      <view class="apt-header">
        <view class="apt-header-row">
          <view class="apt-left">
            <view class="apt-ring-wrap">
              <view class="apt-ring-outer"></view>
              <view class="apt-ring-fill" style="background: conic-gradient(from -90deg, var(--xia-teal) 0deg, var(--xia-teal) {{aptReport.ringProgress}}deg, rgba(var(--xia-sky-rgb), 0.35) {{aptReport.ringProgress}}deg, rgba(var(--xia-sky-rgb), 0.35) 360deg);"></view>
              <view class="apt-ring-inner">
                <image class="apt-icon-img" src="/assets/icons/sparkles.svg" mode="aspectFit" />
              </view>
            </view>
            <view class="apt-left-text">
              <view class="apt-label">APT 天赋潜能</view>
              <view class="apt-score">总分 {{result.totalScore}}/{{result.maxTotalScore}}</view>
            </view>
          </view>
          <view class="apt-right">
            <view class="apt-right-sub">天赋潜能评估</view>
            <view class="apt-right-title">综合潜能指数</view>
            <view class="apt-level-badge apt-level-{{aptReport.levelKey}}">{{aptReport.levelLabel}}</view>
            <view class="apt-right-desc">{{aptReport.levelDesc}}</view>
            <view wx:if="{{aptReport.topDim || aptReport.weakDim}}" class="apt-tags">
              <view wx:if="{{aptReport.topDim}}" class="apt-tag apt-level-{{aptReport.topDim.level}}"><text class="apt-tag-lbl">最强</text><text class="apt-tag-val">{{aptReport.topDim.name}}</text></view>
              <view wx:if="{{aptReport.weakDim}}" class="apt-tag apt-level-{{aptReport.weakDim.level}}"><text class="apt-tag-lbl">待提升</text><text class="apt-tag-val">{{aptReport.weakDim.name}}</text></view>
            </view>
          </view>
        </view>
      </view>
      <view class="apt-body">
        <view class="apt-section">
          <view class="apt-section-title">可视化分析</view>
          <view class="apt-charts-row">
            <view class="apt-radar-wrap"><view class="apt-chart-title">六维天赋雷达</view><canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas></view>
            <view class="apt-bar-wrap"><view class="apt-chart-title">维度得分排序</view><canvas canvas-id="result-bar" class="result-chart-canvas" style="width:100%;height:400px;"></canvas></view>
          </view>
        </view>
        <view class="apt-divider"></view>
        <view class="apt-section">
          <view class="apt-section-title">职业与发展方向建议</view>
          <view class="apt-career-card">{{aptReport.careerSuggestion}}</view>
        </view>
        <view class="apt-divider"></view>
        <view class="apt-section">
          <view class="apt-section-title">六维度解读与发展建议</view>
          <view wx:for="{{aptReport.dimensionList}}" wx:key="id" class="apt-dim-item apt-level-{{item.level}}">
            <view class="apt-dim-head"><view class="apt-dim-name">{{item.name}}</view><view class="apt-dim-meta">{{item.percent}}% · {{item.levelLabel}}</view></view>
            <view class="apt-dim-bar"><view class="apt-dim-fill" style="width: {{item.percent}}%"></view></view>
            <view class="apt-dim-desc">{{aptReport.dimInsights[item.id] || '暂无解读'}}</view>
            <view wx:if="{{item.isWeak && item.improveTip}}" class="apt-improve">💡 {{item.improveTip}}</view>
          </view>
        </view>
        <view class="apt-divider"></view>
        <view class="apt-section apt-summary-section">
          <view class="apt-section-title">综合建议</view>
          <view class="apt-summary-card">
            <view wx:for="{{aptReport.summary}}" wx:key="*this" class="apt-summary-item">· {{item}}</view>
          </view>
        </view>
      </view>
      <view class="apt-footer">
        <view class="apt-footer-text">报告生成时间：{{aptReport.formatDate}} · APT 天赋潜能评估</view>
        <view class="apt-footer-links"><navigator url="/pages/test/test?testId=apt" class="apt-footer-link">返回测验</navigator><navigator url="/pages/index/index" class="apt-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>

  <!-- HIT 霍兰德职业兴趣结果（严格参照 web 版） -->
  <view wx:if="{{testId === 'hit' && result && hitReport}}" class="hit-report-wrap">
    <view class="hit-report-paper">
      <view class="hit-header">
        <view class="hit-header-row">
          <view class="hit-left">
            <view class="hit-icon-wrap">
              <image class="hit-icon-img" src="/assets/huolande-icon.png" mode="aspectFit" />
            </view>
          </view>
          <view class="hit-right">
            <view class="hit-right-sub">HIT 霍兰德职业兴趣</view>
            <view class="hit-right-title">你的霍兰德代码：{{result.hollandCode}}</view>
            <view class="hit-right-top3">前三型：{{hitReport.topThreeDisplay}}</view>
            <view class="hit-career-box">
              <view class="hit-career-label">推荐职业：</view>
              <view class="hit-career-val">{{hitReport.codeExamples}}</view>
            </view>
            <view class="hit-consistency-box">
              <view class="hit-consistency-lbl">一致性 {{hitReport.consistency.level}}</view>
              <view class="hit-consistency-short">· {{hitReport.consistency.short}}</view>
            </view>
          </view>
        </view>
      </view>
      <view class="hit-body">
        <view class="hit-section">
          <view class="hit-section-title"><view class="hit-section-icon"></view>霍兰德六边形</view>
          <view class="hit-hexagon-card">
            <view class="hit-hexagon-canvas-wrap">
              <canvas canvas-id="hit-hexagon" class="hit-hexagon-canvas" style="width:280px;height:280px;"></canvas>
            </view>
            <view class="hit-hexagon-text">
              <view class="hit-code-intro-item"><view class="hit-intro-num">1</view><view class="hit-intro-txt">{{hitReport.codeIntro.what}}</view></view>
              <view class="hit-code-intro-item"><view class="hit-intro-num">2</view><view class="hit-intro-txt">你的代码 <text class="hit-code-bold">{{result.hollandCode}}</text>：首位 <text class="hit-code-bold">{{result.topThree && result.topThree[0] ? result.topThree[0].name : '-'}}</text> 为主导型</view></view>
            </view>
          </view>
        </view>
        <view class="hit-divider"></view>
        <view class="hit-section">
          <view class="hit-section-title">六型雷达</view>
          <view class="hit-chart-wrap"><canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:320px;"></canvas></view>
        </view>
        <view class="hit-divider"></view>
        <view class="hit-section">
          <view class="hit-section-title">六型得分</view>
          <view class="hit-chart-wrap"><canvas canvas-id="result-bar" class="result-chart-canvas" style="width:100%;height:360px;"></canvas></view>
        </view>
        <view class="hit-divider"></view>
        <view class="hit-section">
          <view class="hit-section-title">六型解读</view>
          <view class="hit-dim-grid">
            <view wx:for="{{hitReport.dimensionList}}" wx:key="id" class="hit-dim-card {{item.isTop ? 'hit-top' : ''}}">
              <view class="hit-dim-card-icon {{item.isTop ? 'hit-icon-top' : 'hit-icon-normal'}}">{{item.id}}</view>
              <view class="hit-dim-card-body">
                <view class="hit-dim-card-head"><view class="hit-dim-card-name">{{item.name}}（{{item.id}}）</view><view class="hit-dim-card-score">{{item.rawScore}}/15</view></view>
                <view wx:if="{{item.roleLabel}}" class="hit-dim-role">{{item.roleLabel}}</view>
                <view class="hit-dim-bar"><view class="hit-dim-fill" style="width: {{item.percent}}%"></view></view>
                <view class="hit-dim-short">{{item.dimShort}}</view>
              </view>
            </view>
          </view>
        </view>
        <view class="hit-divider"></view>
        <view class="hit-section">
          <view class="hit-usage-row">
            <view class="hit-usage-item"><view class="hit-usage-dot"></view>前三型组成代码 · 首位主导</view>
            <view class="hit-usage-item"><view class="hit-usage-dot"></view>可查 O*NET / 职业索引</view>
            <view class="hit-usage-item"><view class="hit-usage-dot"></view>兴趣≠能力 · 仅供参考</view>
          </view>
        </view>
      </view>
      <view class="hit-footer">
        <view class="hit-footer-text">报告生成时间：{{hitReport.formatDate}} · HIT 霍兰德职业兴趣测试</view>
        <view class="hit-footer-links"><navigator url="/pages/test/test?testId=hit" class="hit-footer-link">返回测验</navigator><navigator url="/pages/index/index" class="hit-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>

  <!-- DTH 黑暗三角结果（严格参照 web 版） -->
  <view wx:if="{{testId === 'dth' && result && dthReport}}" class="dth-report-wrap">
    <view class="dth-report-paper">
      <view class="dth-header">
        <view class="dth-header-row">
          <view class="dth-left">
            <view class="dth-icon-wrap">
              <image class="dth-icon-img" src="/assets/icons/shield.svg" mode="aspectFit" />
            </view>
          </view>
          <view class="dth-right">
            <view class="dth-right-sub">DTH 黑暗三角人格</view>
            <view class="dth-right-title">{{dthReport.conclusion.title}}</view>
            <view class="dth-right-meta">三维均分 {{dthReport.avgPercent}}% · 总分 {{result.totalScore}}/{{result.maxTotalScore}}</view>
          </view>
        </view>
      </view>
      <view class="dth-body">
        <view class="dth-section">
          <view class="dth-conclusion-card">
            <view class="dth-conclusion-icon-wrap dth-icon-{{dthReport.overallLevel}}">
              <view class="dth-conclusion-icon-inner">{{dthReport.overallLevel === 'low' ? '✓' : dthReport.overallLevel === 'moderate' ? 'i' : '!'}}</view>
            </view>
            <view class="dth-conclusion-content">
              <view class="dth-conclusion-title">{{dthReport.conclusion.title}}</view>
              <view class="dth-conclusion-summary">{{dthReport.conclusion.summary}}</view>
              <view class="dth-conclusion-profile">{{dthReport.profileHint}}</view>
              <view class="dth-conclusion-hint">{{dthReport.conclusion.hint}}</view>
            </view>
          </view>
        </view>
        <view class="dth-divider"></view>
        <view class="dth-section">
          <view class="dth-section-title">三角雷达</view>
          <view class="dth-chart-wrap"><canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:288px;"></canvas></view>
        </view>
        <view class="dth-divider"></view>
        <view class="dth-section">
          <view class="dth-section-title">三维度得分</view>
          <view class="dth-chart-wrap"><canvas canvas-id="result-bar" class="result-chart-canvas" style="width:100%;height:240px;"></canvas></view>
        </view>
        <view class="dth-divider"></view>
        <view class="dth-section">
          <view class="dth-section-title">维度解读</view>
          <view wx:for="{{dthReport.dimensionList}}" wx:key="id" class="dth-dim-card">
            <view class="dth-dim-head"><view class="dth-dim-name">{{item.name}}</view><view class="dth-dim-meta">{{item.percent}}% · {{item.levelLabel}}</view></view>
            <view class="dth-dim-bar"><view class="dth-dim-fill" style="width: {{item.percent}}%"></view></view>
            <view wx:if="{{item.levelConclusion}}" class="dth-dim-level-box">✓ {{item.levelConclusion}}</view>
            <view class="dth-dim-desc">{{dthReport.dimInsights[item.id] || '暂无解读'}}</view>
          </view>
        </view>
        <view class="dth-divider"></view>
        <view class="dth-section">
          <view class="dth-disclaimer-card">
            <view class="dth-disclaimer-title">说明：</view>
            <view class="dth-disclaimer-text">{{dthReport.disclaimer}}</view>
          </view>
        </view>
      </view>
      <view class="dth-footer">
        <view class="dth-footer-text">报告生成时间：{{dthReport.formatDate}} · DTH 黑暗三角人格测试</view>
        <view class="dth-footer-links"><navigator url="/pages/test/test?testId=dth" class="dth-footer-link">返回测验</navigator><navigator url="/pages/index/index" class="dth-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>

  <!-- TLA 年上年下结果（严格参照 web 版） -->
  <view wx:if="{{testId === 'tla' && result && tlaReport}}" class="tla-report-wrap">
    <view class="tla-report-paper">
      <view class="tla-header">
        <view class="tla-header-row">
          <view class="tla-left">
            <view class="tla-icon-wrap">
              <image class="tla-icon-img" src="/assets/icons/heart-filled.svg" mode="aspectFit" />
            </view>
          </view>
          <view class="tla-right">
            <view class="tla-right-sub"><image class="tla-sparkles-img" src="/assets/icons/sparkles.svg" mode="aspectFit" />TLA 年上年下恋爱测试</view>
            <view class="tla-right-title">你的恋爱偏好报告</view>
            <view class="tla-tags">
              <view wx:for="{{tlaReport.topTwo}}" wx:key="id" class="tla-tag"><image wx:if="{{item.cute.icon}}" class="tla-tag-icon" src="{{item.cute.icon}}" mode="aspectFit" /><text wx:else class="tla-tag-emoji">{{item.cute.emoji}}</text><text class="tla-tag-label">{{item.cute.label}}</text></view>
            </view>
          </view>
        </view>
      </view>
      <view class="tla-body">
        <view class="tla-section">
          <view class="tla-portrait-card">
            <image class="tla-portrait-icon" src="/assets/icons/heart-filled.svg" mode="aspectFit" />
            <view class="tla-portrait-content">
              <view class="tla-portrait-title">你的恋爱画像</view>
              <view class="tla-portrait-desc">{{tlaReport.profileDesc}}</view>
            </view>
          </view>
        </view>
        <view class="tla-divider"></view>
        <view class="tla-section">
          <view class="tla-section-title"><image class="tla-section-icon" src="/assets/icons/radar.svg" mode="aspectFit" />恋爱偏好雷达</view>
          <view class="tla-chart-wrap"><canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:288px;"></canvas></view>
        </view>
        <view class="tla-divider"></view>
        <view class="tla-section">
          <view class="tla-section-title"><image class="tla-section-icon" src="/assets/icons/chart-bar.svg" mode="aspectFit" />四维得分</view>
          <view class="tla-chart-wrap"><canvas canvas-id="result-bar" class="result-chart-canvas" style="width:100%;height:224px;"></canvas></view>
        </view>
        <view class="tla-divider"></view>
        <view class="tla-section">
          <view class="tla-section-title"><image class="tla-section-icon" src="/assets/icons/mail.svg" mode="aspectFit" />维度解读</view>
          <view class="tla-dim-grid">
            <view wx:for="{{tlaReport.dimensionList}}" wx:key="id" class="tla-dim-card {{item.isTop ? 'tla-top' : ''}}">
              <view class="tla-dim-head">
                <view class="tla-dim-name-row"><image wx:if="{{item.icon}}" class="tla-dim-icon" src="{{item.icon}}" mode="aspectFit" /><text wx:else class="tla-dim-emoji">{{item.emoji}}</text><view class="tla-dim-name">{{item.name}}</view><view wx:if="{{item.isTop}}" class="tla-dim-badge">偏好较高</view></view>
                <view class="tla-dim-percent">{{item.percent}}%</view>
              </view>
              <view class="tla-dim-bar"><view class="tla-dim-fill" style="width: {{item.percent}}%"></view></view>
              <view class="tla-dim-desc">{{tlaReport.dimInsights[item.id] || '暂无解读'}}</view>
            </view>
          </view>
        </view>
        <view class="tla-divider"></view>
        <view class="tla-section">
          <view class="tla-section-title"><image class="tla-section-icon" src="/assets/icons/info.svg" mode="aspectFit" />温馨提示</view>
          <view class="tla-tip-card">{{tlaReport.tip}}</view>
        </view>
      </view>
      <view class="tla-footer">
        <view class="tla-footer-text">报告生成时间：{{tlaReport.formatDate}} · 年上年下恋爱测试</view>
        <view class="tla-footer-links">
          <navigator url="/pages/test/test?testId=tla" class="tla-footer-btn">返回测验</navigator>
          <navigator url="/pages/index/index" class="tla-footer-link">返回首页</navigator>
        </view>
      </view>
    </view>
  </view>

  <!-- FFT 水果塑结果（严格按 web 布局） -->
  <view wx:if="{{testId === 'fft' && result && fftReport}}" class="fft-report-wrap">
    <view class="fft-report-paper">
      <!-- 头部：与 web 一致 -->
      <view class="fft-header">
        <view class="fft-header-inner">
          <view class="fft-header-row">
            <view class="fft-emoji-circle">
              <text class="fft-emoji">{{fftReport.primaryEmoji}}</text>
            </view>
            <view class="fft-header-text">
              <view class="fft-meta">FFT 水果塑形 · 九型人格</view>
              <view class="fft-title">你是{{fftReport.primary.name}}人格</view>
              <view class="fft-sub">主型 {{fftReport.primary.percent}}% · {{fftReport.primary.rawScore}}/{{fftReport.primary.maxScore}}</view>
              <view class="fft-tags">
                <view wx:if="{{fftReport.secondary}}" class="fft-tag fft-tag-secondary">{{fftReport.secondary.emoji}} 次型 {{fftReport.secondary.name}} {{fftReport.secondary.percent}}%</view>
                <view wx:if="{{fftReport.tertiary}}" class="fft-tag fft-tag-tertiary">{{fftReport.tertiary.emoji}} {{fftReport.tertiary.name}}</view>
              </view>
            </view>
          </view>
          <!-- 九型得分：3 列网格 -->
          <view class="fft-score-grid-wrap">
            <view class="fft-score-grid-title">九型得分</view>
            <view class="fft-score-grid">
              <view wx:for="{{fftReport.scoreGrid}}" wx:key="id" class="fft-score-cell {{item.isPrimary ? 'fft-score-primary' : ''}} {{item.isSecondary ? 'fft-score-secondary' : ''}}">
                <text class="fft-score-emoji">{{item.emoji}}</text>
                <text class="fft-score-name">{{item.nameShort}}</text>
                <text class="fft-score-pct">{{item.percent}}%</text>
              </view>
            </view>
          </view>
        </view>
      </view>

      <view class="fft-body">
        <!-- 主型解读 -->
        <view class="fft-section">
          <view class="fft-section-title"><text class="fft-section-emoji">{{fftReport.primaryEmoji}}</text>{{fftReport.primary.name}}解读</view>
          <view class="fft-primary-card">
            <view class="fft-primary-desc">{{fftReport.dimInsights[fftReport.primary.id] || '暂无解读'}}</view>
            <view class="fft-primary-disclaimer">本测验基于情境选择法，54 题三选一。主型为得分最高维度，次型为第二高。结果仅供自我觉察，不构成专业心理评估。</view>
          </view>
        </view>

        <!-- 九维雷达 -->
        <view class="fft-section">
          <view class="fft-section-title">九维雷达</view>
          <view class="fft-radar-wrap">
            <canvas canvas-id="result-radar" class="fft-chart-canvas" style="width:100%;height:256px;"></canvas>
          </view>
        </view>

        <!-- 九型柱状图 -->
        <view class="fft-section">
          <view class="fft-section-title">九型得分</view>
          <view class="fft-bar-wrap">
            <canvas canvas-id="result-bar" class="fft-chart-canvas" style="width:100%;height:240px;"></canvas>
          </view>
        </view>

        <!-- 九型概览：3 列网格 -->
        <view class="fft-section">
          <view class="fft-section-title">九型概览</view>
          <view class="fft-overview-grid">
            <view wx:for="{{fftReport.dimensionList}}" wx:key="id" class="fft-overview-card {{item.isPrimary ? 'fft-overview-primary' : ''}} {{item.isSecondary ? 'fft-overview-secondary' : ''}}">
              <view class="fft-overview-head">
                <text class="fft-overview-emoji">{{item.emoji}}</text>
                <view class="fft-overview-badges">
                  <text wx:if="{{item.isPrimary}}" class="fft-badge fft-badge-primary">主型</text>
                  <text wx:if="{{item.isSecondary}}" class="fft-badge fft-badge-secondary">次型</text>
                  <text class="fft-overview-pct">{{item.percent}}%</text>
                </view>
              </view>
              <view class="fft-overview-name">{{item.name}}</view>
              <view class="fft-overview-bar"><view class="fft-overview-fill" style="width:{{item.percent}}%;background-color:{{item.color}};"></view></view>
              <view class="fft-overview-desc">{{fftReport.dimInsights[item.id] || '暂无解读'}}</view>
            </view>
          </view>
        </view>
      </view>

      <view class="fft-footer">
        <view class="fft-footer-text">报告生成时间：{{fftReport.formatDate}}</view>
        <view class="fft-footer-links">
          <navigator url="/pages/test/test?testId=fft" class="fft-footer-link">返回测验</navigator>
          <text class="fft-footer-dot">·</text>
          <navigator url="/pages/index/index" class="fft-footer-link">返回首页</navigator>
        </view>
      </view>
    </view>
  </view>

  <!-- YBT 病娇结果（严格按 web 布局） -->
  <view wx:if="{{testId === 'ybt' && result && ybtReport}}" class="ybt-report-wrap">
    <view class="ybt-report-paper">
      <!-- 头部：profile 图片、标题、四维均分 -->
      <view class="ybt-header">
        <view class="ybt-header-inner">
          <view class="ybt-avatar-wrap">
            <image class="ybt-avatar" src="{{ybtReport.profileImage}}" mode="aspectFill" />
          </view>
          <view class="ybt-header-text">
            <view class="ybt-meta">YBT 病娇测试 · 恋爱占有倾向</view>
            <view class="ybt-title">{{ybtReport.profile.title}}</view>
            <view class="ybt-sub">四维均分 {{ybtReport.avgPercent}}% · 总分 {{ybtReport.totalScore}}/{{ybtReport.maxTotalScore}}</view>
          </view>
        </view>
      </view>

      <view class="ybt-body">
        <!-- 综合结论卡片 -->
        <view class="ybt-section">
          <view class="ybt-conclusion-card {{ybtReport.isRiskProfile ? 'ybt-conclusion-risk' : ''}}">
            <view class="ybt-conclusion-row">
              <view class="ybt-conclusion-img-wrap">
                <image class="ybt-conclusion-img" src="{{ybtReport.profileImage}}" mode="aspectFill" />
              </view>
              <view class="ybt-conclusion-content">
                <view class="ybt-conclusion-title">{{ybtReport.profile.title}} · 综合结论</view>
                <view class="ybt-conclusion-summary">{{ybtReport.profile.summary}}</view>
                <view class="ybt-conclusion-note">{{ybtReport.profile.dominantNote}}</view>
                <view class="ybt-advice-box">
                  <image class="ybt-advice-icon" src="/assets/icons/lightbulb.svg" mode="aspectFit" />
                  <view class="ybt-advice-text">{{ybtReport.profile.advice}}</view>
                </view>
              </view>
            </view>
          </view>
        </view>

        <!-- 四维雷达 -->
        <view class="ybt-section">
          <view class="ybt-section-title"><image class="ybt-section-icon" src="/assets/icons/scan.svg" mode="aspectFit" />四维雷达</view>
          <view class="ybt-radar-wrap">
            <canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:256px;"></canvas>
          </view>
        </view>

        <!-- 四维柱状图 -->
        <view class="ybt-section">
          <view class="ybt-section-title">四维得分</view>
          <view class="ybt-bar-wrap">
            <canvas canvas-id="ybt-bar" class="result-chart-canvas" style="width:320px;height:208px;"></canvas>
          </view>
        </view>

        <!-- 维度解读 -->
        <view class="ybt-section">
          <view class="ybt-section-title">维度解读</view>
          <view wx:for="{{ybtReport.dimensionList}}" wx:key="id" class="ybt-dim-card {{item.isDominant ? 'ybt-dim-dominant' : ''}}">
            <view class="ybt-dim-head">
              <view class="ybt-dim-name-row">
                <image class="ybt-dim-img" src="{{item.dimImage}}" mode="aspectFill" />
                <text class="ybt-dim-name">{{item.name}}</text>
                <text wx:if="{{item.isDominant}}" class="ybt-dim-badge">最高维度</text>
              </view>
              <text class="ybt-dim-meta">{{item.percent}}% · {{item.levelLabel}}</text>
            </view>
            <view class="ybt-dim-bar"><view class="ybt-dim-fill {{item.level === 'high' ? 'ybt-dim-fill-high' : ''}}" style="width:{{item.percent}}%;"></view></view>
            <view wx:if="{{item.levelConclusion}}" class="ybt-level-box">
              <image class="ybt-level-icon" src="/assets/icons/check.svg" mode="aspectFit" />
              <text class="ybt-level-text">{{item.levelConclusion}}</text>
            </view>
            <view class="ybt-dim-desc">{{ybtReport.dimInsights[item.id] || '暂无解读'}}</view>
          </view>
        </view>

        <!-- 恋爱建议 -->
        <view class="ybt-section">
          <view class="ybt-section-title"><image class="ybt-section-icon" src="/assets/icons/chevron-right.svg" mode="aspectFit" />恋爱建议</view>
          <view class="ybt-tips-card">
            <view wx:for="{{ybtReport.relationshipTips}}" wx:key="*this" class="ybt-tip-item">
              <image class="ybt-tip-icon" src="/assets/icons/chevron-right-rose.svg" mode="aspectFit" />
              <text class="ybt-tip-text">{{item}}</text>
            </view>
          </view>
        </view>

        <!-- 说明 -->
        <view class="ybt-section">
          <view class="ybt-disclaimer-card">
            <view class="ybt-disclaimer-title">说明</view>
            <view class="ybt-disclaimer-text">病娇测试从占有欲、控制欲、依赖度、极端倾向四个维度探索恋爱中的倾向。本测验仅供娱乐与自我觉察，不用于诊断。若结果令你困扰，建议咨询专业心理咨询师。</view>
          </view>
        </view>
      </view>

      <view class="ybt-footer">
        <view class="ybt-footer-text">报告生成时间：{{ybtReport.formatDate}}</view>
        <view class="ybt-footer-links">
          <navigator url="/pages/test/test?testId=ybt" class="ybt-footer-link">返回病娇测试</navigator>
          <text class="ybt-footer-dot">·</text>
          <navigator url="/pages/index/index" class="ybt-footer-link">返回首页</navigator>
        </view>
      </view>
    </view>
  </view>

  <!-- RVT 恋爱观结果 - 严格移植 RVTResult.tsx -->
  <view wx:if="{{testId === 'rvt' && result && rvtReport}}" class="rvt-report-wrap">
    <view class="rvt-report-paper">
      <!-- 头部：仿照 web，手机优先纵向居中 -->
      <view class="rvt-header">
        <view class="rvt-header-inner">
          <view class="rvt-icon-wrap">
            <image class="rvt-primary-icon" src="{{rvtReport.primaryIcon}}" mode="aspectFit" />
          </view>
          <view class="rvt-header-text">
            <view class="rvt-meta">RVT 恋爱观测试 · Lee 六种爱情风格</view>
            <view class="rvt-title">你是{{rvtReport.profile.title}}</view>
            <view class="rvt-primary-badge">
              <text class="rvt-badge-lbl">主型</text>
              <text class="rvt-badge-name">{{rvtReport.primaryType.name}}</text>
              <text class="rvt-badge-pct">{{rvtReport.primaryType.percent}}%</text>
            </view>
            <view class="rvt-secondary-badges">
              <view wx:if="{{rvtReport.secondaryType}}" class="rvt-sec-badge">次型 {{rvtReport.secondaryType.name}} {{rvtReport.secondaryType.percent}}%</view>
              <view wx:if="{{rvtReport.thirdName}}" class="rvt-third-badge">{{rvtReport.thirdName}}</view>
            </view>
          </view>
          <!-- 六型得分：紧贴下方 -->
          <view class="rvt-six-type-wrap">
            <view class="rvt-six-type-title">六型得分</view>
            <view class="rvt-six-type-grid">
              <view wx:for="{{rvtReport.sixTypeGrid}}" wx:key="id" class="rvt-six-cell {{item.isPrimary ? 'rvt-six-primary' : ''}} {{item.isSecondary ? 'rvt-six-secondary' : ''}}">
                <text wx:if="{{item.isPrimary}}" class="rvt-six-main-tag">主</text>
                <image class="rvt-six-icon" src="{{item.icon}}" mode="aspectFit" />
                <text class="rvt-six-name">{{item.nameShort}}</text>
                <text class="rvt-six-pct">{{item.percent}}%</text>
              </view>
            </view>
          </view>
        </view>
      </view>

      <view class="rvt-body">
        <!-- 主型解读 -->
        <view class="rvt-section">
          <view class="rvt-section-title-row">
            <image class="rvt-section-icon" src="{{rvtReport.primaryIcon}}" mode="aspectFit" />
            <text class="rvt-section-title">{{rvtReport.profile.title}}</text>
            <view class="rvt-section-badge">主型解读</view>
          </view>
          <view class="rvt-conclusion-card">
            <view class="rvt-conclusion-summary">{{rvtReport.profile.summary}}</view>
            <view class="rvt-conclusion-note">{{rvtReport.profile.dominantNote}}</view>
            <view class="rvt-conclusion-disclaimer">本测验基于 Lee 六种爱情风格，36 题五选一。主型为得分最高维度，次型为第二高。结果仅供自我觉察与娱乐参考，不用于诊断。</view>
          </view>
        </view>

        <!-- 六维雷达 -->
        <view class="rvt-section">
          <view class="rvt-section-title-row">
            <image class="rvt-section-icon" src="/assets/icons/scan.svg" mode="aspectFit" />
            <text class="rvt-section-title">六维雷达</text>
          </view>
          <view class="rvt-radar-wrap">
            <canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas>
          </view>
        </view>

        <!-- 六维柱状图 -->
        <view class="rvt-section">
          <view class="rvt-section-title">六维得分</view>
          <view class="rvt-bar-wrap">
            <canvas canvas-id="result-bar" class="result-chart-canvas" style="width:100%;height:400px;"></canvas>
          </view>
        </view>

        <!-- 维度解读 -->
        <view class="rvt-section">
          <view class="rvt-section-title">维度解读</view>
          <view wx:for="{{rvtReport.dimensionList}}" wx:key="id" class="rvt-dim-card {{item.isDominant ? 'rvt-dim-dominant' : ''}}">
            <view class="rvt-dim-head">
              <view class="rvt-dim-name-row">
                <image class="rvt-dim-icon" src="{{item.dimIcon}}" mode="aspectFit" />
                <text class="rvt-dim-name">{{item.name}}</text>
                <view wx:if="{{item.isDominant}}" class="rvt-dim-dominant-badge">主导型</view>
              </view>
              <text class="rvt-dim-meta">{{item.percent}}% · {{item.levelLabel}}</text>
            </view>
            <view class="rvt-dim-bar">
              <view class="rvt-dim-fill {{item.level === 'high' ? 'rvt-dim-fill-high' : ''}}" style="width:{{item.percent}}%;"></view>
            </view>
            <view wx:if="{{item.levelConclusion}}" class="rvt-level-box">
              <image class="rvt-level-icon" src="/assets/icons/check.svg" mode="aspectFit" />
              <text class="rvt-level-text">{{item.levelConclusion}}</text>
            </view>
            <view class="rvt-dim-desc">{{item.dimInsight}}</view>
          </view>
        </view>

        <!-- 恋爱建议 -->
        <view class="rvt-section">
          <view class="rvt-section-title-row">
            <image class="rvt-section-icon" src="/assets/icons/chevron-right-rose.svg" mode="aspectFit" />
            <text class="rvt-section-title">恋爱建议</text>
          </view>
          <view class="rvt-tips-card">
            <view wx:for="{{rvtReport.relationshipTips}}" wx:key="*this" class="rvt-tip-item">
              <image class="rvt-tip-icon" src="/assets/icons/chevron-right-rose.svg" mode="aspectFit" />
              <text class="rvt-tip-text">{{item}}</text>
            </view>
          </view>
        </view>

        <!-- 说明 -->
        <view class="rvt-section">
          <view class="rvt-disclaimer-card">
            <view class="rvt-disclaimer-title">说明</view>
            <view class="rvt-disclaimer-text">恋爱观测试基于 Lee 六种爱情风格（Eros / Ludus / Storge / Mania / Pragma / Agape），探索你对爱情的信念与期待。本测验仅供自我觉察与娱乐参考，不用于诊断。</view>
          </view>
        </view>
      </view>

      <view class="rvt-footer">
        <view class="rvt-footer-text">报告生成时间：{{rvtReport.formatDate}}</view>
        <view class="rvt-footer-links">
          <navigator url="/pages/test/test?testId=rvt" class="rvt-footer-link">返回恋爱观测试</navigator>
          <text class="rvt-footer-dot">·</text>
          <navigator url="/pages/index/index" class="rvt-footer-link">返回首页</navigator>
        </view>
      </view>
    </view>
  </view>

  <!-- MPT 亲密关系偏好结果 - 严格移植 MPTResult.tsx -->
  <view wx:if="{{testId === 'mpt' && result && mptReport}}" class="mpt-report-wrap">
    <view class="mpt-report-paper">
      <!-- 头部 -->
      <view class="mpt-header">
        <view class="mpt-header-inner">
          <view class="mpt-icon-wrap">
            <image class="mpt-primary-icon" src="{{mptReport.primaryIcon}}" mode="aspectFit" />
          </view>
          <view class="mpt-header-text">
            <view class="mpt-meta">MPT 亲密关系偏好 · 四维分析</view>
            <view class="mpt-title">你是{{mptReport.profile.title}}</view>
            <view class="mpt-primary-badge">
              <text class="mpt-badge-lbl">主型</text>
              <text class="mpt-badge-name">{{mptReport.primaryType.name}}</text>
              <text class="mpt-badge-pct">{{mptReport.primaryType.percent}}%</text>
            </view>
            <view class="mpt-secondary-badges">
              <view wx:if="{{mptReport.secondaryType}}" class="mpt-sec-badge">次型 {{mptReport.secondaryType.name}} {{mptReport.secondaryType.percent}}%</view>
              <view wx:if="{{mptReport.thirdName}}" class="mpt-third-badge">{{mptReport.thirdName}}</view>
            </view>
          </view>
          <!-- 四维得分 -->
          <view class="mpt-four-type-wrap">
            <view class="mpt-four-type-title">四维得分</view>
            <view class="mpt-four-type-grid">
              <view wx:for="{{mptReport.fourTypeGrid}}" wx:key="id" class="mpt-four-cell {{item.isPrimary ? 'mpt-four-primary' : ''}} {{item.isSecondary ? 'mpt-four-secondary' : ''}}">
                <text wx:if="{{item.isPrimary}}" class="mpt-four-main-tag">主</text>
                <image class="mpt-four-icon" src="{{item.icon}}" mode="aspectFit" />
                <text class="mpt-four-name">{{item.nameShort}}</text>
                <text class="mpt-four-pct">{{item.percent}}%</text>
              </view>
            </view>
          </view>
        </view>
      </view>

      <view class="mpt-body">
        <!-- 主型解读 -->
        <view class="mpt-section">
          <view class="mpt-section-title-row">
            <image class="mpt-section-icon" src="{{mptReport.primaryIcon}}" mode="aspectFit" />
            <text class="mpt-section-title">{{mptReport.profile.title}}</text>
            <view class="mpt-section-badge">主型解读</view>
          </view>
          <view class="mpt-conclusion-card">
            <view class="mpt-conclusion-summary">{{mptReport.profile.summary}}</view>
            <view class="mpt-conclusion-note">{{mptReport.profile.dominantNote}}</view>
            <view class="mpt-conclusion-disclaimer">本测验从亲密、激情、浪漫、探索四个维度评估亲密关系偏好，68 题五选一。主型为得分最高维度，次型为第二高。仅供成年人自我觉察，不用于诊断。</view>
          </view>

          <!-- 得分分布特征 -->
          <view wx:if="{{mptReport.patternInfo}}" class="mpt-pattern-card">
            <view class="mpt-pattern-title">{{mptReport.patternInfo.title}}</view>
            <view class="mpt-pattern-analysis">{{mptReport.patternInfo.analysis}}</view>
            <view class="mpt-pattern-implication">{{mptReport.patternInfo.implication}}</view>
          </view>

          <!-- 主次型组合深度解读 -->
          <view wx:if="{{mptReport.comboInsight}}" class="mpt-combo-card">
            <view class="mpt-combo-title">主次型组合解读</view>
            <view class="mpt-combo-text">{{mptReport.comboInsight}}</view>
          </view>

          <!-- 主型深度分析方法 -->
          <view wx:if="{{mptReport.primaryExtended}}" class="mpt-extended-card">
            <view class="mpt-extended-title">主型分析方法</view>
            <view class="mpt-extended-row"><text class="mpt-extended-label">分析说明：</text><text class="mpt-extended-val">{{mptReport.primaryExtended.method}}</text></view>
            <view class="mpt-extended-row"><text class="mpt-extended-label">行为倾向：</text><text class="mpt-extended-val">{{mptReport.primaryExtended.tendency}}</text></view>
            <view class="mpt-extended-row"><text class="mpt-extended-label">发展建议：</text><text class="mpt-extended-val">{{mptReport.primaryExtended.suggestion}}</text></view>
          </view>
        </view>

        <!-- 四维雷达 -->
        <view class="mpt-section">
          <view class="mpt-section-title-row">
            <image class="mpt-section-icon" src="/assets/icons/scan.svg" mode="aspectFit" />
            <text class="mpt-section-title">四维雷达</text>
          </view>
          <view class="mpt-radar-wrap">
            <canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas>
          </view>
        </view>

        <!-- 四维柱状图 -->
        <view class="mpt-section">
          <view class="mpt-section-title">四维得分</view>
          <view class="mpt-bar-wrap">
            <canvas canvas-id="mpt-bar" class="result-chart-canvas" style="width:320px;height:220px;"></canvas>
          </view>
        </view>

        <!-- 维度解读 -->
        <view class="mpt-section">
          <view class="mpt-section-title">维度解读</view>
          <view class="mpt-dim-grid">
            <view wx:for="{{mptReport.dimensionList}}" wx:key="id" class="mpt-dim-card {{item.isDominant ? 'mpt-dim-dominant' : ''}}">
              <view class="mpt-dim-head">
                <view class="mpt-dim-name-row">
                  <image class="mpt-dim-icon" src="{{item.dimIcon}}" mode="aspectFit" />
                  <text class="mpt-dim-name">{{item.name}}</text>
                  <view wx:if="{{item.isDominant}}" class="mpt-dim-dominant-badge">主导型</view>
                </view>
                <text class="mpt-dim-meta">{{item.percent}}% · {{item.levelLabel}}</text>
              </view>
              <view class="mpt-dim-bar"><view class="mpt-dim-fill" style="width:{{item.percent}}%;"></view></view>
              <view wx:if="{{item.levelConclusion}}" class="mpt-level-box">
                <image class="mpt-level-icon" src="/assets/icons/check.svg" mode="aspectFit" />
                <text class="mpt-level-text">{{item.levelConclusion}}</text>
              </view>
              <view wx:if="{{item.extendedSuggestion}}" class="mpt-dim-suggestion">建议：{{item.extendedSuggestion}}</view>
              <view class="mpt-dim-desc">{{item.dimInsight}}</view>
            </view>
          </view>
        </view>

        <!-- 亲密关系建议 -->
        <view class="mpt-section">
          <view class="mpt-section-title-row">
            <image class="mpt-section-icon" src="/assets/icons/chevron-right.svg" mode="aspectFit" />
            <text class="mpt-section-title">亲密关系建议</text>
          </view>
          <view class="mpt-tips-card">
            <view wx:for="{{mptReport.relationshipTips}}" wx:key="*this" class="mpt-tip-item">
              <image class="mpt-tip-icon" src="/assets/icons/chevron-right.svg" mode="aspectFit" />
              <text class="mpt-tip-text">{{item}}</text>
            </view>
          </view>
        </view>

        <!-- 成长方向 -->
        <view wx:if="{{mptReport.growthInfo}}" class="mpt-section">
          <view class="mpt-section-title-row">
            <image class="mpt-section-icon" src="/assets/icons/target.svg" mode="aspectFit" />
            <text class="mpt-section-title">成长方向</text>
          </view>
          <view class="mpt-growth-card">
            <view class="mpt-growth-direction">{{mptReport.growthInfo.direction}}</view>
            <view class="mpt-growth-reason">{{mptReport.growthInfo.reason}}</view>
            <view wx:for="{{mptReport.growthInfo.actions}}" wx:key="*this" class="mpt-growth-item">
              <image class="mpt-growth-icon" src="/assets/icons/check.svg" mode="aspectFit" />
              <text class="mpt-growth-text">{{item}}</text>
            </view>
          </view>
        </view>

        <!-- 潜在注意点 -->
        <view wx:if="{{mptReport.cautionsInfo}}" class="mpt-section">
          <view class="mpt-section-title-row">
            <image class="mpt-section-icon" src="/assets/icons/info.svg" mode="aspectFit" />
            <text class="mpt-section-title">潜在注意点</text>
          </view>
          <view class="mpt-cautions-card">
            <view class="mpt-cautions-title">{{mptReport.cautionsInfo.title}}</view>
            <view wx:for="{{mptReport.cautionsInfo.points}}" wx:key="*this" class="mpt-caution-item">
              <image class="mpt-caution-icon" src="/assets/icons/info.svg" mode="aspectFit" />
              <text class="mpt-caution-text">{{item}}</text>
            </view>
          </view>
        </view>

        <!-- 伴侣匹配与沟通 -->
        <view wx:if="{{mptReport.partnerMatchInfo}}" class="mpt-section">
          <view class="mpt-section-title-row">
            <image class="mpt-section-icon" src="/assets/users-icon.svg" mode="aspectFit" />
            <text class="mpt-section-title">伴侣匹配与沟通</text>
          </view>
          <view class="mpt-partner-card">
            <view class="mpt-partner-lbl">匹配建议</view>
            <view class="mpt-partner-match">{{mptReport.partnerMatchInfo.match}}</view>
            <view class="mpt-partner-lbl">沟通建议</view>
            <view wx:for="{{mptReport.partnerMatchInfo.communicate}}" wx:key="*this" class="mpt-partner-item">
              <image class="mpt-partner-icon" src="/assets/icons/chevron-right.svg" mode="aspectFit" />
              <text class="mpt-partner-text">{{item}}</text>
            </view>
          </view>
        </view>

        <!-- 说明 -->
        <view class="mpt-section">
          <view class="mpt-disclaimer-card">
            <view class="mpt-disclaimer-title">说明</view>
            <view class="mpt-disclaimer-text">本测验从亲密、激情、浪漫、探索四个维度探索你在亲密关系中的偏好。仅供成年人自我觉察与参考，不用于诊断。请在安全与相互尊重的前提下与伴侣沟通偏好与边界。</view>
          </view>
        </view>
      </view>

      <view class="mpt-footer">
        <view class="mpt-footer-text">报告生成时间：{{mptReport.formatDate}}</view>
        <view class="mpt-footer-links">
          <navigator url="/pages/test/test?testId=mpt" class="mpt-footer-link">返回亲密关系偏好测试</navigator>
          <text class="mpt-footer-dot">·</text>
          <navigator url="/pages/index/index" class="mpt-footer-link">返回首页</navigator>
        </view>
      </view>
    </view>
  </view>

  <!-- VBT 易被欺负结果 - 严格移植 VBTResult.tsx -->
  <view wx:if="{{testId === 'vbt' && result && vbtReport}}" class="vbt-report-wrap">
    <view class="vbt-report-paper">
      <view class="vbt-header">
        <view class="vbt-header-inner">
          <view class="vbt-icon-wrap">
            <image class="vbt-primary-icon" src="{{vbtReport.primaryIcon}}" mode="aspectFit" />
          </view>
          <view class="vbt-header-text">
            <view class="vbt-meta">VBT 易被欺负测试 · 四维分析</view>
            <view class="vbt-title">你是{{vbtReport.profile.title}}</view>
            <view class="vbt-zone-badge {{vbtReport.zoneColorClass}}">
              <text class="vbt-badge-lbl">易被欺负指数</text>
              <text class="vbt-badge-num">{{vbtReport.vulnerabilityIndex}}</text>
              <text class="vbt-badge-range">（{{vbtReport.zoneInfo.title}} · {{vbtReport.zoneInfo.range}}）</text>
            </view>
            <view class="vbt-zone-analysis">{{vbtReport.zoneInfo.analysis}}</view>
          </view>
          <view class="vbt-four-type-wrap">
            <view class="vbt-four-type-title">四维得分</view>
            <view class="vbt-four-type-grid">
              <view wx:for="{{vbtReport.fourTypeGrid}}" wx:key="id" class="vbt-four-cell {{item.isStrong ? 'vbt-four-strong' : ''}} {{item.isWeak ? 'vbt-four-weak' : ''}}">
                <image class="vbt-four-icon" src="{{item.icon}}" mode="aspectFit" />
                <text class="vbt-four-name">{{item.nameShort}}</text>
                <text class="vbt-four-pct">{{item.percent}}%</text>
              </view>
            </view>
          </view>
        </view>
      </view>
      <view class="vbt-body">
        <view class="vbt-section">
          <view class="vbt-section-title-row">
            <image class="vbt-section-icon" src="{{vbtReport.primaryIcon}}" mode="aspectFit" />
            <text class="vbt-section-title">{{vbtReport.profile.title}}</text>
            <view class="vbt-section-badge">类型解读</view>
          </view>
          <view class="vbt-conclusion-card">
            <view class="vbt-conclusion-summary">{{vbtReport.profile.summary}}</view>
            <view class="vbt-conclusion-note">{{vbtReport.profile.coreNote}}</view>
            <view class="vbt-conclusion-disclaimer">本测验从边界清晰度、自我主张、敏感度、应对方式四个维度评估「易被欺负」程度。保护力（边界+主张+应对）越高越不易被欺负；敏感度越高越易感到受伤。仅供自我觉察，不用于诊断。</view>
          </view>
          <view class="vbt-zone-card">
            <view class="vbt-zone-card-title">{{vbtReport.zoneInfo.title}}（{{vbtReport.zoneInfo.range}}）</view>
            <view class="vbt-zone-card-analysis">{{vbtReport.zoneInfo.analysis}}</view>
            <view class="vbt-zone-card-implication">{{vbtReport.zoneInfo.implication}}</view>
          </view>
          <view wx:if="{{vbtReport.weakestDimension && vbtReport.weakestDimension.id !== 'sensitive' && vbtReport.weakestExtended}}" class="vbt-weakest-card">
            <view class="vbt-weakest-title">最需关注：{{vbtReport.weakestDimension.name}}</view>
            <view class="vbt-weakest-row"><text class="vbt-weakest-label">分析说明：</text><text class="vbt-weakest-val">{{vbtReport.weakestExtended.method}}</text></view>
            <view class="vbt-weakest-row"><text class="vbt-weakest-label">行为倾向：</text><text class="vbt-weakest-val">{{vbtReport.weakestExtended.tendency}}</text></view>
            <view class="vbt-weakest-row"><text class="vbt-weakest-label">发展建议：</text><text class="vbt-weakest-val">{{vbtReport.weakestExtended.suggestion}}</text></view>
          </view>
        </view>
        <view class="vbt-section">
          <view class="vbt-section-title-row">
            <image class="vbt-section-icon" src="/assets/icons/scan.svg" mode="aspectFit" />
            <text class="vbt-section-title">四维雷达</text>
          </view>
          <view class="vbt-radar-wrap">
            <canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas>
          </view>
        </view>
        <view class="vbt-section">
          <view class="vbt-section-title">保护力维度得分</view>
          <view class="vbt-bar-wrap">
            <canvas canvas-id="vbt-bar" class="result-chart-canvas" style="width:100%;height:220px;"></canvas>
          </view>
        </view>
        <view class="vbt-section">
          <view class="vbt-section-title">维度解读</view>
          <view class="vbt-dim-grid">
            <view wx:for="{{vbtReport.dimensionList}}" wx:key="id" class="vbt-dim-card {{item.isWeak ? 'vbt-dim-weak' : ''}}">
              <view class="vbt-dim-head">
                <view class="vbt-dim-name-row">
                  <image class="vbt-dim-icon" src="{{item.dimIcon}}" mode="aspectFit" />
                  <text class="vbt-dim-name">{{item.name}}</text>
                  <view wx:if="{{item.isWeak && item.isProtectDim}}" class="vbt-dim-weak-badge">需加强</view>
                </view>
                <text class="vbt-dim-meta">{{item.percent}}% · {{item.levelLabel}}</text>
              </view>
              <view class="vbt-dim-bar"><view class="vbt-dim-fill {{item.barColorClass}}" style="width:{{item.percent}}%;"></view></view>
              <view wx:if="{{item.levelConclusion}}" class="vbt-level-box">
                <image class="vbt-level-icon" src="/assets/icons/check.svg" mode="aspectFit" />
                <text class="vbt-level-text">{{item.levelConclusion}}</text>
              </view>
              <view wx:if="{{item.extendedSuggestion}}" class="vbt-dim-suggestion">建议：{{item.extendedSuggestion}}</view>
              <view class="vbt-dim-desc">{{item.dimInsight}}</view>
            </view>
          </view>
        </view>
        <view class="vbt-section">
          <view class="vbt-section-title-row">
            <image class="vbt-section-icon" src="/assets/icons/chevron-right.svg" mode="aspectFit" />
            <text class="vbt-section-title">自我保护行动建议</text>
          </view>
          <view class="vbt-tips-card">
            <view wx:for="{{vbtReport.tips}}" wx:key="*this" class="vbt-tip-item">
              <image class="vbt-tip-icon" src="/assets/icons/chevron-right.svg" mode="aspectFit" />
              <text class="vbt-tip-text">{{item}}</text>
            </view>
          </view>
        </view>
        <view wx:if="{{vbtReport.growthInfo}}" class="vbt-section">
          <view class="vbt-section-title-row">
            <image class="vbt-section-icon" src="/assets/icons/target.svg" mode="aspectFit" />
            <text class="vbt-section-title">成长方向</text>
          </view>
          <view class="vbt-growth-card">
            <view class="vbt-growth-direction">{{vbtReport.growthInfo.direction}}</view>
            <view class="vbt-growth-reason">{{vbtReport.growthInfo.reason}}</view>
          </view>
        </view>
        <view wx:if="{{vbtReport.cautionsInfo}}" class="vbt-section">
          <view class="vbt-section-title-row">
            <image class="vbt-section-icon" src="/assets/icons/info.svg" mode="aspectFit" />
            <text class="vbt-section-title">潜在注意点</text>
          </view>
          <view class="vbt-cautions-card">
            <view wx:for="{{vbtReport.cautionsInfo.points}}" wx:key="*this" class="vbt-caution-item">
              <image class="vbt-caution-icon" src="/assets/icons/info.svg" mode="aspectFit" />
              <text class="vbt-caution-text">{{item}}</text>
            </view>
          </view>
        </view>
        <view class="vbt-section">
          <view class="vbt-disclaimer-card">
            <view class="vbt-disclaimer-title">说明</view>
            <view class="vbt-disclaimer-text">本测验从边界清晰度、自我主张、敏感度、应对方式四个维度探索你在人际互动中的自我保护力。易被欺负指数综合了保护力与敏感度，值越高越需注意边界与自我主张。仅供自我觉察与参考，不用于诊断。若持续感到困扰，建议寻求专业心理咨询支持。</view>
          </view>
        </view>
      </view>
      <view class="vbt-footer">
        <view class="vbt-footer-text">报告生成时间：{{vbtReport.formatDate}}</view>
        <view class="vbt-footer-links">
          <navigator url="/pages/test/test?testId=vbt" class="vbt-footer-link">返回易被欺负测试</navigator>
          <text class="vbt-footer-dot">·</text>
          <navigator url="/pages/index/index" class="vbt-footer-link">返回首页</navigator>
        </view>
      </view>
    </view>
  </view>

  <!-- City 宜居城市结果 - 严格移植 CityResult.tsx -->
  <view wx:if="{{testId === 'city' && result && cityReport}}" class="city-report-wrap">
    <view class="city-report-paper">
      <view class="city-header">
        <view class="city-header-inner">
          <view class="city-icon-wrap">
            <image class="city-primary-icon" src="{{cityReport.primaryIcon}}" mode="aspectFit" />
          </view>
          <view class="city-header-text">
            <view class="city-meta">宜居城市测试 · 五维分析</view>
            <view class="city-title">为你推荐的两座城市</view>
            <view class="city-top-badges">
              <view wx:for="{{cityReport.topCities}}" wx:key="city.id" class="city-badge {{index === 0 ? 'city-badge-first' : 'city-badge-second'}}">
                <text class="city-badge-name">{{item.city.name}}</text>
                <text class="city-badge-province">（{{item.city.province}}）</text>
                <view class="city-badge-match">匹配 {{item.matchPercent}}%</view>
              </view>
            </view>
            <view class="city-profile-line">你是{{cityReport.profile.title}} · {{cityReport.profile.focus}}</view>
          </view>
          <view class="city-five-type-wrap">
            <view class="city-five-type-title">五维偏好</view>
            <view class="city-five-type-grid">
              <view wx:for="{{cityReport.fourTypeGrid}}" wx:key="id" class="city-five-cell">
                <image class="city-five-icon" src="{{item.icon}}" mode="aspectFit" />
                <text class="city-five-name">{{item.nameShort}}</text>
                <text class="city-five-pct">{{item.percent}}%</text>
              </view>
            </view>
          </view>
        </view>
      </view>
      <view class="city-body">
        <view class="city-section">
          <view class="city-section-title-row">
            <image class="city-section-icon" src="{{cityReport.primaryIcon}}" mode="aspectFit" />
            <text class="city-section-title">推荐城市详细分析</text>
          </view>
          <view class="city-detail-cards">
            <view wx:for="{{cityReport.topCities}}" wx:key="city.id" class="city-detail-card {{index === 0 ? 'city-detail-first' : 'city-detail-second'}}">
              <view class="city-detail-row">
                <view class="city-detail-left">
                  <view class="city-detail-header">
                    <view class="city-rec-badge {{index === 0 ? 'city-rec-first' : 'city-rec-second'}}">推荐 {{index + 1}}</view>
                    <view class="city-detail-name">{{item.city.name}}<text class="city-detail-province">（{{item.city.province}}）</text></view>
                  </view>
                  <view class="city-detail-brief">{{item.city.brief}}</view>
                  <view class="city-detail-reason">匹配原因：{{item.matchReason}}</view>
                </view>
                <view class="city-detail-right">
                  <view class="city-detail-percent">{{item.matchPercent}}%</view>
                  <view class="city-detail-percent-lbl">综合匹配度</view>
                </view>
              </view>
              <view class="city-dim-tags">
                <view wx:for="{{item.dimMatchList}}" wx:key="id" class="city-dim-tag">{{item.name}} {{item.percent}}%</view>
              </view>
            </view>
          </view>
        </view>
        <view class="city-section">
          <view class="city-section-title-row">
            <image class="city-section-icon" src="/assets/icons/info.svg" mode="aspectFit" />
            <text class="city-section-title">分析方法说明</text>
          </view>
          <view class="city-methodology-card">
            <view wx:for="{{cityReport.methodology.steps}}" wx:key="step" class="city-methodology-step">
              <view class="city-methodology-num">{{item.step}}</view>
              <view class="city-methodology-content">
                <view class="city-methodology-title">{{item.title}}</view>
                <view class="city-methodology-desc">{{item.desc}}</view>
              </view>
            </view>
            <view class="city-methodology-disclaimer">{{cityReport.methodology.disclaimer}}</view>
          </view>
        </view>
        <view class="city-section">
          <view class="city-section-title-row">
            <image class="city-section-icon" src="/assets/icons/scan.svg" mode="aspectFit" />
            <text class="city-section-title">五维雷达</text>
          </view>
          <view class="city-radar-wrap">
            <canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas>
          </view>
        </view>
        <view class="city-section">
          <view class="city-section-title">五维得分</view>
          <view class="city-bar-wrap">
            <canvas canvas-id="city-bar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas>
          </view>
        </view>
        <view class="city-section">
          <view class="city-section-title">维度解读</view>
          <view class="city-dim-grid">
            <view wx:for="{{cityReport.dimensionList}}" wx:key="id" class="city-dim-card">
              <view class="city-dim-head">
                <view class="city-dim-name-row">
                  <image class="city-dim-icon" src="{{item.dimIcon}}" mode="aspectFit" />
                  <text class="city-dim-name">{{item.name}}</text>
                </view>
                <text class="city-dim-meta">{{item.percent}}% · {{item.levelLabel}}</text>
              </view>
              <view class="city-dim-bar"><view class="city-dim-fill" style="width:{{item.percent}}%;"></view></view>
              <view wx:if="{{item.levelConclusion}}" class="city-level-box">
                <image class="city-level-icon" src="/assets/icons/chevron-right.svg" mode="aspectFit" />
                <text class="city-level-text">{{item.levelConclusion}}</text>
              </view>
              <view class="city-dim-desc">{{item.dimInsight}}</view>
            </view>
          </view>
        </view>
        <view class="city-section">
          <view class="city-section-title-row">
            <image class="city-section-icon" src="{{cityReport.primaryIcon}}" mode="aspectFit" />
            <text class="city-section-title">该类型典型城市参考</text>
          </view>
          <view class="city-typical-card">
            <view class="city-typical-focus">{{cityReport.profile.focus}}</view>
            <view class="city-typical-cities">{{cityReport.profile.typicalCities}}</view>
          </view>
        </view>
        <view class="city-section">
          <view class="city-disclaimer-card">
            <view class="city-disclaimer-title">说明</view>
            <view class="city-disclaimer-text">本测试从气候、生活节奏、文化氛围、成本考量、社交需求五个维度探索你的城市偏好，并基于全国主要城市的五维画像进行匹配推荐。推荐结果仅供自我探索与择城参考，实际择城请结合职业发展、家庭因素等综合决策。</view>
          </view>
        </view>
      </view>
      <view class="city-footer">
        <view class="city-footer-text">报告生成时间：{{cityReport.formatDate}}</view>
        <view class="city-footer-links">
          <navigator url="/pages/test/test?testId=city" class="city-footer-link">返回宜居城市测试</navigator>
          <text class="city-footer-dot">·</text>
          <navigator url="/pages/index/index" class="city-footer-link">返回首页</navigator>
        </view>
      </view>
    </view>
  </view>

  <!-- LBT 恋爱脑结果 - 严格移植 LBTResult.tsx -->
  <view wx:if="{{testId === 'lbt' && result && lbtReport}}" class="lbt-report-wrap">
    <view class="lbt-report-paper">
      <!-- 头部：仿照 web -->
      <view class="lbt-header">
        <view class="lbt-header-inner">
          <view class="lbt-icon-wrap">
            <image class="lbt-header-icon" src="/assets/icons/heart.svg" mode="aspectFit" />
          </view>
          <view class="lbt-header-text">
            <view class="lbt-meta">LBT 恋爱脑测试 · 投入与平衡</view>
            <view class="lbt-title">你是{{lbtReport.profile.title}}</view>
            <view class="lbt-overall-badge">
              <text class="lbt-badge-lbl">整体</text>
              <text class="lbt-badge-val">{{lbtReport.levelLabel}}</text>
              <text class="lbt-badge-pct">{{lbtReport.percent}}%</text>
            </view>
          </view>
          <!-- 三维得分 -->
          <view class="lbt-three-type-wrap">
            <view class="lbt-three-type-title">三维得分</view>
            <view class="lbt-three-type-grid">
              <view wx:for="{{lbtReport.threeTypeGrid}}" wx:key="id" class="lbt-three-cell {{item.isHighest ? 'lbt-three-highest' : ''}}">
                <image class="lbt-three-icon" src="{{item.icon}}" mode="aspectFit" />
                <text class="lbt-three-name">{{item.nameShort}}</text>
                <text class="lbt-three-pct">{{item.percent}}%</text>
              </view>
            </view>
          </view>
        </view>
      </view>

      <view class="lbt-body">
        <!-- 综合结论 -->
        <view class="lbt-section">
          <view class="lbt-section-title-row">
            <image class="lbt-section-icon" src="/assets/icons/heart.svg" mode="aspectFit" />
            <text class="lbt-section-title">{{lbtReport.profile.title}}解读</text>
          </view>
          <view class="lbt-conclusion-card">
            <view class="lbt-conclusion-summary">{{lbtReport.profile.summary}}</view>
            <view class="lbt-conclusion-note">{{lbtReport.profile.dominantNote}}</view>
            <view class="lbt-conclusion-disclaimer">本测验从情感依赖、优先倾斜、理性平衡三维度评估恋爱投入程度。总分 {{lbtReport.totalScore}}/{{lbtReport.maxTotalScore}}。结果仅供自我觉察，不构成专业评估。若困扰持续，建议寻求专业心理咨询。</view>
          </view>
        </view>

        <!-- 三维雷达 -->
        <view class="lbt-section">
          <view class="lbt-section-title-row">
            <image class="lbt-section-icon" src="/assets/icons/scan.svg" mode="aspectFit" />
            <text class="lbt-section-title">三维雷达</text>
          </view>
          <view class="lbt-radar-wrap">
            <canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas>
          </view>
        </view>

        <!-- 三维柱状图 -->
        <view class="lbt-section">
          <view class="lbt-section-title">三维得分</view>
          <view class="lbt-bar-wrap">
            <canvas canvas-id="lbt-bar" class="result-chart-canvas" style="width:320px;height:200px;"></canvas>
          </view>
        </view>

        <!-- 维度解读 -->
        <view class="lbt-section">
          <view class="lbt-section-title">维度解读</view>
          <view wx:for="{{lbtReport.dimensionList}}" wx:key="id" class="lbt-dim-card">
            <view class="lbt-dim-head">
              <view class="lbt-dim-name-row">
                <image class="lbt-dim-icon" src="{{item.dimIcon}}" mode="aspectFit" />
                <text class="lbt-dim-name">{{item.name}}</text>
              </view>
              <text class="lbt-dim-meta">{{item.percent}}% · {{item.levelLabel}}</text>
            </view>
            <view class="lbt-dim-bar">
              <view class="lbt-dim-fill {{item.barColorTeal ? 'lbt-dim-fill-teal' : ''}}" style="width:{{item.percent}}%;"></view>
            </view>
            <view wx:if="{{item.levelConclusion}}" class="lbt-level-box">
              <image class="lbt-level-icon" src="/assets/icons/check.svg" mode="aspectFit" />
              <text class="lbt-level-text">{{item.levelConclusion}}</text>
            </view>
            <view class="lbt-dim-desc">{{item.dimInsight}}</view>
          </view>
        </view>

        <!-- 恋爱建议 -->
        <view class="lbt-section">
          <view class="lbt-section-title-row">
            <image class="lbt-section-icon" src="/assets/icons/chevron-right.svg" mode="aspectFit" />
            <text class="lbt-section-title">恋爱建议</text>
          </view>
          <view class="lbt-tips-card">
            <view wx:for="{{lbtReport.relationshipTips}}" wx:key="*this" class="lbt-tip-item">
              <image class="lbt-tip-icon" src="/assets/icons/chevron-right.svg" mode="aspectFit" />
              <text class="lbt-tip-text">{{item}}</text>
            </view>
          </view>
        </view>
      </view>

      <view class="lbt-footer">
        <view class="lbt-footer-text">报告生成时间：{{lbtReport.formatDate}}</view>
        <view class="lbt-footer-links">
          <navigator url="/pages/test/test?testId=lbt" class="lbt-footer-link">返回恋爱脑测试</navigator>
          <text class="lbt-footer-dot">·</text>
          <navigator url="/pages/index/index" class="lbt-footer-link">返回首页</navigator>
        </view>
      </view>
    </view>
  </view>

  <!-- MBTI 完整报告 - 移植自 MBTIResult.tsx -->
  <view wx:if="{{testId === 'mbti' && result && mbtiReport}}" class="mbti-report-wrap">
    <view class="mbti-report-paper">
      <view class="mbti-header">
        <view class="mbti-header-sub">你的人格类型</view>
        <view class="mbti-header-title">{{result.type}} · {{result.typeInfo.name}}</view>
        <view class="mbti-header-en" wx:if="{{result.typeInfo.nameEn}}">{{result.typeInfo.nameEn}}</view>
        <view class="mbti-slogan" wx:if="{{result.typeInfo.slogan}}">「{{result.typeInfo.slogan}}」</view>
        <view class="mbti-desc">{{result.typeInfo.description}}</view>
        <view class="mbti-strengths" wx:if="{{result.typeInfo.strengths && result.typeInfo.strengths.length}}">
          <view wx:for="{{result.typeInfo.strengths}}" wx:key="*this" class="mbti-strength-tag">#{{item}}</view>
        </view>
      </view>
      <view class="mbti-body">
        <view class="mbti-section">
          <view class="mbti-section-title">四维度详细得分</view>
          <view class="mbti-dim-bars">
            <view wx:for="{{result.dimensionScores}}" wx:key="dimension" class="mbti-dim-bar">
              <view class="mbti-dim-bar-head"><text class="mbti-dim-label">{{item.label}}</text><text class="mbti-dim-scores">{{item.poleALabel}} {{item.scoreA}} · {{item.poleBLabel}} {{item.scoreB}}</text></view>
              <view class="mbti-dim-bar-track">
                <view class="mbti-dim-bar-a" style="width: {{item.scoreA + item.scoreB > 0 ? (item.scoreA / (item.scoreA + item.scoreB)) * 100 : 50}}%"></view>
                <view class="mbti-dim-bar-b"></view>
              </view>
              <view class="mbti-dim-bar-hint">你更偏向 <text class="mbti-bold">{{item.dominantLabel}}</text>，倾向度约 {{item.percentage}}%</view>
            </view>
          </view>
        </view>
        <view class="mbti-divider"></view>
        <view class="mbti-section">
          <view class="mbti-section-title">维度雷达图</view>
          <view class="mbti-chart-row">
            <view class="mbti-radar-wrap"><canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas></view>
            <view class="mbti-radar-legend" wx:if="{{mbtiReport.radarLegendItems && mbtiReport.radarLegendItems.length}}">
              <view class="mbti-legend-title">各维度解读</view>
              <view wx:for="{{mbtiReport.radarLegendItems}}" wx:key="subject" class="mbti-legend-item">
                <view class="mbti-legend-row"><text class="mbti-legend-dim">{{item.subject}}</text><text class="mbti-legend-val">{{item.dominantLabel}} · {{item.value}}/10</text></view>
                <view class="mbti-legend-bar"><view class="mbti-legend-fill" style="width: {{item.pct}}%"></view></view>
              </view>
            </view>
          </view>
        </view>
        <view class="mbti-divider"></view>
        <view class="mbti-section" wx:if="{{mbtiInsight && mbtiInsight.depthAnalysis}}">
          <view class="mbti-section-title">人格深度分析</view>
          <view class="mbti-depth-block">
            <view class="mbti-depth-title">核心特质与内在驱动力</view>
            <view class="mbti-depth-text">{{mbtiInsight.depthAnalysis}}</view>
          </view>
          <view class="mbti-analysis-grid">
            <view class="mbti-analysis-card">
              <view class="mbti-card-title">优势特质详解</view>
              <view wx:if="{{mbtiInsight.strengthDetails && mbtiInsight.strengthDetails.length}}" class="mbti-detail-list">
                <view wx:for="{{mbtiInsight.strengthDetails}}" wx:key="trait" class="mbti-detail-item">
                  <view class="mbti-detail-head"><view class="mbti-dot"></view><text>{{item.trait}}</text></view>
                  <view class="mbti-detail-body">{{item.detail}}</view>
                </view>
              </view>
              <view wx:else class="mbti-simple-list">
                <view wx:for="{{result.typeInfo.strengths}}" wx:key="*this" wx:for-item="s" class="mbti-simple-item"><view class="mbti-dot-sm"></view>{{s}}</view>
              </view>
            </view>
            <view class="mbti-analysis-card mbti-card-weak">
              <view class="mbti-card-title">待发展领域与改进建议</view>
              <view wx:if="{{mbtiInsight.weaknessDetails && mbtiInsight.weaknessDetails.length}}" class="mbti-detail-list">
                <view wx:for="{{mbtiInsight.weaknessDetails}}" wx:key="trait" class="mbti-detail-item">
                  <view class="mbti-detail-head"><view class="mbti-dot mbti-dot-weak"></view><text>{{item.trait}}</text></view>
                  <view class="mbti-detail-body">{{item.suggestion}}</view>
                </view>
              </view>
              <view wx:else class="mbti-simple-list">
                <view wx:for="{{result.typeInfo.weaknesses}}" wx:key="*this" wx:for-item="w" class="mbti-simple-item"><view class="mbti-dot-sm mbti-dot-weak"></view>{{w}}</view>
              </view>
            </view>
            <view class="mbti-analysis-card mbti-card-full" wx:if="{{result.typeInfo.career && result.typeInfo.career.length}}">
              <view class="mbti-card-title">职业发展建议</view>
              <view class="mbti-career-tags"><view wx:for="{{result.typeInfo.career}}" wx:key="*this" class="mbti-career-tag">{{item}}</view></view>
              <view wx:if="{{mbtiInsight && mbtiInsight.growthTips && mbtiInsight.growthTips.length}}" class="mbti-growth-tips">
                <view class="mbti-growth-title">可执行的成长建议</view>
                <view wx:for="{{mbtiInsight.growthTips}}" wx:key="*this" class="mbti-growth-item">✓ {{item}}</view>
              </view>
            </view>
          </view>
        </view>
        <view class="mbti-divider"></view>
        <view class="mbti-section" wx:if="{{result.typeInfo.relationships || result.typeInfo.workStyle || result.typeInfo.stressCoping}}">
          <view class="mbti-section-title">多维应用洞察</view>
          <view class="mbti-insight-grid">
            <view class="mbti-insight-card" wx:if="{{result.typeInfo.relationships}}">
              <view class="mbti-insight-title">亲密关系</view>
              <view class="mbti-insight-text">{{result.typeInfo.relationships}}</view>
              <view wx:if="{{mbtiInsight && mbtiInsight.relationshipsInsight}}" class="mbti-insight-deep">
                <view class="mbti-insight-deep-title">深度洞察</view>
                <view class="mbti-insight-deep-text">{{mbtiInsight.relationshipsInsight}}</view>
              </view>
            </view>
            <view class="mbti-insight-card" wx:if="{{result.typeInfo.workStyle}}">
              <view class="mbti-insight-title">工作与职业</view>
              <view class="mbti-insight-text">{{result.typeInfo.workStyle}}</view>
              <view wx:if="{{mbtiInsight && mbtiInsight.workInsight}}" class="mbti-insight-deep">
                <view class="mbti-insight-deep-title">环境与角色建议</view>
                <view class="mbti-insight-deep-text">{{mbtiInsight.workInsight}}</view>
              </view>
            </view>
            <view class="mbti-insight-card" wx:if="{{result.typeInfo.stressCoping}}">
              <view class="mbti-insight-title">压力应对与成长</view>
              <view class="mbti-insight-text">{{result.typeInfo.stressCoping}}</view>
              <view wx:if="{{mbtiInsight && mbtiInsight.stressInsight}}" class="mbti-insight-deep">
                <view class="mbti-insight-deep-title">压力来源与恢复建议</view>
                <view class="mbti-insight-deep-text">{{mbtiInsight.stressInsight}}</view>
              </view>
            </view>
          </view>
        </view>
        <view class="mbti-divider"></view>
        <view class="mbti-section" wx:if="{{mbtiReport.dimensionCards && mbtiReport.dimensionCards.length}}">
          <view class="mbti-section-title">四维度细致解析</view>
          <view class="mbti-dim-cards">
            <view wx:for="{{mbtiReport.dimensionCards}}" wx:key="title" class="mbti-dim-card">
              <view class="mbti-dim-card-title">{{item.title}}</view>
              <view class="mbti-dim-card-content">{{item.content}}</view>
              <view class="mbti-dim-card-tend">你的倾向：{{item.dominantLabel}} · 倾向度约 {{item.percentage}}%</view>
            </view>
          </view>
        </view>
      </view>
      <view class="mbti-footer">
        <view class="mbti-footer-text">报告生成时间：{{mbtiReport.formatDate}} · MBTI 16型人格测试</view>
        <view class="mbti-footer-note">本测试基于荣格心理学理论，结果仅供自我了解与娱乐参考，不构成专业诊断。</view>
        <view class="mbti-footer-links"><navigator url="/pages/test/test?testId=mbti" class="mbti-footer-link">返回测验</navigator><navigator url="/pages/index/index" class="mbti-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>

  <!-- RPI 完整报告 - 移植自 RPIResult.tsx -->
  <view wx:if="{{testId === 'rpi' && rpiReport}}" class="rpi-report-wrap">
    <view class="rpi-report-paper">
      <view class="rpi-header">
        <view class="rpi-header-inner">
          <view class="rpi-header-sub">Romantic Possessiveness Index</view>
          <view class="rpi-header-title">RPI 恋爱占有欲指数测试</view>
          <view class="rpi-header-desc">40 题双视角完整版 · 评估报告</view>
        </view>
      </view>
      <view class="rpi-meta">
        <view class="rpi-meta-grid">
          <view class="rpi-meta-item"><view class="rpi-meta-label">报告日期</view><view class="rpi-meta-val">{{rpiReport.formatDate}}</view></view>
          <view class="rpi-meta-item"><view class="rpi-meta-label">题数</view><view class="rpi-meta-val">40 题（双视角各 20 题）</view></view>
          <view class="rpi-meta-item"><view class="rpi-meta-label">维度</view><view class="rpi-meta-val">控制 / 嫉妒 / 依赖 / 安全感</view></view>
          <view class="rpi-meta-item"><view class="rpi-meta-label">计分</view><view class="rpi-meta-val">每维度 5–25 分，总分 20–100</view></view>
        </view>
      </view>
      <view class="rpi-body">
        <view class="rpi-card rpi-core-card">
          <view class="rpi-core-inner">
            <view class="rpi-core-left">
              <view class="rpi-core-sub">核心结论卡</view>
              <view class="rpi-core-title">关系占有欲画像</view>
              <view class="rpi-core-desc">{{rpiReport.hasSelf && rpiReport.hasPartner ? '双视角已完成，可对比彼此在控制、嫉妒、依赖与安全感上的差异。' : rpiReport.hasSelf ? '已完成自我视角，继续完成伴侣视角可生成更完整对比。' : '已完成伴侣视角，继续完成自我视角可生成更完整对比。'}}</view>
              <view class="rpi-core-tags">
                <view class="rpi-tag">人格类型：{{rpiReport.personaLabel}}</view>
                <view class="rpi-tag rpi-tag-2">核心结论：{{rpiReport.coreLabel}}</view>
              </view>
            </view>
            <view class="rpi-core-icon-wrap">
              <image class="rpi-core-icon" src="/assets/users-icon.svg" mode="aspectFit" />
            </view>
          </view>
        </view>

        <view class="rpi-divider"></view>
        <view class="rpi-section">
          <view class="rpi-section-title"><view class="rpi-section-num">1</view>可视化概览</view>
          <view class="rpi-section-desc">图表展示四个维度的占有欲强度，便于一眼对比。</view>
          <view class="rpi-chart-wrap">
            <view class="rpi-bar-wrap"><canvas canvas-id="result-rpi-bar" class="result-chart-canvas" style="width:100%;height:200px;"></canvas></view>
          </view>
        </view>

        <view class="rpi-divider"></view>
        <view class="rpi-section">
          <view class="rpi-section-title"><view class="rpi-section-num">2</view>总体结论</view>
          <view class="rpi-section-desc">RPI 从四个维度评估恋爱占有欲：控制欲望、嫉妒强度、情感依赖、关系不安全感。每维度 5 题，维度分为 5 题之和（5–25 分），总分为四维度之和（20–100 分）。等级：1–25 低占有欲、26–50 适中、51–75 偏高、76–100 极高。</view>
          <view class="rpi-incomplete-tip" wx:if="{{!rpiReport.hasSelf || !rpiReport.hasPartner}}">{{rpiReport.hasSelf ? '伴侣视角已作答 ' : '自我视角已作答 '}}{{rpiReport.hasSelf ? rpiReport.partnerAnswered : rpiReport.selfAnswered}}/20，完成后可生成对比报告。</view>
          <view class="rpi-total-cards">
            <view wx:if="{{rpiReport.hasSelf && result.self}}" class="rpi-total-card rpi-level-{{rpiReport.selfLevelClass}}">
              <view class="rpi-total-label">自我视角</view>
              <view class="rpi-total-val">{{result.self.total}} 分 · {{result.self.levelLabel}}</view>
            </view>
            <view wx:if="{{rpiReport.hasPartner && result.partner}}" class="rpi-total-card rpi-level-{{rpiReport.partnerLevelClass}}">
              <view class="rpi-total-label">伴侣视角</view>
              <view class="rpi-total-val">{{result.partner.total}} 分 · {{result.partner.levelLabel}}</view>
            </view>
          </view>
          <view class="rpi-top3-grid">
            <view class="rpi-top3-cell"><view class="rpi-top3-lbl">自我突出维度</view><view class="rpi-top3-name">{{rpiReport.selfTop ? rpiReport.selfTop.name : '暂无'}}</view><view class="rpi-top3-detail">{{rpiReport.selfTop ? '得分 ' + rpiReport.selfTop.scoreSum + ' · ' + rpiReport.selfTop.levelLabel : '完成度 ' + rpiReport.selfAnswered + '/20'}}</view></view>
            <view class="rpi-top3-cell"><view class="rpi-top3-lbl">伴侣突出维度</view><view class="rpi-top3-name">{{rpiReport.partnerTop ? rpiReport.partnerTop.name : '暂无'}}</view><view class="rpi-top3-detail">{{rpiReport.partnerTop ? '得分 ' + rpiReport.partnerTop.scoreSum + ' · ' + rpiReport.partnerTop.levelLabel : '完成度 ' + rpiReport.partnerAnswered + '/20'}}</view></view>
            <view class="rpi-top3-cell"><view class="rpi-top3-lbl">视角差异最大</view><view class="rpi-top3-name">{{rpiReport.maxDiff ? rpiReport.maxDiff.name : '需完成双视角'}}</view><view class="rpi-top3-detail">{{rpiReport.maxDiff ? '差异 ' + (rpiReport.maxDiff.diff > 0 ? '+' : '') + rpiReport.maxDiff.diff : '完成双视角后计算'}}</view></view>
          </view>
        </view>

        <view class="rpi-divider"></view>
        <view class="rpi-section">
          <view class="rpi-section-title"><view class="rpi-section-num">3</view>维度得分</view>
          <view class="rpi-view-switcher" wx:if="{{rpiReport.hasSelf && rpiReport.hasPartner}}">
            <view class="rpi-view-btn {{rpiReport.view === 'both' ? 'rpi-view-active' : ''}}" data-view="both" bindtap="onRpiViewChange">双视角</view>
            <view class="rpi-view-btn {{rpiReport.view === 'self' ? 'rpi-view-active' : ''}}" data-view="self" bindtap="onRpiViewChange">自我视角</view>
            <view class="rpi-view-btn {{rpiReport.view === 'partner' ? 'rpi-view-active' : ''}}" data-view="partner" bindtap="onRpiViewChange">伴侣视角</view>
          </view>
          <view class="rpi-perspective-cards">
            <view wx:if="{{rpiReport.view !== 'partner' && rpiReport.hasSelf && result.self}}" class="rpi-perspective-block">
              <view class="rpi-perspective-header"><text class="rpi-perspective-title">自我视角</text><view class="rpi-perspective-badge">{{result.self.total}} 分 · {{result.self.levelLabel}}</view></view>
              <view wx:for="{{result.self.dimensionScores}}" wx:key="id" class="rpi-dim-row">
                <view class="rpi-dim-left"><text class="rpi-dim-name">{{item.name}}</text><view class="rpi-dim-badge rpi-badge-{{item.level}}">{{item.levelLabel}}</view></view>
                <view class="rpi-dim-hint" wx:if="{{item.hint}}">{{item.hint}}</view>
                <view class="rpi-dim-right"><text class="rpi-dim-mean">均分 {{item.scoreMeanStr}}</text><text class="rpi-dim-sum">{{item.scoreSum}}</text><view class="rpi-dim-bar-wrap"><view class="rpi-dim-bar"><view class="rpi-dim-fill rpi-fill-{{item.level}}" style="width: {{(item.scoreSum/25)*100}}%"></view></view></view></view>
              </view>
            </view>
            <view wx:if="{{rpiReport.view !== 'self' && rpiReport.hasPartner && result.partner}}" class="rpi-perspective-block">
              <view class="rpi-perspective-header"><text class="rpi-perspective-title">伴侣视角</text><view class="rpi-perspective-badge">{{result.partner.total}} 分 · {{result.partner.levelLabel}}</view></view>
              <view wx:for="{{result.partner.dimensionScores}}" wx:key="id" class="rpi-dim-row">
                <view class="rpi-dim-left"><text class="rpi-dim-name">{{item.name}}</text><view class="rpi-dim-badge rpi-badge-{{item.level}}">{{item.levelLabel}}</view></view>
                <view class="rpi-dim-hint" wx:if="{{item.hint}}">{{item.hint}}</view>
                <view class="rpi-dim-right"><text class="rpi-dim-mean">均分 {{item.scoreMeanStr}}</text><text class="rpi-dim-sum">{{item.scoreSum}}</text><view class="rpi-dim-bar-wrap"><view class="rpi-dim-bar"><view class="rpi-dim-fill rpi-fill-{{item.level}}" style="width: {{(item.scoreSum/25)*100}}%"></view></view></view></view>
              </view>
            </view>
          </view>
        </view>

        <view wx:if="{{result.comparison && rpiReport.hasSelf && rpiReport.hasPartner}}" class="rpi-divider"></view>
        <view wx:if="{{result.comparison && rpiReport.hasSelf && rpiReport.hasPartner}}" class="rpi-section">
          <view class="rpi-section-title"><view class="rpi-section-num">4</view>双视角对比</view>
          <view class="rpi-section-desc">{{result.comparison.summary}}</view>
          <view class="rpi-compare-table">
            <view class="rpi-table-row rpi-table-head"><text>维度</text><text>自我</text><text>伴侣</text><text>差异</text></view>
            <view wx:for="{{result.comparison.dimensionDiffs}}" wx:key="id" class="rpi-table-row"><text>{{item.name}}</text><text>{{item.selfSum}}</text><text>{{item.partnerSum}}</text><text>{{item.diff > 0 ? '+' : ''}}{{item.diff}}</text></view>
          </view>
        </view>

        <view class="rpi-divider"></view>
        <view class="rpi-section">
          <view class="rpi-section-title"><view class="rpi-section-num">5</view>沟通与关系建议</view>
          <view class="rpi-advice-list">
            <view class="rpi-advice-item">· 使用「我感到…」而非「你总是…」的表达方式，减少指责感。</view>
            <view class="rpi-advice-item">· 对安全感与边界需求给出可执行的具体约定（如回复频率、与异性相处的底线）。</view>
            <view class="rpi-advice-item">· 若双方在控制、嫉妒或依赖维度上差异较大，可共同讨论各自舒适区与底线。</view>
            <view class="rpi-advice-item">· 本结果仅作关系沟通与自我觉察参考，不替代伴侣咨询或个体心理咨询。</view>
          </view>
        </view>
      </view>
      <view class="rpi-footer">
        <view class="rpi-footer-title">声明</view>
        <view class="rpi-footer-text">本报告依据自评/为伴侣评结果生成，仅供关系沟通与自我觉察参考，不构成任何临床诊断。数据在本地处理，不上传服务器。</view>
        <view class="rpi-footer-meta">{{rpiReport.formatDate}} · RPI 恋爱占有欲指数</view>
        <view class="rpi-footer-links"><navigator url="/pages/rpi-test/rpi-test" class="rpi-footer-link">返回测试</navigator><navigator url="/pages/index/index" class="rpi-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>

  <!-- SRI 性压抑指数 - 未完成时提示 -->
  <view wx:if="{{testId === 'sri' && result && !sriReport}}" class="sri-incomplete-wrap">
    <view class="sri-incomplete-msg">请完成全部 48 题后查看结果</view>
    <navigator url="/pages/test/test?testId=sri" class="sri-incomplete-link">返回继续测试</navigator>
  </view>
  <!-- SRI 性压抑指数完整报告 - 移植自 SRIResult.tsx -->
  <view wx:elif="{{testId === 'sri' && sriReport}}" class="sri-report-wrap">
    <view class="sri-report-paper">
      <view class="sri-header">
        <view class="sri-header-inner">
          <view class="sri-header-sub">Sexual Repression Index</view>
          <view class="sri-header-title">SRI 性压抑指数测试</view>
          <view class="sri-header-desc">四维度评估报告 · 欲望表达 · 观念冲突 · 情绪紧张 · 行为抑制</view>
        </view>
      </view>
      <view class="sri-meta">
        <view class="sri-meta-grid">
          <view class="sri-meta-item"><view class="sri-meta-label">报告日期</view><view class="sri-meta-val">{{sriReport.formatDate}}</view></view>
          <view class="sri-meta-item"><view class="sri-meta-label">题数</view><view class="sri-meta-val">48 题</view></view>
          <view class="sri-meta-item"><view class="sri-meta-label">维度</view><view class="sri-meta-val">欲望表达 / 观念冲突 / 情绪紧张 / 行为抑制</view></view>
          <view class="sri-meta-item"><view class="sri-meta-label">指数范围</view><view class="sri-meta-val">0–100（越高越压抑）</view></view>
        </view>
      </view>
      <view class="sri-body">
        <view class="sri-card sri-core-card">
          <view class="sri-core-inner">
            <view class="sri-core-left">
              <view class="sri-core-sub">核心结论卡</view>
              <view class="sri-core-title">综合结论：{{result.levelLabel}}</view>
              <view class="sri-core-desc">当前性压抑指数为 {{result.totalIndex}} 分，主要影响集中在{{sriReport.topDim ? sriReport.topDim.name : '核心维度'}}。</view>
              <view class="sri-core-tags">
                <view class="sri-tag">人格类型：{{result.levelLabel}}型</view>
                <view class="sri-tag sri-tag-2">核心结论：{{sriReport.topDim ? sriReport.topDim.name : '待完成'}}</view>
              </view>
            </view>
            <view class="sri-core-icon-wrap">
              <image class="sri-core-icon" src="/assets/icons/person-intimate.svg" mode="aspectFit" />
            </view>
          </view>
        </view>
        <view class="sri-divider"></view>
        <view class="sri-section">
          <view class="sri-section-title"><view class="sri-section-num">1</view>可视化概览</view>
          <view class="sri-section-desc">通过图表直观看到四维度得分分布与压抑程度。</view>
          <view class="sri-radar-wrap" wx:if="{{sriReport.radarData && sriReport.radarData.length}}">
            <view class="sri-chart-title">四维雷达图</view>
            <canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas>
          </view>
          <view class="sri-chart-wrap">
            <canvas canvas-id="result-sri-bar" class="result-chart-canvas" style="width:100%;height:200px;"></canvas>
          </view>
        </view>
        <view class="sri-divider"></view>
        <view class="sri-section">
          <view class="sri-section-title"><view class="sri-section-num">2</view>总体结论</view>
          <view class="sri-section-desc">SRI 从四个维度评估性压抑程度：欲望表达、观念冲突、情绪紧张、行为抑制。指数 0–100，越高表示压抑程度越高。等级：0–20 很低、20–40 偏低、40–60 中等、60–80 偏高、80–100 很高。</view>
          <view class="sri-total-card sri-total-{{sriReport.totalLevelClass}}">
            <view class="sri-total-row">
              <view><view class="sri-total-label">性压抑指数</view><view class="sri-total-val">{{result.totalIndex}} 分 · {{result.levelLabel}}</view></view>
              <view class="sri-total-bar-wrap"><view class="sri-total-bar"><view class="sri-total-fill" style="width: {{result.totalIndex}}%"></view></view></view>
            </view>
          </view>
          <view class="sri-stats-grid">
            <view class="sri-stat-cell"><view class="sri-stat-lbl">总体指数</view><view class="sri-stat-val">{{result.totalIndex}}</view><view class="sri-stat-hint">0–100</view></view>
            <view class="sri-stat-cell"><view class="sri-stat-lbl">等级</view><view class="sri-stat-val">{{result.levelLabel}}</view><view class="sri-stat-hint">五等级</view></view>
            <view class="sri-stat-cell"><view class="sri-stat-lbl">最高维度</view><view class="sri-stat-val">{{sriReport.topDim ? sriReport.topDim.name : '暂无'}}</view><view class="sri-stat-hint">{{sriReport.topDimHint}}</view></view>
            <view class="sri-stat-cell"><view class="sri-stat-lbl">完成度</view><view class="sri-stat-val">{{sriReport.answeredCount}}/48</view><view class="sri-stat-hint">全部完成</view></view>
          </view>
        </view>
        <view class="sri-divider"></view>
        <view class="sri-section">
          <view class="sri-section-title"><view class="sri-section-num">3</view>四维度得分</view>
          <view class="sri-dim-table">
            <view wx:for="{{sriReport.dimScoresForView}}" wx:key="id" class="sri-dim-row">
              <view class="sri-dim-left"><text class="sri-dim-name">{{item.name}}</text><view class="sri-dim-badge sri-badge-{{item.level}}">{{item.levelLabel}}</view></view>
              <view class="sri-dim-hint">{{item.hint}}</view>
              <view class="sri-dim-right"><text class="sri-dim-mean">均分 {{item.scoreStr}}</text><text class="sri-dim-index">{{item.index}}</text><view class="sri-dim-bar-wrap"><view class="sri-dim-bar"><view class="sri-dim-fill sri-fill-{{item.level}}" style="width: {{item.pct}}%"></view></view></view></view>
            </view>
          </view>
        </view>
        <view class="sri-divider"></view>
        <view class="sri-section">
          <view class="sri-section-title"><view class="sri-section-num">4</view>多角度解读</view>
          <view class="sri-section-desc">以下从维度组合与突出维度两个角度对结果进行解读。</view>
          <view class="sri-interp-card">
            <view class="sri-interp-title">突出维度（得分最高的 3 个）</view>
            <view wx:for="{{result.top3Dimensions}}" wx:key="id" class="sri-interp-item">{{index + 1}}. {{item.name}}（{{item.levelLabel}}）{{item.hint}}</view>
          </view>
          <view class="sri-interp-card">
            <view class="sri-interp-title">观念与表达</view>
            <view class="sri-interp-text">{{sriReport.conflictExpressionText}}</view>
          </view>
          <view class="sri-interp-card">
            <view class="sri-interp-title">情绪与行为</view>
            <view class="sri-interp-text">{{sriReport.anxietyInhibitionText}}</view>
          </view>
          <view class="sri-interp-card">
            <view class="sri-interp-title">综合视角</view>
            <view class="sri-interp-text">{{sriReport.summaryText}}</view>
          </view>
        </view>
        <view class="sri-divider"></view>
        <view class="sri-section">
          <view class="sri-section-title"><view class="sri-section-num">5</view>建议方向</view>
          <view class="sri-advice-list">
            <view class="sri-advice-item">· 用「我需要/我不需要」表达感受，减少自我批评与「应该」的绑架。</view>
            <view class="sri-advice-item">· 与可信赖的伴侣或对象建立安全的对话节奏，逐步明确边界与舒适区。</view>
            <view class="sri-advice-item">· 区分「紧张」与「不愿意」，在自愿前提下小步增加表达或身体接触。</view>
            <view class="sri-advice-item">· 本结果仅作自我了解与沟通参考，不构成任何临床诊断；如有持续困扰可寻求伴侣/性心理咨询。</view>
          </view>
        </view>
      </view>
      <view class="sri-footer">
        <view class="sri-footer-title">声明</view>
        <view class="sri-footer-text">本报告依据自评结果生成，仅供自我探索与沟通参考，不构成任何临床诊断。数据在本地处理，不上传服务器。</view>
        <view class="sri-footer-meta">{{sriReport.formatDate}} · SRI 性压抑指数</view>
        <view class="sri-footer-links"><navigator url="/pages/test/test?testId=sri" class="sri-footer-link">返回测试</navigator><navigator url="/pages/index/index" class="sri-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>

  <!-- 人格动物塑 - 未完成时提示 -->
  <view wx:if="{{testId === 'animal' && result && !animalReport}}" class="animal-incomplete-wrap">
    <view class="animal-incomplete-msg">请完成至少 30 题后查看结果（共 60 题）</view>
    <navigator url="/pages/animal-test/animal-test" class="animal-incomplete-link">返回继续测试</navigator>
  </view>
  <!-- 人格动物塑完整报告 - 移植自 AnimalResult.tsx -->
  <view wx:elif="{{testId === 'animal' && animalReport}}" class="animal-report-wrap">
    <view class="animal-report-paper">
      <view class="animal-header">
        <view class="animal-icon-wrap">{{animalReport.mainAnimal.emoji}}</view>
        <view class="animal-header-sub">您的精神动物是</view>
        <view class="animal-header-title">{{animalReport.mainAnimal.name}}</view>
        <view class="animal-header-desc">{{animalReport.mainAnimal.description}}</view>
        <view class="animal-traits">
          <view wx:for="{{animalReport.mainAnimal.traits}}" wx:key="*this" class="animal-trait-tag">#{{item}}</view>
        </view>
        <view wx:if="{{animalReport.mainAnimal.weaknesses && animalReport.mainAnimal.weaknesses.length}}" class="animal-weaknesses">
          <view class="animal-weak-label">潜在弱点</view>
          <view class="animal-weak-tags">
            <view wx:for="{{animalReport.mainAnimal.weaknesses}}" wx:key="*this" class="animal-weak-tag">{{item}}</view>
          </view>
        </view>
      </view>
      <view class="animal-body">
        <view class="animal-section">
          <view class="animal-section-title">雷达图 · 维度解析</view>
          <view class="animal-charts-row">
            <view class="animal-radar-wrap">
              <canvas canvas-id="result-radar" class="result-chart-canvas" style="width:100%;height:280px;"></canvas>
            </view>
            <view class="animal-dim-list">
              <view wx:for="{{animalReport.radarData}}" wx:key="name" class="animal-dim-item">
                <view class="animal-dim-head"><text>{{item.name}}</text><text class="animal-dim-score">{{item.score}} / 10</text></view>
                <view class="animal-dim-bar"><view class="animal-dim-fill" style="width: {{(item.score/10)*100}}%"></view></view>
              </view>
            </view>
          </view>
        </view>
        <view class="animal-divider"></view>
        <view class="animal-section">
          <view class="animal-section-title">综合特质分析</view>
          <view class="animal-compound-grid">
            <view wx:for="{{animalReport.compoundRows}}" wx:key="key" class="animal-compound-item">
              <view class="animal-compound-head"><view class="animal-compound-label">{{item.label}}</view><view class="animal-compound-right"><view class="animal-compound-status">{{item.status}}</view><text class="animal-compound-val">{{item.value.toFixed(1)}}</text></view></view>
              <view class="animal-compound-bar"><view class="animal-compound-fill" style="width: {{(item.value/10)*100}}%"></view></view>
            </view>
          </view>
        </view>
        <view class="animal-divider"></view>
        <view class="animal-section">
          <view class="animal-section-title">潜在特质：第二动物形态</view>
          <view class="animal-secondary-card">
            <view class="animal-secondary-icon">{{animalReport.secondaryAnimal.emoji}}</view>
            <view class="animal-secondary-content">
              <view class="animal-secondary-name">{{animalReport.secondaryAnimal.name}}</view>
              <view class="animal-secondary-traits">
                <text wx:for="{{animalReport.secondaryAnimal.traits}}" wx:key="*this" class="animal-sec-tag">#{{item}}</text>
              </view>
              <view class="animal-secondary-desc">除了{{animalReport.mainAnimal.name}}的主导特质外，你还表现出{{animalReport.secondaryAnimal.name}}的一面。{{animalReport.secondaryAnimal.description}}</view>
            </view>
          </view>
        </view>
        <view class="animal-divider"></view>
        <view class="animal-section">
          <view class="animal-section-title">深度性格分析</view>
          <view class="animal-analysis-grid">
            <view class="animal-analysis-card">
              <view class="animal-analysis-title">能量来源 (E)</view>
              <view class="animal-analysis-text">{{animalReport.analysisE}}</view>
            </view>
            <view class="animal-analysis-card">
              <view class="animal-analysis-title">思维模式 (O)</view>
              <view class="animal-analysis-text">{{animalReport.analysisO}}</view>
            </view>
            <view class="animal-analysis-card">
              <view class="animal-analysis-title">决策风格 (A/C)</view>
              <view class="animal-analysis-text">{{animalReport.analysisAC}}</view>
            </view>
            <view class="animal-analysis-card">
              <view class="animal-analysis-title">抗压能力 (N)</view>
              <view class="animal-analysis-text">{{animalReport.analysisN}}</view>
            </view>
          </view>
        </view>
      </view>
      <view class="animal-footer">
        <view class="animal-footer-meta">{{animalReport.formatDate}} · 动物塑性格测试</view>
        <view class="animal-footer-text">本测试基于FFM五因素模型与动物心理学趣味映射，结果仅供娱乐与参考。</view>
        <view class="animal-footer-links"><navigator url="/pages/animal-test/animal-test" class="animal-footer-link">返回测验</navigator><navigator url="/pages/index/index" class="animal-footer-link">返回首页</navigator></view>
      </view>
    </view>
  </view>
  </view>
  <theme-switcher bindthemechange="onThemeChange"></theme-switcher>
</view>

<view class="page-wrap" style="{{themeStyle}}" wx:else>
  <nav-header currentPath="/"></nav-header>
  <view class="empty">
    <text>请先完成测评</text>
    <navigator url="/pages/index/index" class="link">返回首页</navigator>
  </view>
  <theme-switcher bindthemechange="onThemeChange"></theme-switcher>
</view>
