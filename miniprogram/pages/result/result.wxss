.page-wrap {
  min-height: 100vh;
  padding-bottom: 120rpx;
  overflow-x: hidden;
}

.container {
  padding: 24rpx;
  overflow-x: hidden;
  box-sizing: border-box;
  max-width: 100vw;
}

/* 所有报告容器不超出屏幕 */
[class$="-report-wrap"],
[class$="-report-paper"] {
  max-width: 100%;
  overflow-x: hidden;
  box-sizing: border-box;
}

.actions {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.link {
  font-size: 28rpx;
  color: var(--xia-deep);
}

.retake {
  font-size: 28rpx;
  color: var(--xia-teal);
  text-decoration: underline;
}

.result-card {
  margin-bottom: 24rpx;
}

.result-title {
  font-size: 36rpx;
  font-weight: 600;
  color: var(--xia-deep);
  margin-bottom: 8rpx;
}

.result-subtitle {
  font-size: 30rpx;
  color: var(--xia-teal);
  margin-bottom: 8rpx;
}

.result-meta {
  font-size: 26rpx;
  color: var(--xia-deep);
  margin-bottom: 24rpx;
}

.result-desc {
  font-size: 28rpx;
  color: var(--xia-deep);
  line-height: 1.6;
  margin-bottom: 24rpx;
}

.factor-radar-section { margin-bottom: 32rpx; }
.factor-radar-title { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 16rpx; }
.factor-radar-wrap { width: 100%; min-height: 320rpx; }

.factor-list {
  margin-bottom: 24rpx;
}

.factor-item {
  margin-bottom: 20rpx;
}

.factor-name {
  font-size: 26rpx;
  font-weight: 500;
  color: var(--xia-deep);
  margin-bottom: 8rpx;
}

.factor-bar-wrap {
  height: 12rpx;
  background: linear-gradient(90deg, rgba(var(--xia-deep-rgb), 0.1) 0%, rgba(var(--xia-deep-rgb), 0.2) 100%);
  border-radius: 999rpx;
  overflow: hidden;
  display: inline-block;
  width: 60%;
  margin-right: 16rpx;
}

.factor-bar {
  height: 100%;
  background: linear-gradient(90deg, var(--xia-teal) 0%, var(--xia-deep) 100%);
  border-radius: 999rpx;
}

.factor-score {
  font-size: 24rpx;
  color: var(--xia-deep);
  display: inline;
}

.section-label {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--xia-deep);
  margin: 24rpx 0 16rpx;
  padding-top: 16rpx;
  border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
}

.factor-row {
  display: flex;
  justify-content: space-between;
  padding: 12rpx 0;
  font-size: 26rpx;
  color: rgba(var(--xia-deep-rgb), 0.8);
}

.factor-row .factor-name {
  margin-bottom: 0;
}

.factor-val {
  color: rgba(var(--xia-deep-rgb), 0.6);
}

.dim-scores {
  margin-top: 16rpx;
}

.dim-row {
  padding: 8rpx 0;
  font-size: 26rpx;
  color: rgba(var(--xia-deep-rgb), 0.8);
}

.rpi-block {
  padding-bottom: 24rpx;
}

.empty {
  padding: 100rpx;
  text-align: center;
  color: rgba(var(--xia-deep-rgb), 0.6);
}

.empty .link {
  display: block;
  margin-top: 24rpx;
}

/* ========== SCL90 完整报告 - 移植自 SCL90Result.tsx ========== */
.scl90-report-wrap { padding: 0 0 120rpx; }
.scl90-report-paper {
  border-radius: 32rpx;
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: #fff;
  overflow: hidden;
  box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08);
}

.scl90-header {
  background: var(--xia-deep);
  padding: 48rpx 32rpx 64rpx;
  color: #fff;
}
.scl90-header-sub { font-size: 22rpx; opacity: 0.8; letter-spacing: 0.1em; }
.scl90-header-title { font-size: 44rpx; font-weight: 700; margin-top: 16rpx; }
.scl90-header-desc { font-size: 28rpx; opacity: 0.8; margin-top: 8rpx; }

.scl90-meta {
  background: rgba(var(--xia-deep-rgb), 0.04);
  border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.1);
  padding: 40rpx 32rpx;
}
.scl90-meta-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24rpx; }
.scl90-meta-label { font-size: 20rpx; font-weight: 600; color: var(--xia-deep); text-transform: uppercase; }
.scl90-meta-val { font-size: 26rpx; font-weight: 500; color: var(--xia-deep); margin-top: 8rpx; }

.scl90-body { padding: 48rpx 32rpx; }

/* 通用卡片 - 主题色渐变，保证可读性 */
.scl90-card {
  background: linear-gradient(135deg, rgba(var(--xia-sky-rgb), 0.06) 0%, #fff 45%, rgba(var(--xia-cream-rgb), 0.12) 100%);
  border-radius: 24rpx;
  padding: 40rpx 36rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 8rpx 32rpx -8rpx rgba(var(--xia-aqua-rgb), 0.12);
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
}

/* 卡片1：心理健康画像 */
.scl90-card-header {
  display: flex;
  align-items: center;
  gap: 12rpx;
}
.scl90-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--xia-aqua) 0%, var(--xia-teal) 100%);
  box-shadow: 0 0 12rpx rgba(var(--xia-teal-rgb), 0.4);
}
.scl90-card-title {
  font-size: 26rpx;
  font-weight: 500;
  color: var(--xia-deep);
}
.scl90-main-result {
  font-size: 64rpx;
  font-weight: 700;
  color: var(--xia-teal);
  margin-top: 20rpx;
  letter-spacing: -0.02em;
  text-shadow: 0 2rpx 8rpx rgba(var(--xia-teal-rgb), 0.2);
}
.scl90-desc {
  font-size: 28rpx;
  color: var(--xia-deep);
  line-height: 1.75;
  margin-top: 24rpx;
}
.scl90-core-chip {
  display: inline-flex;
  align-items: center;
  gap: 16rpx;
  margin-top: 32rpx;
  padding: 18rpx 28rpx;
  border-radius: 999rpx;
  background: rgba(var(--xia-deep-rgb), 0.05);
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.15);
}
.scl90-core-lbl { font-size: 26rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.7); }
.scl90-core-val { font-size: 28rpx; font-weight: 700; color: var(--xia-teal); }

/* 卡片2：突出维度 */
.scl90-dim-card { padding: 32rpx 36rpx; }
.scl90-dim-row {
  display: flex;
  align-items: flex-start;
  gap: 32rpx;
}
.scl90-dim-label {
  font-size: 26rpx;
  font-weight: 500;
  color: var(--xia-deep);
  flex-shrink: 0;
}
.scl90-dim-items {
  flex: 1;
  display: flex;
  justify-content: space-between;
  gap: 24rpx;
}
.scl90-dim-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}
.scl90-dim-name {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--xia-deep);
  text-align: center;
}
.scl90-dim-score {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--xia-teal);
}

/* 卡片3：测试总分 */
.scl90-score-card {}
.scl90-score-top {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}
.scl90-score-left {}
.scl90-score-label {
  font-size: 26rpx;
  font-weight: 500;
  color: var(--xia-deep);
  display: block;
}
.scl90-score-main {
  display: flex;
  align-items: baseline;
  gap: 8rpx;
  margin-top: 8rpx;
}
.scl90-score-num {
  font-size: 72rpx;
  font-weight: 700;
  color: var(--xia-teal);
  letter-spacing: -0.02em;
}
.scl90-score-max {
  font-size: 28rpx;
  font-weight: 500;
  color: rgba(var(--xia-deep-rgb), 0.6);
}
.scl90-bar-icon {
  display: flex;
  align-items: flex-end;
  gap: 6rpx;
  height: 64rpx;
}
.scl90-bar-1, .scl90-bar-2, .scl90-bar-3 {
  width: 12rpx;
  border-radius: 4rpx 4rpx 0 0;
  background: linear-gradient(180deg, var(--xia-teal) 0%, var(--xia-deep) 100%);
}
.scl90-bar-1 { height: 24rpx; opacity: 0.6; }
.scl90-bar-2 { height: 40rpx; opacity: 0.8; }
.scl90-bar-3 { height: 56rpx; opacity: 1; }

.scl90-score-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24rpx;
  margin-top: 28rpx;
}
.scl90-score-cell {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  padding: 20rpx;
  border-radius: 16rpx;
  background: rgba(var(--xia-deep-rgb), 0.05);
}
.scl90-cell-lbl {
  font-size: 26rpx;
  font-weight: 500;
  color: var(--xia-deep);
}
.scl90-cell-val {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--xia-teal);
}

.scl90-top-dim {
  margin-top: 32rpx;
  padding-top: 24rpx;
  border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.15);
}
.scl90-top-dim-row {
  display: flex;
  align-items: center;
  gap: 24rpx;
}
.scl90-top-dim-label {
  font-size: 26rpx;
  font-weight: 500;
  color: var(--xia-deep);
  flex-shrink: 0;
}
.scl90-top-dim-bar-wrap {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 16rpx;
  min-width: 0;
}
.scl90-top-dim-bar {
  flex: 1;
  height: 14rpx;
  background: rgba(var(--xia-deep-rgb), 0.08);
  border-radius: 999rpx;
  overflow: hidden;
  min-width: 80rpx;
}
.scl90-top-dim-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--xia-aqua) 0%, var(--xia-teal) 50%, var(--xia-deep) 100%);
  border-radius: 999rpx;
  transition: width 0.3s ease;
}
.scl90-top-dim-val {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--xia-teal);
  flex-shrink: 0;
}

.scl90-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.15); margin: 64rpx 0; }

.scl90-section { margin-bottom: 48rpx; }
.scl90-section-title { display: flex; align-items: center; gap: 16rpx; font-size: 36rpx; font-weight: 700; color: var(--xia-deep); }
.scl90-section-num { width: 56rpx; height: 56rpx; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: var(--xia-deep); color: #fff; font-size: 24rpx; font-weight: 700; }
.scl90-section-desc { font-size: 28rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 32rpx; }
.scl90-section-text { font-size: 28rpx; color: var(--xia-deep); line-height: 1.7; margin-top: 32rpx; }
.scl90-bold { font-weight: 700; color: var(--xia-deep); }

.scl90-charts-row { display: flex; flex-direction: column; gap: 32rpx; margin-top: 48rpx; overflow: hidden; max-width: 100%; }
.scl90-chart-left, .scl90-chart-right { width: 100%; }
.scl90-pie-legend { display: flex; flex-wrap: wrap; gap: 24rpx; margin-top: 24rpx; font-size: 22rpx; color: var(--xia-deep); }
.scl90-legend-item { display: flex; align-items: center; gap: 12rpx; }
.scl90-legend-dot { width: 16rpx; height: 16rpx; border-radius: 50%; }
.scl90-bar-wrap { margin-top: 40rpx; }
.result-chart-canvas, .result-radar-canvas { width: 320px !important; max-width: 100%; display: block; margin: 0 auto; }
.fft-chart-canvas { width: 320px !important; max-width: 100%; display: block; margin: 0 auto; }

/* 全局：图表/卡片容器不超出屏幕 */
.chart-wrap, .radar-wrap, .bar-wrap,
.scl90-chart-left, .scl90-chart-right, .scl90-bar-wrap,
.aat-radar-wrap, .aat-bar-wrap,
.psych-age-radar-wrap, .psych-age-bar-wrap,
.apt-radar-wrap, .apt-bar-wrap,
.hit-chart-wrap, .dth-chart-wrap,
.tla-chart-wrap, .fft-radar-wrap, .fft-bar-wrap,
.ybt-radar-wrap, .ybt-bar-wrap, .rvt-radar-wrap, .rvt-bar-wrap,
.mpt-radar-wrap, .mpt-bar-wrap, .vbt-radar-wrap, .vbt-bar-wrap,
.city-radar-wrap, .city-bar-wrap, .lbt-radar-wrap, .lbt-bar-wrap,
.sri-radar-wrap, .animal-radar-wrap, .mbti-radar-wrap,
.factor-radar-wrap {
  width: 100%;
  max-width: 100%;
  overflow: hidden;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.scl90-factor-table, .scl90-indicator-table {
  border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: #fff; overflow: hidden; margin-top: 32rpx;
}
.scl90-table-row { display: grid; grid-template-columns: 60rpx 1fr 80rpx 100rpx 120rpx; gap: 16rpx; padding: 24rpx 32rpx; align-items: center; font-size: 26rpx; }
.scl90-factor-table .scl90-table-row > view:last-child { justify-self: center; }
.scl90-indicator-table .scl90-table-row { grid-template-columns: 1fr 120rpx 1fr; }
.scl90-table-head { background: rgba(var(--xia-deep-rgb), 0.06); font-weight: 600; color: var(--xia-deep); font-size: 24rpx; }
.scl90-table-alt { background: rgba(var(--xia-deep-rgb), 0.03); }
.scl90-score-normal { color: var(--xia-sky); font-weight: 700; }
.scl90-score-mild { color: var(--xia-aqua); font-weight: 700; }
.scl90-score-moderate { color: var(--xia-teal); font-weight: 700; }
.scl90-score-severe { color: var(--xia-deep); font-weight: 700; }

.scl90-level-badge {
  display: inline-flex; padding: 8rpx 20rpx; border-radius: 999rpx;
  font-size: 22rpx; font-weight: 600;
}
.scl90-level-normal { background: rgba(var(--xia-deep-rgb), 0.06); color: var(--xia-deep); border: 2rpx solid rgba(var(--xia-deep-rgb), 0.15); }
.scl90-level-mild { background: rgba(var(--xia-aqua-rgb), 0.15); color: var(--xia-deep); border: 2rpx solid rgba(var(--xia-aqua-rgb), 0.4); }
.scl90-level-moderate { background: rgba(var(--xia-teal-rgb), 0.15); color: var(--xia-deep); border: 2rpx solid rgba(var(--xia-teal-rgb), 0.5); }
.scl90-level-severe { background: rgba(var(--xia-deep-rgb), 0.12); color: var(--xia-deep); border: 2rpx solid rgba(var(--xia-deep-rgb), 0.4); }

.scl90-total-conclusion {
  border-radius: 32rpx; border: 2rpx solid; padding: 40rpx; margin-top: 32rpx;
}
.scl90-conclusion-normal { border-color: rgba(var(--xia-deep-rgb), 0.15); background: rgba(var(--xia-deep-rgb), 0.04); }
.scl90-conclusion-mild { border-color: rgba(var(--xia-aqua-rgb), 0.4); background: rgba(var(--xia-aqua-rgb), 0.1); }
.scl90-conclusion-moderate { border-color: rgba(var(--xia-teal-rgb), 0.5); background: rgba(var(--xia-teal-rgb), 0.1); }
.scl90-conclusion-severe { border-color: rgba(var(--xia-deep-rgb), 0.35); background: rgba(var(--xia-deep-rgb), 0.08); }
.scl90-conclusion-row { display: flex; align-items: center; gap: 16rpx; }
.scl90-conclusion-label { font-size: 28rpx; font-weight: 700; }
.scl90-conclusion-val { font-size: 36rpx; font-weight: 700; }
.scl90-conclusion-desc { font-size: 28rpx; color: var(--xia-deep); opacity: 0.9; margin-top: 12rpx; line-height: 1.5; }

.scl90-judge-note { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.7); margin-top: 24rpx; }

.scl90-top3-card {
  border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: rgba(var(--xia-deep-rgb), 0.04); padding: 40rpx; margin-top: 32rpx;
}
.scl90-top3-item { padding: 0; }
.scl90-top3-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 32rpx 0; }
.scl90-top3-row { display: flex; gap: 32rpx; }
.scl90-top3-num { width: 64rpx; height: 64rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: var(--xia-deep); color: #fff; font-size: 28rpx; font-weight: 700; }
.scl90-top3-content { flex: 1; }
.scl90-top3-head { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; margin-bottom: 12rpx; }
.scl90-top3-name { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); }
.scl90-top3-desc { font-size: 28rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.5; }
.scl90-top3-detail { font-size: 28rpx; color: var(--xia-deep); line-height: 1.5; margin-top: 8rpx; }

.scl90-factor-detail {
  padding: 32rpx 0;
  border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
}
.scl90-factor-detail:last-child { border-bottom: none; }
.scl90-factor-detail-row { display: flex; justify-content: space-between; align-items: flex-start; gap: 24rpx; }
.scl90-factor-detail-head { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; }
.scl90-factor-detail-score { font-size: 36rpx; font-weight: 700; }
.scl90-factor-detail-text { font-size: 26rpx; color: var(--xia-deep); line-height: 1.5; margin-top: 12rpx; }
.scl90-factor-detail-bar {
  height: 12rpx; background: linear-gradient(90deg, rgba(var(--xia-deep-rgb), 0.12) 0%, rgba(var(--xia-deep-rgb), 0.2) 100%); border-radius: 999rpx;
  overflow: hidden; margin-top: 16rpx;
}
.scl90-factor-bar-fill { height: 100%; border-radius: 999rpx; }
.scl90-bar-normal { background: linear-gradient(90deg, var(--xia-sky) 0%, var(--xia-aqua) 100%); }
.scl90-bar-mild { background: linear-gradient(90deg, var(--xia-aqua) 0%, var(--xia-teal) 100%); }
.scl90-bar-moderate { background: linear-gradient(90deg, var(--xia-teal) 0%, var(--xia-deep) 100%); }
.scl90-bar-severe { background: linear-gradient(90deg, var(--xia-deep) 0%, rgba(var(--xia-deep-rgb), 0.9) 100%); }

.scl90-suggest-row { display: flex; align-items: center; flex-wrap: wrap; gap: 24rpx; margin-top: 32rpx; }
.scl90-suggest-label { font-size: 28rpx; font-weight: 500; color: var(--xia-deep); }
.scl90-suggest-badges { display: flex; flex-wrap: wrap; gap: 16rpx; }
.scl90-sugg-badge {
  border-radius: 999rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.15);
  padding: 16rpx 32rpx; font-size: 24rpx; font-weight: 600;
  background: rgba(var(--xia-deep-rgb), 0.04); color: rgba(var(--xia-deep-rgb), 0.5);
}
.scl90-sugg-badge.active {
  background: rgba(var(--xia-deep-rgb), 0.08); color: var(--xia-deep); border-color: rgba(var(--xia-deep-rgb), 0.25);
}
.scl90-sugg-badge:nth-child(2).active { background: rgba(var(--xia-aqua-rgb), 0.12); border-color: rgba(var(--xia-aqua-rgb), 0.5); }
.scl90-sugg-badge:nth-child(3).active { background: rgba(var(--xia-teal-rgb), 0.12); border-color: rgba(var(--xia-teal-rgb), 0.5); }
.scl90-sugg-badge:nth-child(4).active { background: rgba(var(--xia-deep-rgb), 0.12); border-color: rgba(var(--xia-deep-rgb), 0.4); }

.scl90-suggest-list { margin-top: 48rpx; }
.scl90-sugg-item { display: flex; gap: 24rpx; margin-bottom: 32rpx; }
.scl90-sugg-num {
  width: 40rpx; height: 40rpx; flex-shrink: 0;
  display: flex; align-items: center; justify-content: center;
  background: rgba(var(--xia-deep-rgb), 0.08); border-radius: 8rpx;
  font-size: 20rpx; font-weight: 700; color: var(--xia-deep);
}
.scl90-sugg-title { font-weight: 600; color: var(--xia-deep); }

.scl90-footer {
  border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: rgba(var(--xia-deep-rgb), 0.03);
  padding: 48rpx 32rpx;
}
.scl90-footer-title { font-size: 24rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.7); }
.scl90-footer-text { font-size: 24rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 16rpx; }
.scl90-footer-meta { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.65); margin-top: 40rpx; padding-top: 32rpx; border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); }
.scl90-footer-links { display: flex; flex-wrap: wrap; gap: 32rpx; margin-top: 24rpx; }
.scl90-footer-link { font-size: 26rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== RPI 完整报告 ========== */
.rpi-report-wrap { padding: 24rpx; }
.rpi-report-paper {
  border-radius: 24rpx; overflow: hidden;
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08);
}
.rpi-header { background: var(--xia-deep); padding: 48rpx 32rpx; }
.rpi-header-sub { font-size: 20rpx; font-weight: 500; letter-spacing: 0.1em; color: rgba(255,255,255,0.7); }
.rpi-header-title { font-size: 40rpx; font-weight: 700; color: #fff; margin-top: 16rpx; }
.rpi-header-desc { font-size: 26rpx; color: rgba(255,255,255,0.8); margin-top: 8rpx; }

.rpi-meta { background: rgba(var(--xia-deep-rgb), 0.04); padding: 40rpx 32rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.rpi-meta-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 32rpx; }
.rpi-meta-label { font-size: 20rpx; font-weight: 600; color: var(--xia-deep); text-transform: uppercase; }
.rpi-meta-val { font-size: 26rpx; color: var(--xia-deep); margin-top: 8rpx; }

.rpi-body { padding: 48rpx 32rpx; }
.rpi-card { border-radius: 24rpx; padding: 40rpx; margin-bottom: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); }
.rpi-core-card { background: linear-gradient(135deg, rgba(var(--xia-sky-rgb), 0.08) 0%, #fff 50%, rgba(var(--xia-cream-rgb), 0.1) 100%); }
.rpi-core-inner { display: flex; flex-direction: row; align-items: center; justify-content: space-between; gap: 32rpx; flex-wrap: wrap; }
.rpi-core-left { flex: 1; min-width: 0; }
.rpi-core-icon-wrap { width: 128rpx; height: 128rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; border-radius: 32rpx; background: rgba(var(--xia-deep-rgb), 0.06); box-shadow: 0 12rpx 24rpx rgba(var(--xia-teal-rgb), 0.15); }
.rpi-core-icon { width: 80rpx; height: 80rpx; }
.rpi-core-sub { font-size: 22rpx; font-weight: 600; letter-spacing: 0.1em; color: rgba(var(--xia-deep-rgb), 0.7); }
.rpi-core-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); margin-top: 12rpx; }
.rpi-core-desc { font-size: 28rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 24rpx; }
.rpi-core-tags { display: flex; flex-wrap: wrap; gap: 16rpx; margin-top: 24rpx; }
.rpi-tag { padding: 12rpx 24rpx; border-radius: 999rpx; font-size: 24rpx; font-weight: 600; background: rgba(var(--xia-deep-rgb), 0.06); border: 2rpx solid rgba(var(--xia-deep-rgb), 0.15); color: var(--xia-deep); }
.rpi-tag-2 { background: rgba(255,255,255,0.6); }

.rpi-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 48rpx 0; }
.rpi-section { margin-bottom: 48rpx; }
.rpi-section-title { display: flex; align-items: center; gap: 12rpx; font-size: 32rpx; font-weight: 700; color: var(--xia-deep); }
.rpi-section-num { width: 48rpx; height: 48rpx; display: flex; align-items: center; justify-content: center; background: var(--xia-deep); color: #fff; font-size: 24rpx; border-radius: 50%; }
.rpi-section-desc { font-size: 28rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 24rpx; }

.rpi-chart-wrap { margin-top: 32rpx; }
.rpi-incomplete-tip { font-size: 28rpx; color: var(--xia-deep); padding: 24rpx; border-radius: 16rpx; background: rgba(var(--xia-sky-rgb), 0.1); margin-top: 24rpx; }
.rpi-total-cards { display: flex; flex-wrap: wrap; gap: 24rpx; margin-top: 24rpx; }
.rpi-total-card { padding: 32rpx; border-radius: 24rpx; border: 2rpx solid; flex: 1; min-width: 200rpx; }
.rpi-level-low { background: rgba(var(--xia-mint-rgb), 0.15); border-color: var(--xia-teal); }
.rpi-level-mod { background: rgba(var(--xia-sky-rgb), 0.15); border-color: var(--xia-sky); }
.rpi-level-high { background: rgba(var(--xia-aqua-rgb), 0.15); border-color: var(--xia-aqua); }
.rpi-level-very { background: rgba(var(--xia-deep-rgb), 0.08); border-color: var(--xia-deep); }
.rpi-total-label { font-size: 22rpx; font-weight: 600; color: var(--xia-deep); }
.rpi-total-val { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); margin-top: 8rpx; }

.rpi-top3-grid { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 24rpx; margin-top: 24rpx; }
.rpi-top3-cell { padding: 24rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.rpi-top3-lbl { font-size: 22rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.6); }
.rpi-top3-name { font-size: 28rpx; font-weight: 700; color: var(--xia-deep); margin-top: 8rpx; }
.rpi-top3-detail { font-size: 24rpx; color: var(--xia-deep); margin-top: 8rpx; }

.rpi-view-switcher { display: flex; gap: 16rpx; margin-top: 24rpx; flex-wrap: wrap; }
.rpi-view-btn { padding: 16rpx 32rpx; border-radius: 999rpx; font-size: 26rpx; font-weight: 500; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.15); background: rgba(var(--xia-deep-rgb), 0.04); color: var(--xia-deep); }
.rpi-view-active { background: var(--xia-deep); color: #fff; border-color: var(--xia-deep); }

.rpi-perspective-cards { display: flex; flex-direction: column; gap: 32rpx; margin-top: 24rpx; }
.rpi-perspective-block { border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: #fff; padding: 32rpx; }
.rpi-perspective-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24rpx; padding-bottom: 24rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.rpi-perspective-title { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); }
.rpi-perspective-badge { padding: 12rpx 24rpx; border-radius: 12rpx; font-size: 26rpx; font-weight: 700; background: rgba(var(--xia-deep-rgb), 0.06); color: var(--xia-deep); }

.rpi-dim-row { display: flex; flex-direction: column; gap: 12rpx; padding: 24rpx 0; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.rpi-dim-row:last-child { border-bottom: none; }
.rpi-dim-hint { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.5; }
.rpi-dim-left { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; }
.rpi-dim-name { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.rpi-dim-badge { padding: 8rpx 16rpx; border-radius: 999rpx; font-size: 22rpx; font-weight: 600; border: 2rpx solid; }
.rpi-badge-low { background: rgba(var(--xia-mint-rgb), 0.2); border-color: var(--xia-teal); color: var(--xia-deep); }
.rpi-badge-moderate { background: rgba(var(--xia-sky-rgb), 0.2); border-color: var(--xia-sky); color: var(--xia-deep); }
.rpi-badge-high { background: rgba(var(--xia-aqua-rgb), 0.2); border-color: var(--xia-aqua); color: var(--xia-deep); }
.rpi-badge-veryHigh { background: rgba(var(--xia-deep-rgb), 0.1); border-color: var(--xia-deep); color: var(--xia-deep); }
.rpi-dim-right { display: flex; align-items: center; gap: 24rpx; }
.rpi-dim-mean { font-size: 26rpx; color: var(--xia-deep); }
.rpi-dim-sum { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); min-width: 48rpx; text-align: right; }
.rpi-dim-bar-wrap { width: 96rpx; height: 16rpx; border-radius: 999rpx; background: rgba(var(--xia-deep-rgb), 0.1); overflow: hidden; }
.rpi-dim-bar { height: 100%; }
.rpi-dim-fill { height: 100%; border-radius: 999rpx; }
.rpi-fill-low { background: var(--xia-teal); }
.rpi-fill-moderate { background: var(--xia-sky); }
.rpi-fill-high { background: var(--xia-aqua); }
.rpi-fill-veryHigh { background: var(--xia-deep); }

.rpi-compare-table { border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); overflow: hidden; margin-top: 24rpx; }
.rpi-table-row { display: grid; grid-template-columns: 1fr 80rpx 80rpx 80rpx; gap: 16rpx; padding: 24rpx 32rpx; align-items: center; font-size: 26rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.rpi-table-row:last-child { border-bottom: none; }
.rpi-table-head { background: rgba(var(--xia-deep-rgb), 0.06); font-weight: 600; color: var(--xia-deep); }
.rpi-advice-list { margin-top: 24rpx; }
.rpi-advice-item { font-size: 28rpx; color: var(--xia-deep); line-height: 1.7; margin-bottom: 16rpx; }
.rpi-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-deep-rgb), 0.03); padding: 48rpx 32rpx; }
.rpi-footer-title { font-size: 24rpx; font-weight: 600; color: var(--xia-deep); }
.rpi-footer-text { font-size: 24rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 16rpx; }
.rpi-footer-meta { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.65); margin-top: 32rpx; padding-top: 24rpx; border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); }
.rpi-footer-links { display: flex; gap: 32rpx; margin-top: 24rpx; }
.rpi-footer-link { font-size: 26rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== SRI 性压抑指数 ========== */
.sri-incomplete-wrap { padding: 80rpx 48rpx; text-align: center; }
.sri-incomplete-msg { font-size: 32rpx; color: var(--xia-deep); margin-bottom: 32rpx; }
.sri-incomplete-link { font-size: 28rpx; color: var(--xia-teal); text-decoration: underline; }

.sri-report-wrap { padding: 24rpx; }
.sri-report-paper {
  border-radius: 24rpx; overflow: hidden;
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08);
}
.sri-header { background: var(--xia-deep); padding: 48rpx 32rpx; }
.sri-header-sub { font-size: 20rpx; font-weight: 500; letter-spacing: 0.1em; color: rgba(255,255,255,0.7); }
.sri-header-title { font-size: 40rpx; font-weight: 700; color: #fff; margin-top: 16rpx; }
.sri-header-desc { font-size: 26rpx; color: rgba(255,255,255,0.8); margin-top: 8rpx; }

.sri-meta { background: rgba(var(--xia-deep-rgb), 0.04); padding: 40rpx 32rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.sri-meta-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 32rpx; }
.sri-meta-label { font-size: 20rpx; font-weight: 600; color: var(--xia-deep); }
.sri-meta-val { font-size: 26rpx; color: var(--xia-deep); margin-top: 8rpx; }

.sri-body { padding: 48rpx 32rpx; }
.sri-card { border-radius: 24rpx; padding: 40rpx; margin-bottom: 24rpx; }
.sri-core-card { background: linear-gradient(135deg, rgba(var(--xia-sky-rgb), 0.08) 0%, #fff 50%, rgba(var(--xia-cream-rgb), 0.1) 100%); border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); }
.sri-core-inner { display: flex; flex-direction: row; align-items: center; justify-content: space-between; gap: 32rpx; flex-wrap: wrap; }
.sri-core-left { flex: 1; min-width: 0; }
.sri-core-icon-wrap { width: 128rpx; height: 128rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; border-radius: 32rpx; background: rgba(var(--xia-deep-rgb), 0.06); }
.sri-core-icon { width: 80rpx; height: 80rpx; }
.sri-core-sub { font-size: 22rpx; font-weight: 600; letter-spacing: 0.1em; color: rgba(var(--xia-deep-rgb), 0.7); }
.sri-core-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); margin-top: 12rpx; }
.sri-core-desc { font-size: 28rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 24rpx; }
.sri-core-tags { display: flex; flex-wrap: wrap; gap: 16rpx; margin-top: 24rpx; }
.sri-tag { padding: 12rpx 24rpx; border-radius: 999rpx; font-size: 24rpx; font-weight: 600; background: rgba(var(--xia-deep-rgb), 0.06); border: 2rpx solid rgba(var(--xia-deep-rgb), 0.15); color: var(--xia-deep); }
.sri-tag-2 { background: rgba(255,255,255,0.6); }

.sri-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 48rpx 0; }
.sri-section { margin-bottom: 48rpx; }
.sri-section-title { display: flex; align-items: center; gap: 12rpx; font-size: 32rpx; font-weight: 700; color: var(--xia-deep); }
.sri-section-num { width: 48rpx; height: 48rpx; display: flex; align-items: center; justify-content: center; background: var(--xia-deep); color: #fff; font-size: 24rpx; border-radius: 50%; }
.sri-section-desc { font-size: 28rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 24rpx; }
.sri-radar-wrap { width: 100%; min-height: 320rpx; margin-bottom: 32rpx; padding: 24rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.sri-chart-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 16rpx; }
.sri-chart-wrap { margin-top: 32rpx; }

.sri-total-card { border-radius: 24rpx; padding: 40rpx; margin-top: 24rpx; border: 2rpx solid; }
.sri-total-veryLow { background: rgba(var(--xia-mint-rgb), 0.15); border-color: #2C6F7A; }
.sri-total-low { background: rgba(var(--xia-sky-rgb), 0.15); border-color: #68D4DB; }
.sri-total-mid { background: rgba(var(--xia-cream-rgb), 0.2); border-color: #9EABB8; }
.sri-total-high { background: rgba(var(--xia-aqua-rgb), 0.15); border-color: #7FBFE1; }
.sri-total-veryHigh { background: rgba(var(--xia-deep-rgb), 0.08); border-color: var(--xia-deep); }
.sri-total-row { display: flex; align-items: center; justify-content: space-between; gap: 24rpx; }
.sri-total-label { font-size: 22rpx; font-weight: 600; color: var(--xia-deep); }
.sri-total-val { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); margin-top: 8rpx; }
.sri-total-bar-wrap { width: 128rpx; height: 24rpx; border-radius: 999rpx; background: rgba(255,255,255,0.7); overflow: hidden; }
.sri-total-bar, .sri-total-fill { height: 100%; border-radius: 999rpx; }
.sri-total-fill { background: var(--xia-teal); transition: width 0.3s; }

.sri-stats-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24rpx; margin-top: 24rpx; }
.sri-stat-cell { padding: 24rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.sri-stat-lbl { font-size: 22rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.7); }
.sri-stat-val { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); margin-top: 8rpx; }
.sri-stat-hint { font-size: 24rpx; color: var(--xia-deep); margin-top: 8rpx; }

.sri-dim-table { border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); overflow: hidden; margin-top: 24rpx; }
.sri-dim-row { display: flex; flex-direction: column; gap: 12rpx; padding: 24rpx 32rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.sri-dim-row:last-child { border-bottom: none; }
.sri-dim-left { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; }
.sri-dim-name { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.sri-dim-badge { padding: 8rpx 16rpx; border-radius: 999rpx; font-size: 22rpx; font-weight: 600; border: 2rpx solid; }
.sri-badge-veryLow { background: rgba(44,111,122,0.15); border-color: #2C6F7A; color: var(--xia-deep); }
.sri-badge-low { background: rgba(104,212,219,0.15); border-color: #68D4DB; color: var(--xia-deep); }
.sri-badge-mid { background: rgba(158,171,184,0.15); border-color: #9EABB8; color: var(--xia-deep); }
.sri-badge-high { background: rgba(127,191,225,0.15); border-color: #7FBFE1; color: var(--xia-deep); }
.sri-badge-veryHigh { background: rgba(var(--xia-deep-rgb), 0.1); border-color: var(--xia-deep); color: var(--xia-deep); }
.sri-dim-hint { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.5; }
.sri-dim-right { display: flex; align-items: center; gap: 24rpx; }
.sri-dim-mean { font-size: 26rpx; color: var(--xia-deep); }
.sri-dim-index { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); min-width: 48rpx; text-align: right; }
.sri-dim-bar-wrap { width: 96rpx; height: 16rpx; border-radius: 999rpx; background: rgba(var(--xia-deep-rgb), 0.1); overflow: hidden; }
.sri-dim-bar { height: 100%; }
.sri-dim-fill { height: 100%; border-radius: 999rpx; }
.sri-fill-veryLow { background: #2C6F7A; }
.sri-fill-low { background: #68D4DB; }
.sri-fill-mid { background: #9EABB8; }
.sri-fill-high { background: #7FBFE1; }
.sri-fill-veryHigh { background: var(--xia-deep); }

.sri-interp-card { border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); padding: 32rpx; margin-top: 24rpx; background: #fff; }
.sri-interp-title { font-size: 28rpx; font-weight: 700; color: var(--xia-deep); }
.sri-interp-item { font-size: 28rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 16rpx; }
.sri-interp-text { font-size: 28rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 16rpx; }
.sri-advice-list { margin-top: 24rpx; }
.sri-advice-item { font-size: 28rpx; color: var(--xia-deep); line-height: 1.7; margin-bottom: 16rpx; }
.sri-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-deep-rgb), 0.03); padding: 48rpx 32rpx; }
.sri-footer-title { font-size: 24rpx; font-weight: 600; color: var(--xia-deep); }
.sri-footer-text { font-size: 24rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 16rpx; }
.sri-footer-meta { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.65); margin-top: 32rpx; padding-top: 24rpx; border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); }
.sri-footer-links { display: flex; gap: 32rpx; margin-top: 24rpx; }
.sri-footer-link { font-size: 26rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== 人格动物塑 ========== */
.animal-incomplete-wrap { padding: 80rpx 48rpx; text-align: center; }
.animal-incomplete-msg { font-size: 32rpx; color: var(--xia-deep); margin-bottom: 32rpx; }
.animal-incomplete-link { font-size: 28rpx; color: var(--xia-teal); text-decoration: underline; }

.animal-report-wrap { padding: 24rpx; }
.animal-report-paper {
  border-radius: 24rpx; overflow: hidden;
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08);
}
.animal-header {
  background: linear-gradient(135deg, rgba(var(--xia-sky-rgb), 0.15) 0%, rgba(var(--xia-cream-rgb), 0.2) 50%, #fff 100%);
  padding: 64rpx 32rpx;
  text-align: center;
}
.animal-icon-wrap { font-size: 128rpx; margin-bottom: 32rpx; }
.animal-header-sub { font-size: 26rpx; font-weight: 600; letter-spacing: 0.15em; color: rgba(var(--xia-deep-rgb), 0.6); }
.animal-header-title { font-size: 56rpx; font-weight: 700; color: var(--xia-deep); margin-top: 16rpx; }
.animal-header-desc { font-size: 30rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 32rpx; opacity: 0.9; }
.animal-traits { display: flex; flex-wrap: wrap; justify-content: center; gap: 24rpx; margin-top: 40rpx; }
.animal-trait-tag { padding: 16rpx 32rpx; border-radius: 999rpx; font-size: 28rpx; font-weight: 500; background: rgba(var(--xia-deep-rgb), 0.06); color: var(--xia-deep); }
.animal-weaknesses { margin-top: 40rpx; }
.animal-weak-label { font-size: 24rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.5); margin-bottom: 16rpx; }
.animal-weak-tags { display: flex; flex-wrap: wrap; justify-content: center; gap: 16rpx; }
.animal-weak-tag { padding: 12rpx 24rpx; border-radius: 999rpx; font-size: 26rpx; background: rgba(239,68,68,0.1); color: #dc2626; border: 2rpx solid rgba(239,68,68,0.2); }

.animal-body { padding: 48rpx 32rpx; }
.animal-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 48rpx 0; }
.animal-section { margin-bottom: 48rpx; }
.animal-section-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 32rpx; }
.animal-charts-row { display: flex; flex-direction: column; gap: 56rpx; }
.animal-radar-wrap { width: 100%; min-height: 380rpx; margin-bottom: 16rpx; }
.animal-dim-list { display: flex; flex-direction: column; gap: 24rpx; padding-top: 40rpx; margin-top: 8rpx; border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); }
.animal-dim-item {}
.animal-dim-head { display: flex; justify-content: space-between; font-size: 28rpx; margin-bottom: 8rpx; }
.animal-dim-score { font-weight: 700; color: var(--xia-deep); }
.animal-dim-bar { height: 16rpx; border-radius: 999rpx; background: rgba(var(--xia-deep-rgb), 0.08); overflow: hidden; }
.animal-dim-fill { height: 100%; border-radius: 999rpx; background: var(--xia-teal); }

.animal-compound-grid { display: flex; flex-direction: column; gap: 32rpx; }
.animal-compound-item { padding: 24rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: #fff; }
.animal-compound-head { display: flex; justify-content: space-between; align-items: center; }
.animal-compound-label { font-size: 28rpx; font-weight: 500; color: var(--xia-deep); }
.animal-compound-right { display: flex; align-items: center; gap: 16rpx; }
.animal-compound-status { padding: 8rpx 20rpx; border-radius: 999rpx; font-size: 24rpx; background: rgba(var(--xia-deep-rgb), 0.06); color: var(--xia-deep); }
.animal-compound-val { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); }
.animal-compound-bar { height: 16rpx; border-radius: 999rpx; background: rgba(var(--xia-deep-rgb), 0.08); overflow: hidden; margin-top: 16rpx; }
.animal-compound-fill { height: 100%; border-radius: 999rpx; background: var(--xia-teal); }

.animal-secondary-card { display: flex; gap: 32rpx; padding: 40rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-cream-rgb), 0.15); }
.animal-secondary-icon { font-size: 80rpx; flex-shrink: 0; }
.animal-secondary-content { flex: 1; }
.animal-secondary-name { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); }
.animal-secondary-traits { display: flex; flex-wrap: wrap; gap: 16rpx; margin-top: 16rpx; }
.animal-sec-tag { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.6); }
.animal-secondary-desc { font-size: 28rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 24rpx; }

.animal-analysis-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24rpx; }
.animal-analysis-card { padding: 32rpx; border-radius: 20rpx; background: rgba(var(--xia-sky-rgb), 0.08); border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.animal-analysis-title { font-size: 28rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 16rpx; }
.animal-analysis-text { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; }

.animal-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.animal-footer-meta { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.6); }
.animal-footer-text { font-size: 24rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 16rpx; }
.animal-footer-links { display: flex; justify-content: center; gap: 32rpx; margin-top: 32rpx; }
.animal-footer-link { font-size: 28rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== MBTI 完整报告 ========== - 移植自 MBTIResult.tsx */
.mbti-report-wrap { padding: 24rpx; }
.mbti-report-paper {
  border-radius: 24rpx; overflow: hidden;
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08);
}
.mbti-header {
  background: linear-gradient(135deg, rgba(var(--xia-sky-rgb), 0.2) 0%, rgba(var(--xia-cream-rgb), 0.3) 50%, #fff 100%);
  padding: 64rpx 32rpx;
  text-align: center;
}
.mbti-avatar-wrap {
  width: 160rpx; height: 160rpx; margin: 0 auto 32rpx;
  border-radius: 50%; overflow: hidden; background: #fff;
  box-shadow: 0 8rpx 24rpx rgba(var(--xia-deep-rgb), 0.12);
  display: flex; align-items: center; justify-content: center; padding: 16rpx;
}
.mbti-avatar-img { width: 100%; height: 100%; }
.mbti-header-sub { font-size: 26rpx; font-weight: 600; letter-spacing: 0.2em; color: rgba(var(--xia-deep-rgb), 0.5); }
.mbti-header-title { font-size: 48rpx; font-weight: 700; color: var(--xia-deep); margin-top: 16rpx; }
.mbti-header-en { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 8rpx; }
.mbti-slogan { font-size: 28rpx; font-weight: 500; font-style: italic; color: var(--xia-teal); margin-top: 24rpx; }
.mbti-desc { font-size: 28rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 24rpx; opacity: 0.85; }
.mbti-strengths { display: flex; flex-wrap: wrap; justify-content: center; gap: 16rpx; margin-top: 32rpx; }
.mbti-strength-tag { padding: 12rpx 24rpx; border-radius: 999rpx; font-size: 24rpx; font-weight: 500; background: rgba(var(--xia-teal-rgb), 0.1); color: var(--xia-teal); }

.mbti-body { padding: 48rpx 32rpx; }
.mbti-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 48rpx 0; }
.mbti-section { margin-bottom: 48rpx; }
.mbti-section-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }

.mbti-dim-bars { display: flex; flex-direction: column; gap: 32rpx; }
.mbti-dim-bar { padding: 24rpx; border-radius: 16rpx; background: rgba(var(--xia-deep-rgb), 0.02); }
.mbti-dim-bar-head { display: flex; justify-content: space-between; margin-bottom: 12rpx; font-size: 26rpx; }
.mbti-dim-label { font-weight: 600; color: var(--xia-deep); }
.mbti-dim-scores { color: rgba(var(--xia-deep-rgb), 0.7); }
.mbti-dim-bar-track { display: flex; height: 24rpx; border-radius: 999rpx; overflow: hidden; background: rgba(var(--xia-deep-rgb), 0.08); }
.mbti-dim-bar-a { background: var(--xia-teal); border-radius: 999rpx 0 0 999rpx; }
.mbti-dim-bar-b { flex: 1; background: rgba(var(--xia-sky-rgb), 0.6); border-radius: 0 999rpx 999rpx 0; }
.mbti-dim-bar-hint { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 12rpx; }
.mbti-bold { font-weight: 700; color: var(--xia-deep); }

.mbti-chart-row { display: flex; flex-direction: column; gap: 40rpx; }
.mbti-radar-wrap { width: 100%; min-height: 380rpx; }
.mbti-radar-legend { padding: 24rpx; border-radius: 16rpx; background: rgba(var(--xia-cream-rgb), 0.3); }
.mbti-legend-title { font-size: 30rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }
.mbti-legend-item { margin-bottom: 20rpx; }
.mbti-legend-item:last-child { margin-bottom: 0; }
.mbti-legend-row { display: flex; justify-content: space-between; font-size: 26rpx; margin-bottom: 8rpx; }
.mbti-legend-dim { color: rgba(var(--xia-deep-rgb), 0.8); }
.mbti-legend-val { font-weight: 700; color: var(--xia-deep); }
.mbti-legend-bar { height: 16rpx; border-radius: 999rpx; background: rgba(var(--xia-deep-rgb), 0.08); overflow: hidden; }
.mbti-legend-fill { height: 100%; border-radius: 999rpx; background: var(--xia-teal); }

.mbti-depth-block { padding: 32rpx; border-radius: 16rpx; background: linear-gradient(135deg, rgba(var(--xia-sky-rgb), 0.05) 0%, rgba(var(--xia-cream-rgb), 0.15) 100%); margin-bottom: 32rpx; }
.mbti-depth-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 16rpx; }
.mbti-depth-text { font-size: 26rpx; color: var(--xia-deep); line-height: 1.7; opacity: 0.85; white-space: pre-line; }

.mbti-analysis-grid { display: flex; flex-direction: column; gap: 24rpx; }
.mbti-analysis-card { padding: 32rpx; border-radius: 16rpx; background: rgba(var(--xia-sky-rgb), 0.08); border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.mbti-analysis-card.mbti-card-weak { background: rgba(var(--xia-cream-rgb), 0.3); }
.mbti-analysis-card.mbti-card-full { background: rgba(var(--xia-aqua-rgb), 0.08); }
.mbti-card-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 20rpx; }
.mbti-detail-list { display: flex; flex-direction: column; gap: 20rpx; }
.mbti-detail-item {}
.mbti-detail-head { display: flex; align-items: center; gap: 12rpx; font-size: 26rpx; font-weight: 500; color: var(--xia-deep); }
.mbti-dot { width: 12rpx; height: 12rpx; border-radius: 50%; background: var(--xia-teal); flex-shrink: 0; }
.mbti-dot-weak { background: rgba(var(--xia-deep-rgb), 0.4); }
.mbti-dot-sm { width: 8rpx; height: 8rpx; border-radius: 50%; background: var(--xia-teal); flex-shrink: 0; margin-right: 8rpx; }
.mbti-detail-body { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; padding-left: 24rpx; opacity: 0.85; }
.mbti-simple-list { display: flex; flex-direction: column; gap: 12rpx; font-size: 26rpx; color: var(--xia-deep); }
.mbti-simple-item { display: flex; align-items: center; gap: 12rpx; }
.mbti-career-tags { display: flex; flex-wrap: wrap; gap: 16rpx; margin-bottom: 24rpx; }
.mbti-career-tag { padding: 12rpx 24rpx; border-radius: 999rpx; font-size: 24rpx; background: rgba(var(--xia-deep-rgb), 0.05); color: var(--xia-deep); }
.mbti-growth-tips {}
.mbti-growth-title { font-size: 26rpx; font-weight: 500; color: var(--xia-deep); margin-bottom: 16rpx; }
.mbti-growth-item { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; padding: 16rpx 20rpx; border-radius: 12rpx; background: rgba(255,255,255,0.6); margin-bottom: 12rpx; opacity: 0.9; }
.mbti-growth-item:last-child { margin-bottom: 0; }

.mbti-insight-grid { display: flex; flex-direction: column; gap: 24rpx; }
.mbti-insight-card { padding: 32rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: #fff; }
.mbti-insight-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 16rpx; }
.mbti-insight-text { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; margin-bottom: 20rpx; opacity: 0.85; }
.mbti-insight-deep { padding: 20rpx; border-radius: 12rpx; background: rgba(var(--xia-cream-rgb), 0.3); }
.mbti-insight-deep-title { font-size: 22rpx; font-weight: 600; letter-spacing: 0.1em; color: rgba(var(--xia-deep-rgb), 0.6); margin-bottom: 12rpx; }
.mbti-insight-deep-text { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; opacity: 0.9; }

.mbti-dim-cards { display: flex; flex-direction: column; gap: 24rpx; }
.mbti-dim-card { padding: 32rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: #fff; }
.mbti-dim-card-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 12rpx; }
.mbti-dim-card-content { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; margin-bottom: 20rpx; opacity: 0.85; }
.mbti-dim-card-tend { padding: 16rpx 24rpx; border-radius: 12rpx; font-size: 26rpx; background: rgba(var(--xia-cream-rgb), 0.3); }
.mbti-dim-card-tend .mbti-bold { color: var(--xia-teal); }

.mbti-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.mbti-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.mbti-footer-note { font-size: 24rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 16rpx; opacity: 0.8; }
.mbti-footer-links { display: flex; justify-content: center; gap: 32rpx; margin-top: 24rpx; }
.mbti-footer-link { font-size: 28rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== AAT 学习适应性测验报告 - 移植自 AATResult.tsx ========== */
.aat-report-wrap { padding: 24rpx; }
.aat-report-paper {
  border-radius: 24rpx; overflow: hidden;
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08);
}
.aat-header {
  background: linear-gradient(135deg, rgba(var(--xia-sky-rgb), 0.15) 0%, rgba(var(--xia-cream-rgb), 0.4) 50%, #fff 100%);
  padding: 48rpx 32rpx;
  border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08);
}
.aat-header-inner { display: flex; align-items: center; gap: 40rpx; flex-wrap: wrap; }
.aat-header-circle-wrap { position: relative; flex-shrink: 0; }
.aat-circle-outer {
  width: 180rpx; height: 180rpx;
  border-radius: 50%;
  border: 12rpx solid rgba(var(--xia-deep-rgb), 0.15);
  display: flex; align-items: center; justify-content: center;
  background: rgba(255,255,255,0.6);
}
.aat-circle-inner { font-size: 48rpx; font-weight: 700; color: var(--xia-deep); }
.aat-circle-unit { font-size: 24rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.6); margin-left: 2rpx; }
.aat-header-text { flex: 1; min-width: 0; }
.aat-header-sub { font-size: 22rpx; font-weight: 600; letter-spacing: 0.15em; color: rgba(var(--xia-deep-rgb), 0.5); }
.aat-header-title { font-size: 40rpx; font-weight: 700; color: var(--xia-deep); margin-top: 16rpx; }
.aat-level-badge {
  display: inline-block; margin-top: 16rpx; padding: 12rpx 28rpx;
  border-radius: 999rpx; font-size: 26rpx; font-weight: 600; border: 2rpx solid;
}
.aat-level-excellent { background: rgba(var(--xia-teal-rgb), 0.2); border-color: var(--xia-teal); color: var(--xia-deep); }
.aat-level-good { background: rgba(var(--xia-sky-rgb), 0.2); border-color: var(--xia-sky); color: var(--xia-deep); }
.aat-level-fair { background: rgba(var(--xia-aqua-rgb), 0.2); border-color: var(--xia-aqua); color: var(--xia-deep); }
.aat-level-poor { background: rgba(var(--xia-deep-rgb), 0.1); border-color: var(--xia-deep); color: var(--xia-deep); }
.aat-level-veryPoor { background: rgba(var(--xia-deep-rgb), 0.08); border-color: var(--xia-deep); color: var(--xia-deep); }
.aat-header-desc { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 20rpx; opacity: 0.85; }
.aat-meta { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.5); margin-top: 12rpx; }

.aat-stats-row { display: flex; flex-wrap: wrap; gap: 20rpx; margin-top: 40rpx; }
.aat-stat-card {
  flex: 1; min-width: 140rpx; padding: 24rpx;
  border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08);
  background: rgba(255,255,255,0.8);
}
.aat-stat-lbl { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.6); }
.aat-stat-val { font-size: 36rpx; font-weight: 700; color: var(--xia-teal); margin-top: 8rpx; }
.aat-stat-card:last-child .aat-stat-val { color: var(--xia-deep); }
.aat-stat-hint { font-size: 20rpx; color: rgba(var(--xia-deep-rgb), 0.5); margin-top: 4rpx; }
.aat-stat-wide { flex: 2; min-width: 200rpx; }

.aat-groups-row { display: flex; flex-wrap: wrap; gap: 24rpx; margin-top: 32rpx; padding: 24rpx; border-radius: 16rpx; background: rgba(255,255,255,0.7); }
.aat-group-cell { flex: 1; min-width: 120rpx; text-align: center; }
.aat-group-name { font-size: 20rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.aat-group-avg { font-size: 32rpx; font-weight: 700; color: var(--xia-teal); margin-top: 8rpx; }
.aat-group-bar { height: 8rpx; background: rgba(var(--xia-deep-rgb), 0.1); border-radius: 999rpx; overflow: hidden; margin-top: 12rpx; }
.aat-group-fill { height: 100%; background: var(--xia-teal); border-radius: 999rpx; }

.aat-body { padding: 48rpx 32rpx; }
.aat-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 48rpx 0; }
.aat-section { margin-bottom: 48rpx; }
.aat-section-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }
.aat-chart-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 16rpx; }

.aat-charts-row { display: flex; flex-direction: column; gap: 40rpx; overflow: hidden; max-width: 100%; }
.aat-radar-wrap { width: 100%; min-height: 380rpx; padding: 24rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.aat-bar-wrap { width: 100%; min-height: 420rpx; padding: 24rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }

.aat-group-cards { display: flex; flex-direction: column; gap: 24rpx; }
.aat-group-card {
  padding: 32rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: #fff;
}
.aat-group-card-head { display: flex; justify-content: space-between; align-items: flex-start; }
.aat-group-card-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.aat-group-card-hint { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.5); margin-top: 4rpx; }
.aat-group-card-avg { font-size: 36rpx; font-weight: 700; color: var(--xia-teal); }
.aat-group-card-bar { height: 16rpx; background: rgba(var(--xia-deep-rgb), 0.08); border-radius: 999rpx; overflow: hidden; margin-top: 20rpx; }
.aat-group-card-fill { height: 100%; background: var(--xia-teal); border-radius: 999rpx; }
.aat-group-tags { display: flex; flex-wrap: wrap; gap: 12rpx; margin-top: 16rpx; }
.aat-factor-tag { padding: 8rpx 20rpx; border-radius: 999rpx; font-size: 22rpx; border: 2rpx solid; }
.aat-factor-tag.aat-level-good { background: rgba(var(--xia-teal-rgb), 0.15); border-color: var(--xia-teal); color: var(--xia-deep); }
.aat-factor-tag.aat-level-fair { background: rgba(var(--xia-sky-rgb), 0.2); border-color: var(--xia-sky); color: var(--xia-deep); }
.aat-factor-tag.aat-level-poor { background: rgba(var(--xia-aqua-rgb), 0.2); border-color: var(--xia-aqua); color: var(--xia-deep); }
.aat-factor-tag.aat-level-veryPoor { background: rgba(var(--xia-deep-rgb), 0.1); border-color: var(--xia-deep); color: var(--xia-deep); }

.aat-compare-row { display: flex; flex-direction: column; gap: 40rpx; }
.aat-top3-block { padding: 32rpx; border-radius: 16rpx; background: rgba(var(--xia-mint-rgb), 0.15); }
.aat-block-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 24rpx; }
.aat-top3-cards { display: flex; flex-wrap: wrap; gap: 20rpx; }
.aat-top3-item {
  flex: 1; min-width: 160rpx; padding: 24rpx; border-radius: 16rpx;
  background: #fff; border: 2rpx solid rgba(var(--xia-teal-rgb), 0.2);
  text-align: center;
}
.aat-top3-item.aat-top3-first { border-color: var(--xia-teal); box-shadow: 0 4rpx 16rpx rgba(var(--xia-teal-rgb), 0.15); }
.aat-top3-rank { width: 48rpx; height: 48rpx; line-height: 48rpx; margin: 0 auto 12rpx; border-radius: 50%; background: rgba(var(--xia-teal-rgb), 0.15); font-size: 26rpx; font-weight: 700; color: var(--xia-teal); }
.aat-top3-name { font-size: 26rpx; font-weight: 500; color: var(--xia-deep); }
.aat-top3-pct { font-size: 28rpx; font-weight: 700; color: var(--xia-teal); margin-top: 8rpx; }

.aat-bottom3-block { padding: 32rpx; border-radius: 16rpx; background: rgba(var(--xia-aqua-rgb), 0.08); }
.aat-priority-card {
  padding: 32rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-aqua-rgb), 0.4);
  background: linear-gradient(135deg, rgba(var(--xia-aqua-rgb), 0.05) 0%, #fff 100%);
  margin-bottom: 24rpx;
}
.aat-priority-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; }
.aat-priority-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.aat-priority-pct { font-size: 40rpx; font-weight: 700; color: var(--xia-deep); }
.aat-priority-tip { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; padding: 20rpx; border-radius: 12rpx; background: rgba(var(--xia-cream-rgb), 0.5); }
.aat-bottom3-list { display: flex; flex-direction: column; gap: 16rpx; }
.aat-bottom3-item { display: flex; align-items: center; gap: 20rpx; padding: 24rpx; border-radius: 12rpx; background: rgba(255,255,255,0.8); }
.aat-bottom3-rank { width: 40rpx; height: 40rpx; line-height: 40rpx; text-align: center; border-radius: 50%; background: rgba(var(--xia-deep-rgb), 0.08); font-size: 22rpx; font-weight: 700; color: var(--xia-deep); }
.aat-bottom3-name { flex: 1; font-size: 28rpx; font-weight: 500; color: var(--xia-deep); }
.aat-bottom3-pct { font-size: 28rpx; font-weight: 700; color: var(--xia-deep); }

.aat-factor-detail {
  padding: 32rpx; border-radius: 16rpx; border: 2rpx solid; margin-bottom: 24rpx;
}
.aat-factor-detail:last-child { margin-bottom: 0; }
.aat-factor-detail.aat-level-good { background: rgba(var(--xia-mint-rgb), 0.2); border-color: var(--xia-mint); }
.aat-factor-detail.aat-level-fair { background: rgba(var(--xia-sky-rgb), 0.2); border-color: var(--xia-sky); }
.aat-factor-detail.aat-level-poor { background: rgba(var(--xia-aqua-rgb), 0.2); border-color: var(--xia-aqua); }
.aat-factor-detail.aat-level-veryPoor { background: rgba(var(--xia-deep-rgb), 0.1); border-color: var(--xia-deep); }
.aat-factor-detail-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; }
.aat-factor-detail-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.aat-factor-detail-meta { font-size: 26rpx; font-weight: 600; color: var(--xia-deep); }
.aat-factor-detail-bar { height: 12rpx; background: rgba(255,255,255,0.5); border-radius: 999rpx; overflow: hidden; margin-bottom: 20rpx; }
.aat-factor-bar-fill { height: 100%; border-radius: 999rpx; background: var(--xia-teal); }
.aat-factor-detail-desc { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; margin-bottom: 16rpx; opacity: 0.85; }
.aat-factor-improve { font-size: 24rpx; color: var(--xia-teal); line-height: 1.6; padding: 16rpx; border-radius: 12rpx; background: rgba(var(--xia-teal-rgb), 0.1); }

.aat-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.aat-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.aat-footer-note { font-size: 24rpx; color: var(--xia-deep); line-height: 1.6; margin-top: 16rpx; opacity: 0.8; }
.aat-footer-links { display: flex; justify-content: center; gap: 32rpx; margin-top: 24rpx; }
.aat-footer-link { font-size: 28rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== 心理年龄测验报告（严格参照 web 版） ========== */
.psych-age-report-wrap { padding: 24rpx; }
.psych-age-report-paper {
  border-radius: 48rpx; overflow: hidden;
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08);
  background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08);
}
.psych-age-header {
  background: linear-gradient(135deg, rgba(var(--xia-mint-rgb), 0.25) 0%, rgba(var(--xia-cream-rgb), 0.4) 50%, #fff 100%);
  padding: 64rpx 48rpx;
  border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08);
}
.psych-age-header-row { display: flex; flex-direction: column; align-items: center; gap: 48rpx; }
@media (min-width: 600px) {
  .psych-age-header-row { flex-direction: row; align-items: center; justify-content: space-between; gap: 64rpx; }
  .psych-age-right { align-items: flex-start; text-align: left; }
  .psych-age-tags { justify-content: flex-start; }
}
.psych-age-left { display: flex; flex-direction: column; align-items: center; gap: 24rpx; flex-shrink: 0; }
.psych-age-ring-wrap { position: relative; width: 224rpx; height: 224rpx; flex-shrink: 0; }
.psych-age-ring-outer {
  position: absolute; inset: 0; border-radius: 50%;
  border: 12rpx solid rgba(var(--xia-sky-rgb), 0.35);
}
.psych-age-ring-fill {
  position: absolute; inset: 0; border-radius: 50%;
  -webkit-mask: radial-gradient(circle at center, transparent 72rpx, black 72rpx);
  mask: radial-gradient(circle at center, transparent 72rpx, black 72rpx);
}
.psych-age-ring-inner {
  position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%);
  width: 160rpx; height: 160rpx; border-radius: 50%;
  background: rgba(255,255,255,0.95); box-shadow: 0 8rpx 24rpx rgba(44,111,122,0.12);
  overflow: hidden; border: 4rpx solid rgba(var(--xia-teal-rgb), 0.2);
}
.psych-age-icon-img { width: 100%; height: 100%; display: block; }
.psych-age-left-text { text-align: center; }
.psych-age-label { font-size: 20rpx; font-weight: 600; letter-spacing: 0.25em; color: rgba(var(--xia-deep-rgb), 0.4); }
.psych-age-score { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 8rpx; }
.psych-age-right { flex: 1; min-width: 0; display: flex; flex-direction: column; align-items: center; gap: 24rpx; text-align: center; }
.psych-age-right-sub { font-size: 22rpx; font-weight: 500; letter-spacing: 0.2em; color: rgba(var(--xia-deep-rgb), 0.5); }
.psych-age-right-title { font-size: 48rpx; font-weight: 700; color: var(--xia-deep); line-height: 1.2; letter-spacing: -0.02em; }
.psych-age-right-desc { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.6; }
.psych-age-tags { display: flex; flex-wrap: wrap; justify-content: center; gap: 24rpx; }
.psych-age-tag { display: flex; align-items: center; gap: 12rpx; padding: 20rpx 32rpx; border-radius: 32rpx; border: 2rpx solid; box-shadow: 0 2rpx 8rpx rgba(var(--xia-deep-rgb), 0.06); }
.psych-age-tag-lbl { font-size: 26rpx; font-weight: 500; }
.psych-age-tag-val { font-size: 26rpx; font-weight: 600; }
.psych-age-trend-young { background: rgba(var(--xia-mint-rgb), 0.2); border-color: var(--xia-mint); color: var(--xia-deep); }
.psych-age-trend-balanced { background: rgba(var(--xia-sky-rgb), 0.2); border-color: var(--xia-sky); color: var(--xia-deep); }
.psych-age-trend-aged { background: rgba(var(--xia-aqua-rgb), 0.2); border-color: var(--xia-aqua); color: var(--xia-deep); }

.psych-age-body { padding: 48rpx 32rpx; }
.psych-age-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 48rpx 0; }
.psych-age-section { margin-bottom: 48rpx; }
.psych-age-section-title { font-size: 34rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }
.psych-age-range-desc { font-size: 28rpx; color: var(--xia-deep); line-height: 1.7; opacity: 0.9; }
.psych-age-chart-row { display: flex; flex-direction: column; gap: 40rpx; overflow: hidden; max-width: 100%; }
.psych-age-radar-wrap, .psych-age-bar-wrap { width: 100%; min-height: 360rpx; padding: 24rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.psych-age-chart-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 16rpx; }

.psych-age-dim-item { padding: 24rpx; border-radius: 16rpx; border: 2rpx solid; margin-bottom: 20rpx; }
.psych-age-dim-item:last-child { margin-bottom: 0; }
.psych-age-dim-item.psych-age-trend-young { background: rgba(var(--xia-mint-rgb), 0.15); border-color: rgba(var(--xia-mint-rgb), 0.4); }
.psych-age-dim-item.psych-age-trend-balanced { background: rgba(var(--xia-sky-rgb), 0.15); border-color: rgba(var(--xia-sky-rgb), 0.4); }
.psych-age-dim-item.psych-age-trend-aged { background: rgba(var(--xia-deep-rgb), 0.06); border-color: rgba(var(--xia-deep-rgb), 0.2); }
.psych-age-dim-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12rpx; }
.psych-age-dim-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.psych-age-dim-meta { font-size: 24rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.6); }
.psych-age-dim-desc { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; opacity: 0.9; }

.psych-age-guidance { margin-bottom: 0; }
.psych-age-guidance-inner { padding: 36rpx 32rpx; border-radius: 20rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: linear-gradient(135deg, rgba(var(--xia-cream-rgb), 0.4) 0%, rgba(var(--xia-sky-rgb), 0.06) 100%); }
.psych-age-guidance-block { margin-bottom: 32rpx; }
.psych-age-guidance-block:last-of-type { margin-bottom: 0; }
.psych-age-guidance-label { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 20rpx; padding-left: 8rpx; border-left: 6rpx solid var(--xia-teal); line-height: 1.5; }
.psych-age-guidance-list { display: flex; flex-direction: column; gap: 16rpx; }
.psych-age-guidance-item { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.85); line-height: 1.75; padding: 20rpx 24rpx; background: rgba(255,255,255,0.7); border-radius: 12rpx; border-left: 4rpx solid rgba(var(--xia-teal-rgb), 0.4); }
.psych-age-guidance-note { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.6); line-height: 1.7; margin-top: 28rpx; padding: 28rpx; background: rgba(var(--xia-sky-rgb), 0.12); border-radius: 16rpx; border: 2rpx dashed rgba(var(--xia-teal-rgb), 0.2); }

.psych-age-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.psych-age-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.psych-age-footer-links { display: flex; justify-content: center; gap: 32rpx; margin-top: 24rpx; }
.psych-age-footer-link { font-size: 28rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== APT 天赋潜能报告（严格参照 web 版） ========== */
.apt-report-wrap { padding: 24rpx; }
.apt-report-paper { border-radius: 48rpx; overflow: hidden; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08); }
.apt-header {
  background: linear-gradient(135deg, rgba(var(--xia-mint-rgb), 0.25) 0%, rgba(var(--xia-cream-rgb), 0.4) 50%, #fff 100%);
  padding: 64rpx 48rpx;
  border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08);
}
.apt-header-row { display: flex; flex-direction: column; align-items: center; gap: 48rpx; }
.apt-left { display: flex; flex-direction: column; align-items: center; gap: 24rpx; flex-shrink: 0; }
.apt-ring-wrap { position: relative; width: 224rpx; height: 224rpx; flex-shrink: 0; }
.apt-ring-outer { position: absolute; inset: 0; border-radius: 50%; border: 12rpx solid rgba(var(--xia-sky-rgb), 0.35); }
.apt-ring-fill {
  position: absolute; inset: 0; border-radius: 50%;
  -webkit-mask: radial-gradient(circle at center, transparent 72rpx, black 72rpx);
  mask: radial-gradient(circle at center, transparent 72rpx, black 72rpx);
}
.apt-ring-inner {
  position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%);
  width: 160rpx; height: 160rpx; border-radius: 50%;
  background: rgba(255,255,255,0.95); box-shadow: 0 8rpx 24rpx rgba(44,111,122,0.12);
  overflow: hidden; border: 4rpx solid rgba(var(--xia-teal-rgb), 0.2);
}
.apt-icon-img { width: 100%; height: 100%; padding: 32rpx; box-sizing: border-box; }
.apt-left-text { text-align: center; }
.apt-label { font-size: 20rpx; font-weight: 600; letter-spacing: 0.25em; color: rgba(var(--xia-deep-rgb), 0.4); }
.apt-score { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 8rpx; }
.apt-right { flex: 1; min-width: 0; display: flex; flex-direction: column; align-items: center; gap: 24rpx; text-align: center; }
.apt-right-sub { font-size: 22rpx; font-weight: 500; letter-spacing: 0.2em; color: rgba(var(--xia-deep-rgb), 0.5); }
.apt-right-title { font-size: 48rpx; font-weight: 700; color: var(--xia-deep); line-height: 1.2; letter-spacing: -0.02em; }
.apt-level-badge { display: inline-block; margin-top: 8rpx; padding: 12rpx 28rpx; border-radius: 16rpx; font-size: 26rpx; font-weight: 600; border: 2rpx solid; }
.apt-level-excellent { background: rgba(var(--xia-teal-rgb), 0.2); border-color: var(--xia-teal); color: var(--xia-deep); }
.apt-level-good { background: rgba(var(--xia-sky-rgb), 0.2); border-color: var(--xia-sky); color: var(--xia-deep); }
.apt-level-fair { background: rgba(var(--xia-aqua-rgb), 0.2); border-color: var(--xia-aqua); color: var(--xia-deep); }
.apt-level-low { background: rgba(var(--xia-deep-rgb), 0.1); border-color: var(--xia-deep); color: var(--xia-deep); }
.apt-right-desc { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.6; }
.apt-tags { display: flex; flex-wrap: wrap; justify-content: center; gap: 24rpx; }
.apt-tag { display: flex; align-items: center; gap: 12rpx; padding: 20rpx 32rpx; border-radius: 32rpx; border: 2rpx solid; box-shadow: 0 2rpx 8rpx rgba(var(--xia-deep-rgb), 0.06); }
.apt-tag-lbl { font-size: 26rpx; font-weight: 500; }
.apt-tag-val { font-size: 26rpx; font-weight: 600; }
@media (min-width: 600px) {
  .apt-header-row { flex-direction: row; align-items: center; justify-content: space-between; gap: 64rpx; }
  .apt-right { align-items: flex-start; text-align: left; }
  .apt-tags { justify-content: flex-start; }
}
.apt-body { padding: 48rpx 32rpx; }
.apt-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 48rpx 0; }
.apt-section { margin-bottom: 48rpx; }
.apt-section-title { font-size: 34rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }
.apt-charts-row { display: flex; flex-direction: column; gap: 40rpx; overflow: hidden; max-width: 100%; }
.apt-radar-wrap, .apt-bar-wrap { width: 100%; min-height: 360rpx; padding: 24rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.apt-chart-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 16rpx; }
.apt-career-card { font-size: 28rpx; color: var(--xia-deep); line-height: 1.7; padding: 32rpx; border-radius: 16rpx; background: rgba(var(--xia-mint-rgb), 0.15); border: 2rpx solid rgba(var(--xia-teal-rgb), 0.3); }
.apt-dim-item { padding: 24rpx; border-radius: 16rpx; border: 2rpx solid; margin-bottom: 20rpx; }
.apt-dim-item:last-child { margin-bottom: 0; }
.apt-dim-item.apt-level-excellent { background: rgba(var(--xia-mint-rgb), 0.15); border-color: rgba(var(--xia-teal-rgb), 0.4); }
.apt-dim-item.apt-level-good { background: rgba(var(--xia-sky-rgb), 0.15); border-color: rgba(var(--xia-sky-rgb), 0.4); }
.apt-dim-item.apt-level-fair { background: rgba(var(--xia-aqua-rgb), 0.1); border-color: rgba(var(--xia-deep-rgb), 0.15); }
.apt-dim-item.apt-level-low { background: rgba(var(--xia-deep-rgb), 0.06); border-color: rgba(var(--xia-deep-rgb), 0.2); }
.apt-dim-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12rpx; }
.apt-dim-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.apt-dim-meta { font-size: 24rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.6); }
.apt-dim-bar { height: 12rpx; background: rgba(var(--xia-deep-rgb), 0.1); border-radius: 999rpx; overflow: hidden; margin-bottom: 16rpx; }
.apt-dim-fill { height: 100%; background: var(--xia-teal); border-radius: 999rpx; }
.apt-dim-desc { font-size: 26rpx; color: var(--xia-deep); line-height: 1.6; opacity: 0.9; }
.apt-improve { font-size: 24rpx; color: var(--xia-teal); line-height: 1.6; margin-top: 12rpx; padding: 16rpx; border-radius: 12rpx; background: rgba(var(--xia-teal-rgb), 0.1); }
.apt-summary-section { margin-bottom: 0; }
.apt-summary-card {
  padding: 36rpx 32rpx; border-radius: 20rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08);
  background: linear-gradient(135deg, rgba(var(--xia-cream-rgb), 0.4) 0%, rgba(var(--xia-sky-rgb), 0.06) 100%);
}
.apt-summary-item {
  font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.85); line-height: 1.75;
  margin-bottom: 24rpx; padding: 20rpx 24rpx;
  background: rgba(255,255,255,0.7); border-radius: 12rpx; border-left: 4rpx solid rgba(var(--xia-teal-rgb), 0.4);
}
.apt-summary-item:last-child { margin-bottom: 0; }
.apt-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.apt-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.apt-footer-links { display: flex; justify-content: center; gap: 32rpx; margin-top: 24rpx; }
.apt-footer-link { font-size: 28rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== HIT 霍兰德职业兴趣报告（严格参照 web 版） ========== */
.hit-report-wrap { padding: 24rpx; }
.hit-report-paper { border-radius: 48rpx; overflow: hidden; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08); }
.hit-header {
  background: linear-gradient(135deg, rgba(var(--xia-sky-rgb), 0.2) 0%, rgba(var(--xia-cream-rgb), 0.4) 50%, #fff 100%);
  padding: 64rpx 48rpx;
  border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08);
}
.hit-header-row { display: flex; flex-direction: column; align-items: center; gap: 48rpx; }
.hit-left { flex-shrink: 0; }
.hit-icon-wrap { width: 160rpx; height: 160rpx; border-radius: 50%; overflow: hidden; background: rgba(255,255,255,0.95); box-shadow: 0 8rpx 24rpx rgba(44,111,122,0.15); border: 4rpx solid rgba(var(--xia-teal-rgb), 0.2); }
.hit-icon-img { width: 100%; height: 100%; display: block; }
.hit-right { flex: 1; min-width: 0; display: flex; flex-direction: column; align-items: center; gap: 24rpx; text-align: center; }
.hit-right-sub { font-size: 22rpx; font-weight: 500; letter-spacing: 0.2em; color: rgba(var(--xia-deep-rgb), 0.5); }
.hit-right-title { font-size: 44rpx; font-weight: 700; color: var(--xia-deep); line-height: 1.2; }
.hit-right-top3 { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.5; }
.hit-career-box { width: 100%; padding: 24rpx 32rpx; border-radius: 32rpx; border: 2rpx solid var(--xia-sky); background: rgba(var(--xia-sky-rgb), 0.15); text-align: left; }
.hit-career-label { font-size: 26rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 8rpx; }
.hit-career-val { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.9); line-height: 1.6; }
.hit-consistency-box { display: flex; align-items: center; gap: 8rpx; padding: 16rpx 24rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(255,255,255,0.7); }
.hit-consistency-lbl { font-size: 24rpx; font-weight: 600; color: var(--xia-teal); }
.hit-consistency-short { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.6); }
@media (min-width: 600px) {
  .hit-header-row { flex-direction: row; align-items: center; justify-content: space-between; gap: 64rpx; }
  .hit-right { align-items: flex-start; text-align: left; }
}
.hit-body { padding: 48rpx 32rpx; }
.hit-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 48rpx 0; }
.hit-section { margin-bottom: 48rpx; }
.hit-section-title { font-size: 34rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }
.hit-section-title { display: flex; align-items: center; gap: 12rpx; }
.hit-section-icon { width: 36rpx; height: 36rpx; border-radius: 50%; background: var(--xia-teal); opacity: 0.6; flex-shrink: 0; }
.hit-hexagon-card { display: flex; flex-direction: column; gap: 40rpx; padding: 40rpx; border-radius: 20rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); }
@media (min-width: 600px) { .hit-hexagon-card { flex-direction: row; align-items: center; gap: 64rpx; } }
.hit-hexagon-canvas-wrap { flex-shrink: 0; display: flex; justify-content: center; overflow: hidden; max-width: 100%; }
.hit-hexagon-canvas { width: 280px; height: 280px; max-width: 100%; }
.hit-hexagon-text { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 24rpx; }
.hit-code-intro-item { display: flex; align-items: flex-start; gap: 16rpx; }
.hit-intro-num { flex-shrink: 0; width: 40rpx; height: 40rpx; line-height: 40rpx; text-align: center; border-radius: 50%; font-size: 20rpx; font-weight: 700; }
.hit-code-intro-item:nth-child(1) .hit-intro-num { background: rgba(var(--xia-teal-rgb), 0.2); color: var(--xia-teal); }
.hit-code-intro-item:nth-child(2) .hit-intro-num { background: rgba(var(--xia-sky-rgb), 0.3); color: var(--xia-deep); }
.hit-intro-txt { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.85); line-height: 1.7; flex: 1; }
.hit-code-bold { font-weight: 600; color: var(--xia-teal); }
.hit-chart-wrap { width: 100%; min-height: 360rpx; padding: 32rpx; border-radius: 20rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: #fff; }
.hit-dim-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24rpx; }
@media (max-width: 500px) { .hit-dim-grid { grid-template-columns: 1fr; } }
.hit-dim-card { display: flex; gap: 24rpx; padding: 32rpx; border-radius: 20rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: #fff; }
.hit-dim-card.hit-top { border-color: var(--xia-sky); background: rgba(var(--xia-sky-rgb), 0.1); }
.hit-dim-card-icon { flex-shrink: 0; width: 80rpx; height: 80rpx; line-height: 80rpx; text-align: center; border-radius: 16rpx; font-size: 36rpx; font-weight: 700; }
.hit-dim-card-icon.hit-icon-top { background: rgba(var(--xia-teal-rgb), 0.2); color: var(--xia-teal); }
.hit-dim-card-icon.hit-icon-normal { background: rgba(var(--xia-deep-rgb), 0.08); color: rgba(var(--xia-deep-rgb), 0.7); }
.hit-dim-card-body { flex: 1; min-width: 0; }
.hit-dim-card-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8rpx; }
.hit-dim-card-name { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.hit-dim-card-score { font-size: 24rpx; font-weight: 700; color: var(--xia-teal); }
.hit-dim-role { font-size: 20rpx; color: var(--xia-teal); margin-bottom: 12rpx; }
.hit-dim-bar { height: 8rpx; background: rgba(var(--xia-deep-rgb), 0.1); border-radius: 999rpx; overflow: hidden; margin-bottom: 12rpx; }
.hit-dim-fill { height: 100%; background: var(--xia-sky); border-radius: 999rpx; }
.hit-dim-short { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.75); line-height: 1.5; }
.hit-usage-row { display: flex; flex-wrap: wrap; gap: 32rpx; padding: 24rpx 32rpx; border-radius: 16rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); }
.hit-usage-item { display: flex; align-items: center; gap: 12rpx; font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.75); }
.hit-usage-dot { width: 8rpx; height: 8rpx; border-radius: 50%; background: var(--xia-teal); }
.hit-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.hit-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.hit-footer-links { display: flex; justify-content: center; gap: 32rpx; margin-top: 24rpx; }
.hit-footer-link { font-size: 28rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== DTH 黑暗三角报告（严格参照 web 版） ========== */
.dth-report-wrap { padding: 24rpx; }
.dth-report-paper { border-radius: 48rpx; overflow: hidden; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08); }
.dth-header {
  background: linear-gradient(135deg, rgba(var(--xia-deep-rgb), 0.05) 0%, rgba(var(--xia-cream-rgb), 0.4) 50%, #fff 100%);
  padding: 64rpx 48rpx;
  border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08);
}
.dth-header-row { display: flex; flex-direction: column; align-items: center; gap: 48rpx; }
.dth-left { flex-shrink: 0; }
.dth-icon-wrap { width: 160rpx; height: 160rpx; border-radius: 50%; overflow: hidden; background: rgba(255,255,255,0.95); box-shadow: 0 8rpx 24rpx rgba(44,111,122,0.12); border: 4rpx solid rgba(var(--xia-teal-rgb), 0.2); display: flex; align-items: center; justify-content: center; }
.dth-icon-img { width: 80rpx; height: 80rpx; }
.dth-right { flex: 1; min-width: 0; display: flex; flex-direction: column; align-items: center; gap: 16rpx; text-align: center; }
.dth-right-sub { font-size: 22rpx; font-weight: 500; letter-spacing: 0.2em; color: rgba(var(--xia-deep-rgb), 0.5); }
.dth-right-title { font-size: 40rpx; font-weight: 700; color: var(--xia-deep); line-height: 1.2; }
.dth-right-meta { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.7); }
@media (min-width: 600px) {
  .dth-header-row { flex-direction: row; align-items: center; justify-content: space-between; gap: 64rpx; }
  .dth-right { align-items: flex-start; text-align: left; }
}
.dth-body { padding: 48rpx 32rpx; }
.dth-divider { height: 2rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin: 48rpx 0; }
.dth-section { margin-bottom: 48rpx; }
.dth-section-title { font-size: 34rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }
.dth-conclusion-card {
  display: flex; align-items: flex-start; gap: 32rpx;
  padding: 48rpx; border-radius: 32rpx; border: 4rpx solid rgba(var(--xia-deep-rgb), 0.2);
  background: linear-gradient(135deg, rgba(var(--xia-cream-rgb), 0.6) 0%, #fff 100%);
  box-shadow: 0 8rpx 24rpx rgba(var(--xia-deep-rgb), 0.06);
}
.dth-conclusion-icon-wrap { flex-shrink: 0; width: 96rpx; height: 96rpx; border-radius: 24rpx; display: flex; align-items: center; justify-content: center; }
.dth-conclusion-icon-wrap.dth-icon-low { background: rgba(var(--xia-mint-rgb), 0.3); }
.dth-conclusion-icon-wrap.dth-icon-moderate { background: rgba(var(--xia-sky-rgb), 0.3); }
.dth-conclusion-icon-wrap.dth-icon-high { background: rgba(var(--xia-deep-rgb), 0.15); }
.dth-conclusion-icon-inner { font-size: 48rpx; font-weight: 700; color: var(--xia-deep); }
.dth-conclusion-icon-wrap.dth-icon-low .dth-conclusion-icon-inner { color: var(--xia-teal); }
.dth-conclusion-content { flex: 1; min-width: 0; }
.dth-conclusion-title { font-size: 40rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 16rpx; }
.dth-conclusion-summary { font-size: 30rpx; color: rgba(var(--xia-deep-rgb), 0.9); line-height: 1.7; margin-bottom: 16rpx; }
.dth-conclusion-profile { font-size: 28rpx; font-weight: 500; color: var(--xia-teal); margin-bottom: 24rpx; opacity: 0.9; }
.dth-conclusion-hint { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.6; }
.dth-chart-wrap { width: 100%; min-height: 320rpx; padding: 32rpx; border-radius: 20rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: #fff; }
.dth-dim-card { padding: 40rpx; border-radius: 20rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: #fff; margin-bottom: 24rpx; box-shadow: 0 2rpx 12rpx rgba(var(--xia-deep-rgb), 0.04); }
.dth-dim-card:last-child { margin-bottom: 0; }
.dth-dim-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; }
.dth-dim-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.dth-dim-meta { font-size: 26rpx; font-weight: 700; color: var(--xia-teal); }
.dth-dim-bar { height: 12rpx; background: rgba(var(--xia-deep-rgb), 0.1); border-radius: 999rpx; overflow: hidden; margin-bottom: 20rpx; }
.dth-dim-fill { height: 100%; background: rgba(var(--xia-deep-rgb), 0.6); border-radius: 999rpx; }
.dth-dim-level-box { font-size: 26rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.9); padding: 24rpx; border-radius: 16rpx; background: rgba(var(--xia-cream-rgb), 0.4); margin-bottom: 20rpx; }
.dth-dim-desc { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.8); line-height: 1.6; }
.dth-disclaimer-card { padding: 40rpx; border-radius: 20rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); }
.dth-disclaimer-title { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); margin-bottom: 16rpx; }
.dth-disclaimer-text { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.8); line-height: 1.7; }
.dth-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.dth-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.dth-footer-links { display: flex; justify-content: center; gap: 32rpx; margin-top: 24rpx; }
.dth-footer-link { font-size: 28rpx; color: var(--xia-teal); text-decoration: underline; }

/* ========== TLA 年上年下报告（严格参照 web 版，粉色主题） ========== */
.tla-report-wrap { padding: 24rpx; }
.tla-report-paper {
  border-radius: 48rpx; overflow: hidden;
  border: 4rpx solid rgba(253,164,175,0.6);
  background: #fff;
  box-shadow: 0 8rpx 32rpx rgba(253,182,193,0.5);
}
.tla-header {
  position: relative; overflow: hidden;
  background: linear-gradient(135deg, rgba(255,182,193,0.8) 0%, rgba(255,250,250,0.9) 50%, rgba(255,248,220,0.7) 100%);
  padding: 64rpx 48rpx;
  border-bottom: 2rpx solid rgba(253,164,175,0.5);
}
.tla-header-row { display: flex; flex-direction: column; align-items: center; gap: 48rpx; }
.tla-left { flex-shrink: 0; }
.tla-icon-wrap {
  width: 192rpx; height: 192rpx; border-radius: 50%;
  background: rgba(255,255,255,0.9);
  box-shadow: 0 8rpx 24rpx rgba(244,114,182,0.2);
  border: 8rpx solid rgba(253,164,175,0.5);
  display: flex; align-items: center; justify-content: center; overflow: hidden;
}
.tla-icon-img { width: 96rpx; height: 96rpx; }
.tla-right { flex: 1; min-width: 0; display: flex; flex-direction: column; align-items: center; gap: 24rpx; text-align: center; }
.tla-right-sub { font-size: 22rpx; font-weight: 600; letter-spacing: 0.2em; color: rgba(190,18,60,0.8); display: flex; align-items: center; justify-content: center; gap: 8rpx; }
.tla-sparkles-img { width: 28rpx; height: 28rpx; }
.tla-right-title { font-size: 40rpx; font-weight: 700; color: #9f1239; line-height: 1.2; }
.tla-tags { display: flex; flex-wrap: wrap; justify-content: center; gap: 16rpx; }
.tla-tag { display: inline-flex; align-items: center; gap: 8rpx; padding: 12rpx 24rpx; border-radius: 999rpx; background: rgba(253,164,175,0.5); font-size: 26rpx; font-weight: 500; color: #9f1239; }
.tla-tag-icon { width: 28rpx; height: 28rpx; }
.tla-tag-emoji { font-size: 28rpx; }
@media (min-width: 600px) {
  .tla-header-row { flex-direction: row; align-items: center; gap: 64rpx; }
  .tla-right { align-items: flex-start; text-align: left; }
  .tla-right-sub { justify-content: flex-start; }
  .tla-tags { justify-content: flex-start; }
}
.tla-body { padding: 48rpx 32rpx; }
.tla-divider { height: 2rpx; background: rgba(253,164,175,0.3); margin: 48rpx 0; }
.tla-section { margin-bottom: 48rpx; }
.tla-portrait-card {
  display: flex; align-items: flex-start; gap: 32rpx;
  padding: 48rpx; border-radius: 32rpx;
  border: 4rpx solid rgba(253,164,175,0.6);
  background: linear-gradient(135deg, rgba(255,240,245,1) 0%, rgba(255,228,225,0.5) 100%);
  box-shadow: 0 8rpx 24rpx rgba(253,182,193,0.3);
}
.tla-portrait-icon { width: 64rpx; height: 64rpx; flex-shrink: 0; }
.tla-portrait-content { flex: 1; min-width: 0; }
.tla-portrait-title { font-size: 34rpx; font-weight: 700; color: #9f1239; margin-bottom: 16rpx; }
.tla-portrait-desc { font-size: 28rpx; color: rgba(190,18,60,0.9); line-height: 1.7; }
.tla-section-title { display: flex; align-items: center; gap: 12rpx; font-size: 34rpx; font-weight: 700; color: #9f1239; margin-bottom: 24rpx; }
.tla-section-icon { width: 40rpx; height: 40rpx; }
.tla-chart-wrap { width: 100%; min-height: 320rpx; padding: 32rpx; border-radius: 32rpx; border: 4rpx solid rgba(253,164,175,0.5); background: rgba(255,255,255,0.8); }
.tla-dim-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24rpx; }
.tla-dim-card {
  padding: 40rpx; border-radius: 32rpx; border: 4rpx solid rgba(253,164,175,0.5);
  background: #fff; overflow: hidden;
}
.tla-dim-card.tla-top {
  border-color: rgba(244,114,182,0.6);
  background: linear-gradient(135deg, rgba(255,240,245,1) 0%, rgba(255,228,225,0.5) 100%);
}
.tla-dim-head { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 20rpx; }
.tla-dim-name-row { display: flex; align-items: center; gap: 12rpx; flex-wrap: wrap; flex: 1; }
.tla-dim-icon { width: 40rpx; height: 40rpx; }
.tla-dim-emoji { font-size: 40rpx; }
.tla-dim-name { font-size: 28rpx; font-weight: 600; color: #9f1239; }
.tla-dim-badge { font-size: 22rpx; padding: 8rpx 16rpx; border-radius: 999rpx; background: rgba(244,114,182,0.5); color: #9f1239; }
.tla-dim-percent { font-size: 28rpx; font-weight: 700; color: #e11d48; flex-shrink: 0; }
.tla-dim-bar { height: 16rpx; background: rgba(253,164,175,0.4); border-radius: 999rpx; overflow: hidden; margin-bottom: 24rpx; }
.tla-dim-fill { height: 100%; background: linear-gradient(90deg, #f472b6, #f43f5e); border-radius: 999rpx; }
.tla-dim-desc { font-size: 26rpx; color: rgba(190,18,60,0.85); line-height: 1.6; }
.tla-tip-card {
  padding: 40rpx; border-radius: 32rpx;
  border: 2rpx solid rgba(253,164,175,0.5);
  background: rgba(255,240,245,0.5);
  font-size: 26rpx; color: rgba(190,18,60,0.8); line-height: 1.7;
}
@media (max-width: 500px) { .tla-dim-grid { grid-template-columns: 1fr; } }
.tla-footer {
  border-top: 2rpx solid rgba(253,164,175,0.5);
  background: rgba(255,240,245,0.3);
  padding: 48rpx 32rpx; text-align: center;
}
.tla-footer-text { font-size: 24rpx; color: rgba(190,18,60,0.7); }
.tla-footer-links { display: flex; flex-wrap: wrap; justify-content: center; align-items: center; gap: 24rpx; margin-top: 24rpx; }
.tla-footer-btn { padding: 16rpx 40rpx; border-radius: 999rpx; background: rgba(244,114,182,0.7); font-size: 28rpx; font-weight: 500; color: #9f1239; }
.tla-footer-link { font-size: 28rpx; font-weight: 500; color: #e11d48; text-decoration: underline; }

/* ========== FFT 水果塑报告（严格按 web 布局） ========== */
.fft-report-wrap { padding: 24rpx; }
.fft-report-paper {
  border-radius: 32rpx; overflow: hidden;
  border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12);
  background: #fff;
  box-shadow: 0 16rpx 48rpx rgba(var(--xia-deep-rgb), 0.08);
}

/* 头部：gradient bg，与 web 一致 */
.fft-header {
  position: relative;
  overflow: hidden;
  border-bottom: 2rpx solid rgba(var(--xia-haze-rgb), 0.8);
  background: linear-gradient(135deg, rgba(var(--xia-mint-rgb), 0.25) 0%, rgba(var(--xia-cream-rgb), 0.4) 50%, #fff 100%);
  padding: 64rpx 32rpx;
}
.fft-header-inner { position: relative; display: flex; flex-direction: column; align-items: center; gap: 32rpx; text-align: center; }
.fft-header-row { display: flex; flex-direction: column; align-items: center; gap: 32rpx; }
.fft-emoji-circle {
  width: 160rpx; height: 160rpx;
  display: flex; align-items: center; justify-content: center;
  border-radius: 50%;
  background: rgba(255,255,255,0.95);
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,0.06);
  border: 4rpx solid rgba(var(--xia-teal-rgb), 0.2);
}
.fft-emoji { font-size: 80rpx; }
.fft-header-text { display: flex; flex-direction: column; align-items: center; gap: 12rpx; }
.fft-meta { font-size: 20rpx; font-weight: 600; letter-spacing: 0.15em; text-transform: uppercase; color: rgba(var(--xia-deep-rgb), 0.5); }
.fft-title { font-size: 40rpx; font-weight: 700; color: var(--xia-deep); }
.fft-sub { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.7); }
.fft-tags { display: flex; flex-wrap: wrap; justify-content: center; gap: 16rpx; margin-top: 24rpx; }
.fft-tag {
  display: inline-flex; align-items: center; gap: 8rpx;
  padding: 12rpx 24rpx; border-radius: 999rpx;
  font-size: 22rpx; font-weight: 500;
}
.fft-tag-secondary { border: 2rpx solid rgba(var(--xia-haze-rgb), 0.8); background: rgba(255,255,255,0.8); color: var(--xia-deep); }
.fft-tag-tertiary { border: none; background: rgba(var(--xia-haze-rgb), 0.6); color: rgba(var(--xia-deep-rgb), 0.7); }

/* 九型得分 3 列网格 */
.fft-score-grid-wrap {
  width: 100%; max-width: 680rpx;
  padding: 24rpx 32rpx;
  border-radius: 24rpx;
  border: 2rpx solid rgba(var(--xia-haze-rgb), 0.6);
  background: rgba(255,255,255,0.8);
}
.fft-score-grid-title { font-size: 20rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.6); margin-bottom: 16rpx; }
.fft-score-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12rpx 16rpx;
}
.fft-score-cell {
  display: flex; align-items: center; gap: 8rpx;
  padding: 12rpx 16rpx; border-radius: 16rpx;
  font-size: 22rpx;
}
.fft-score-cell.fft-score-primary { background: rgba(var(--xia-teal-rgb), 0.15); font-weight: 600; color: var(--xia-deep); }
.fft-score-cell.fft-score-secondary { background: rgba(var(--xia-mint-rgb), 0.2); color: rgba(var(--xia-deep-rgb), 0.9); }
.fft-score-cell:not(.fft-score-primary):not(.fft-score-secondary) { background: rgba(var(--xia-haze-rgb), 0.3); color: rgba(var(--xia-deep-rgb), 0.7); }
.fft-score-emoji { flex-shrink: 0; font-size: 24rpx; }
.fft-score-name { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.fft-score-pct { flex-shrink: 0; font-weight: 600; font-variant-numeric: tabular-nums; }

/* Body 区域 */
.fft-body { padding: 48rpx 32rpx; overflow: hidden; max-width: 100%; }
.fft-section { margin-bottom: 64rpx; }
.fft-section:last-of-type { margin-bottom: 0; }
.fft-section-title {
  display: flex; align-items: center; gap: 12rpx;
  font-size: 34rpx; font-weight: 700; color: var(--xia-deep);
  margin-bottom: 24rpx;
}
.fft-section-emoji { font-size: 36rpx; }

/* 主型解读卡片 */
.fft-primary-card {
  padding: 32rpx 40rpx;
  border-radius: 24rpx;
  border: 2rpx solid rgba(var(--xia-mint-rgb), 0.5);
  background: rgba(var(--xia-mint-rgb), 0.1);
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.03);
}
.fft-primary-desc { font-size: 28rpx; line-height: 1.7; color: rgba(var(--xia-deep-rgb), 0.9); }
.fft-primary-disclaimer { font-size: 22rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 24rpx; }

/* 九维雷达 / 九型柱状图 */
.fft-radar-wrap, .fft-bar-wrap {
  width: 100%;
  min-height: 256rpx;
  padding: 24rpx 32rpx;
  border-radius: 24rpx;
  border: 2rpx solid rgba(var(--xia-haze-rgb), 0.8);
  background: #fff;
}
.fft-bar-wrap { min-height: 240rpx; }
.fft-chart-canvas { display: block; width: 100% !important; }

/* 九型概览网格 */
.fft-overview-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24rpx;
  overflow: hidden;
  max-width: 100%;
}
.fft-overview-card {
  padding: 24rpx 28rpx;
  border-radius: 24rpx;
  border: 2rpx solid rgba(var(--xia-haze-rgb), 0.8);
  background: #fff;
  overflow: hidden;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.03);
}
.fft-overview-card.fft-overview-primary {
  border-color: rgba(var(--xia-teal-rgb), 0.8);
  background: rgba(var(--xia-teal-rgb), 0.1);
}
.fft-overview-card.fft-overview-secondary {
  border-color: rgba(var(--xia-mint-rgb), 0.6);
  background: rgba(var(--xia-mint-rgb), 0.1);
}
.fft-overview-head { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 12rpx; }
.fft-overview-emoji { font-size: 40rpx; }
.fft-overview-badges { display: flex; align-items: center; gap: 12rpx; }
.fft-badge { padding: 4rpx 12rpx; border-radius: 8rpx; font-size: 18rpx; font-weight: 600; }
.fft-badge-primary { background: rgba(var(--xia-teal-rgb), 0.2); color: var(--xia-teal); }
.fft-badge-secondary { background: rgba(var(--xia-mint-rgb), 0.3); color: var(--xia-teal); }
.fft-overview-pct { font-size: 28rpx; font-weight: 700; color: var(--xia-deep); }
.fft-overview-name { font-size: 26rpx; font-weight: 600; color: var(--xia-deep); }
.fft-overview-bar {
  height: 8rpx; margin-top: 12rpx; margin-bottom: 12rpx;
  border-radius: 999rpx;
  background: rgba(var(--xia-haze-rgb), 0.5);
  overflow: hidden;
}
.fft-overview-fill { height: 100%; border-radius: 999rpx; transition: width 0.3s; }
.fft-overview-desc {
  font-size: 22rpx; line-height: 1.6;
  color: rgba(var(--xia-deep-rgb), 0.75);
  display: -webkit-box; -webkit-line-clamp: 2; overflow: hidden; -webkit-box-orient: vertical;
}

/* Footer */
.fft-footer {
  border-top: 2rpx solid rgba(var(--xia-haze-rgb), 0.8);
  background: rgba(var(--xia-cream-rgb), 0.2);
  padding: 40rpx 32rpx;
  text-align: center;
}
.fft-footer-text { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.fft-footer-links {
  display: flex; flex-wrap: wrap; justify-content: center; align-items: center;
  gap: 16rpx 24rpx;
  margin-top: 24rpx;
}
.fft-footer-link { font-size: 26rpx; font-weight: 500; color: var(--xia-teal); text-decoration: underline; }
.fft-footer-dot { font-size: 24rpx; color: rgba(var(--xia-haze-rgb), 1); }

/* 九型概览：小屏单列，中屏双列 */
@media (max-width: 500px) {
  .fft-overview-grid { grid-template-columns: 1fr; }
}
@media (min-width: 501px) and (max-width: 750px) {
  .fft-overview-grid { grid-template-columns: repeat(2, 1fr); }
}

/* ========== YBT 病娇报告（严格按 web 布局） ========== */
.ybt-report-wrap { padding: 24rpx; }
.ybt-report-paper {
  border-radius: 32rpx; overflow: hidden;
  border: 2rpx solid rgba(var(--xia-haze-rgb), 0.8);
  background: #fff;
  box-shadow: 0 16rpx 48rpx rgba(var(--xia-deep-rgb), 0.08);
}

/* 头部 */
.ybt-header {
  position: relative;
  overflow: hidden;
  border-bottom: 2rpx solid rgba(var(--xia-haze-rgb), 0.8);
  background: linear-gradient(135deg, rgba(255,228,230,0.8) 0%, rgba(var(--xia-cream-rgb), 0.5) 50%, #fff 100%);
  padding: 64rpx 32rpx;
}
.ybt-header-inner { position: relative; display: flex; flex-direction: column; align-items: center; gap: 32rpx; text-align: center; }
.ybt-avatar-wrap {
  width: 160rpx; height: 160rpx;
  border-radius: 50%;
  overflow: hidden;
  background: rgba(255,255,255,0.95);
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,0.06);
  border: 4rpx solid rgba(254,205,211,0.6);
}
.ybt-avatar { width: 100%; height: 100%; }
.ybt-header-text { display: flex; flex-direction: column; align-items: center; gap: 12rpx; }
.ybt-meta { font-size: 20rpx; font-weight: 600; letter-spacing: 0.15em; text-transform: uppercase; color: rgba(var(--xia-deep-rgb), 0.5); }
.ybt-title { font-size: 40rpx; font-weight: 700; color: var(--xia-deep); }
.ybt-sub { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.7); }

/* Body */
.ybt-body { padding: 48rpx 32rpx; overflow: hidden; max-width: 100%; }
.ybt-section { margin-bottom: 64rpx; }
.ybt-section:last-of-type { margin-bottom: 0; }
.ybt-section-title {
  display: flex; align-items: center; gap: 12rpx;
  font-size: 34rpx; font-weight: 700; color: var(--xia-deep);
  margin-bottom: 24rpx;
}
.ybt-section-icon { width: 40rpx; height: 40rpx; }

/* 综合结论卡片 */
.ybt-conclusion-card {
  overflow: hidden;
  border-radius: 24rpx;
  border: 4rpx solid rgba(254,205,211,0.6);
  padding: 32rpx 40rpx;
  background: linear-gradient(135deg, rgba(255,228,230,0.6) 0%, #fff 100%);
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.03);
}
.ybt-conclusion-card.ybt-conclusion-risk {
  border-color: rgba(251,207,232,0.6);
  background: linear-gradient(135deg, rgba(255,228,230,0.9) 0%, rgba(255,251,235,0.5) 100%);
}
.ybt-conclusion-row { display: flex; align-items: flex-start; gap: 32rpx; }
.ybt-conclusion-img-wrap {
  width: 96rpx; height: 96rpx;
  border-radius: 16rpx;
  overflow: hidden;
  flex-shrink: 0;
}
.ybt-conclusion-img { width: 100%; height: 100%; }
.ybt-conclusion-content { flex: 1; min-width: 0; }
.ybt-conclusion-title { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); }
.ybt-conclusion-summary { font-size: 28rpx; line-height: 1.7; color: rgba(var(--xia-deep-rgb), 0.9); margin-top: 16rpx; }
.ybt-conclusion-note { font-size: 28rpx; font-weight: 600; color: rgba(190,18,60,0.9); margin-top: 16rpx; }
.ybt-advice-box {
  display: flex; align-items: flex-start; gap: 16rpx;
  margin-top: 16rpx;
  padding: 24rpx;
  border-radius: 16rpx;
  background: rgba(255,255,255,0.6);
}
.ybt-advice-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.ybt-advice-text { font-size: 24rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); flex: 1; }

/* 四维雷达 / 四维柱状图 */
.ybt-radar-wrap, .ybt-bar-wrap {
  width: 100%;
  min-height: 256rpx;
  padding: 24rpx 32rpx;
  border-radius: 24rpx;
  border: 2rpx solid rgba(var(--xia-haze-rgb), 0.8);
  background: #fff;
}
.ybt-bar-wrap { min-height: 208rpx; }

/* 维度解读卡片 */
.ybt-dim-card {
  overflow: hidden;
  border-radius: 24rpx;
  border: 2rpx solid rgba(var(--xia-haze-rgb), 0.8);
  padding: 32rpx 36rpx;
  margin-bottom: 24rpx;
  background: #fff;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.03);
}
.ybt-dim-card:last-child { margin-bottom: 0; }
.ybt-dim-card.ybt-dim-dominant {
  border-color: rgba(254,205,211,0.8);
  background: rgba(255,228,230,0.4);
}
.ybt-dim-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; }
.ybt-dim-name-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; flex: 1; }
.ybt-dim-img { width: 56rpx; height: 56rpx; border-radius: 12rpx; flex-shrink: 0; }
.ybt-dim-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.ybt-dim-badge {
  padding: 4rpx 16rpx;
  border-radius: 999rpx;
  font-size: 20rpx; font-weight: 500;
  background: rgba(254,205,211,0.6);
  color: #9f1239;
}
.ybt-dim-meta { font-size: 28rpx; font-weight: 700; color: var(--xia-teal); flex-shrink: 0; }
.ybt-dim-bar {
  height: 12rpx;
  margin-bottom: 24rpx;
  border-radius: 999rpx;
  background: rgba(var(--xia-haze-rgb), 0.5);
  overflow: hidden;
}
.ybt-dim-fill { height: 100%; border-radius: 999rpx; background: var(--xia-teal); }
.ybt-dim-fill.ybt-dim-fill-high { background: rgb(190, 18, 60); }
.ybt-level-box {
  display: flex; align-items: flex-start; gap: 16rpx;
  margin-bottom: 16rpx;
  padding: 24rpx;
  border-radius: 16rpx;
  background: rgba(var(--xia-cream-rgb), 0.5);
}
.ybt-level-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 2rpx; }
.ybt-level-text { font-size: 24rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.9); line-height: 1.5; flex: 1; }
.ybt-dim-desc { font-size: 24rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); }

/* 恋爱建议 */
.ybt-tips-card {
  border-radius: 24rpx;
  border: 2rpx solid rgba(254,205,211,0.4);
  background: rgba(255,228,230,0.3);
  padding: 32rpx 40rpx;
}
.ybt-tip-item { display: flex; align-items: flex-start; gap: 16rpx; margin-bottom: 16rpx; }
.ybt-tip-item:last-child { margin-bottom: 0; }
.ybt-tip-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.ybt-tip-text { font-size: 28rpx; line-height: 1.5; color: rgba(var(--xia-deep-rgb), 0.9); flex: 1; }

/* 说明 */
.ybt-disclaimer-card {
  border-radius: 24rpx;
  border: 2rpx solid rgba(var(--xia-haze-rgb), 0.5);
  background: rgba(var(--xia-cream-rgb), 0.2);
  padding: 32rpx 40rpx;
}
.ybt-disclaimer-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.ybt-disclaimer-text { font-size: 24rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); margin-top: 16rpx; }

/* Footer */
.ybt-footer {
  border-top: 2rpx solid rgba(var(--xia-haze-rgb), 0.8);
  background: rgba(var(--xia-cream-rgb), 0.2);
  padding: 40rpx 32rpx;
  text-align: center;
}
.ybt-footer-text { font-size: 22rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.ybt-footer-links {
  display: flex; flex-wrap: wrap; justify-content: center; align-items: center;
  gap: 16rpx 24rpx;
  margin-top: 24rpx;
}
.ybt-footer-link { font-size: 26rpx; font-weight: 500; color: var(--xia-teal); text-decoration: underline; }
.ybt-footer-dot { font-size: 24rpx; color: rgba(var(--xia-haze-rgb), 1); }

/* ========== RVT 恋爱观报告 - 严格移植 RVTResult.tsx ========== */
.rvt-report-wrap { padding: 24rpx; }
.rvt-report-paper { border-radius: 24rpx; overflow: hidden; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08); }
.rvt-header { background: linear-gradient(135deg, rgba(255,182,193,0.2) 0%, rgba(var(--xia-cream-rgb), 0.4) 50%, #fff 100%); padding: 48rpx 32rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.rvt-header-inner { display: flex; flex-direction: column; align-items: center; gap: 32rpx; text-align: center; }
.rvt-icon-wrap { width: 96rpx; height: 96rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(255,182,193,0.4); box-shadow: 0 4rpx 16rpx rgba(244,114,182,0.2); border: 2rpx solid rgba(244,114,182,0.5); }
.rvt-primary-icon { width: 48rpx; height: 48rpx; }
.rvt-header-text { display: flex; flex-direction: column; align-items: center; gap: 12rpx; }
.rvt-meta { font-size: 20rpx; font-weight: 600; letter-spacing: 0.1em; color: rgba(var(--xia-deep-rgb), 0.5); }
.rvt-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); }
.rvt-primary-badge { display: inline-flex; align-items: center; gap: 12rpx; padding: 12rpx 24rpx; border-radius: 999rpx; background: rgba(244,114,182,0.15); border: 1rpx solid rgba(244,114,182,0.4); }
.rvt-badge-lbl { font-size: 20rpx; font-weight: 700; letter-spacing: 0.05em; color: rgb(190,18,60); }
.rvt-badge-name { font-size: 28rpx; font-weight: 600; color: rgb(155,28,64); }
.rvt-badge-pct { font-size: 28rpx; font-weight: 700; color: rgb(190,18,60); }
.rvt-secondary-badges { display: flex; flex-wrap: wrap; justify-content: center; gap: 16rpx; margin-top: 24rpx; }
.rvt-sec-badge, .rvt-third-badge { padding: 8rpx 20rpx; border-radius: 999rpx; font-size: 22rpx; }
.rvt-sec-badge { border: 1rpx solid rgba(var(--xia-deep-rgb), 0.1); background: rgba(255,255,255,0.8); font-weight: 500; color: var(--xia-deep); }
.rvt-third-badge { border: 1rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(255,255,255,0.6); color: rgba(var(--xia-deep-rgb), 0.7); }
.rvt-six-type-wrap { width: 100%; max-width: 560rpx; padding: 24rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(255,255,255,0.8); }
.rvt-six-type-title { font-size: 20rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.6); margin-bottom: 16rpx; }
.rvt-six-type-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16rpx; }
.rvt-six-cell { display: flex; align-items: center; gap: 12rpx; padding: 12rpx 16rpx; border-radius: 16rpx; font-size: 22rpx; background: rgba(var(--xia-deep-rgb), 0.06); color: rgba(var(--xia-deep-rgb), 0.7); }
.rvt-six-cell.rvt-six-primary { border: 2rpx solid rgba(244,114,182,0.5); background: rgba(244,114,182,0.2); font-weight: 700; color: rgb(127,29,61); }
.rvt-six-cell.rvt-six-secondary { background: rgba(255,182,193,0.3); font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.9); }
.rvt-six-main-tag { padding: 4rpx 8rpx; border-radius: 6rpx; background: rgba(244,114,182,0.3); font-size: 16rpx; font-weight: 700; color: rgb(155,28,64); flex-shrink: 0; }
.rvt-six-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; }
.rvt-six-name { flex: 1; min-width: 0; }
.rvt-six-pct { font-weight: 500; flex-shrink: 0; }
.rvt-body { padding: 48rpx 32rpx; }
.rvt-section { margin-bottom: 64rpx; }
.rvt-section:last-of-type { margin-bottom: 0; }
.rvt-section-title-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; margin-bottom: 24rpx; }
.rvt-section-icon { width: 40rpx; height: 40rpx; }
.rvt-section-title { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); }
.rvt-section-badge { padding: 8rpx 16rpx; border-radius: 999rpx; background: rgba(244,114,182,0.2); font-size: 22rpx; font-weight: 600; color: rgb(155,28,64); }
.rvt-conclusion-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(255,182,193,0.5); background: rgba(255,182,193,0.15); box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.rvt-conclusion-summary { font-size: 26rpx; line-height: 1.7; color: rgba(var(--xia-deep-rgb), 0.9); }
.rvt-conclusion-note { font-size: 28rpx; font-weight: 600; color: rgba(190,18,60,0.9); margin-top: 16rpx; }
.rvt-conclusion-disclaimer { font-size: 20rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 24rpx; }
.rvt-radar-wrap, .rvt-bar-wrap { width: 100%; min-height: 320rpx; padding: 24rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.rvt-dim-card { padding: 32rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); margin-bottom: 24rpx; overflow: hidden; background: #fff; box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.rvt-dim-card:last-child { margin-bottom: 0; }
.rvt-dim-card.rvt-dim-dominant { border-color: rgba(255,182,193,0.5); background: rgba(255,182,193,0.2); }
.rvt-dim-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; flex-wrap: wrap; gap: 12rpx; }
.rvt-dim-name-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; flex: 1; }
.rvt-dim-icon { width: 40rpx; height: 40rpx; flex-shrink: 0; }
.rvt-dim-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.rvt-dim-dominant-badge { padding: 8rpx 16rpx; border-radius: 999rpx; background: rgba(255,182,193,0.5); font-size: 20rpx; font-weight: 500; color: rgb(155,28,64); }
.rvt-dim-meta { font-size: 28rpx; font-weight: 700; color: var(--xia-teal); flex-shrink: 0; }
.rvt-dim-bar { height: 12rpx; width: 100%; overflow: hidden; border-radius: 999rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin-bottom: 16rpx; }
.rvt-dim-fill { height: 100%; border-radius: 999rpx; background: var(--xia-teal); }
.rvt-dim-fill.rvt-dim-fill-high { background: rgb(190, 18, 60); }
.rvt-level-box { display: flex; gap: 16rpx; padding: 16rpx 24rpx; border-radius: 16rpx; background: rgba(var(--xia-cream-rgb), 0.5); margin-bottom: 16rpx; }
.rvt-level-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.rvt-level-text { font-size: 26rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.9); line-height: 1.5; flex: 1; }
.rvt-dim-desc { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); }
.rvt-tips-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(255,182,193,0.3); background: rgba(255,182,193,0.15); }
.rvt-tip-item { display: flex; align-items: flex-start; gap: 16rpx; margin-bottom: 16rpx; }
.rvt-tip-item:last-child { margin-bottom: 0; }
.rvt-tip-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.rvt-tip-text { font-size: 28rpx; line-height: 1.5; color: rgba(var(--xia-deep-rgb), 0.9); flex: 1; }
.rvt-disclaimer-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); }
.rvt-disclaimer-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.rvt-disclaimer-text { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); margin-top: 16rpx; }
.rvt-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.rvt-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.rvt-footer-links { display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: 16rpx 32rpx; margin-top: 24rpx; }
.rvt-footer-link { font-size: 28rpx; font-weight: 500; color: var(--xia-teal); text-decoration: underline; }
.rvt-footer-dot { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.3); }

/* ========== MPT 亲密关系偏好报告 - 严格移植 MPTResult.tsx ========== */
.mpt-report-wrap { padding: 24rpx; }
.mpt-report-paper { border-radius: 24rpx; overflow: hidden; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08); }
.mpt-header { background: linear-gradient(135deg, rgba(var(--xia-mint-rgb), 0.5) 0%, rgba(var(--xia-cream-rgb), 0.8) 50%, #fff 100%); padding: 48rpx 32rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.mpt-header-inner { display: flex; flex-direction: column; align-items: center; gap: 32rpx; text-align: center; }
.mpt-icon-wrap { width: 96rpx; height: 96rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(var(--xia-mint-rgb), 0.6); box-shadow: 0 4rpx 16rpx rgba(var(--xia-aqua-rgb), 0.2); border: 2rpx solid rgba(var(--xia-aqua-rgb), 0.5); }
.mpt-primary-icon { width: 48rpx; height: 48rpx; }
.mpt-header-text { display: flex; flex-direction: column; align-items: center; gap: 12rpx; }
.mpt-meta { font-size: 20rpx; font-weight: 600; letter-spacing: 0.1em; color: rgba(var(--xia-deep-rgb), 0.5); }
.mpt-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); }
.mpt-primary-badge { display: inline-flex; align-items: center; gap: 12rpx; padding: 12rpx 24rpx; border-radius: 999rpx; background: rgba(var(--xia-aqua-rgb), 0.2); border: 1rpx solid rgba(var(--xia-teal-rgb), 0.4); }
.mpt-badge-lbl { font-size: 20rpx; font-weight: 700; letter-spacing: 0.05em; color: var(--xia-teal); }
.mpt-badge-name { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.mpt-badge-pct { font-size: 28rpx; font-weight: 700; color: var(--xia-teal); }
.mpt-secondary-badges { display: flex; flex-wrap: wrap; justify-content: center; gap: 16rpx; margin-top: 24rpx; }
.mpt-sec-badge, .mpt-third-badge { padding: 8rpx 20rpx; border-radius: 999rpx; font-size: 22rpx; }
.mpt-sec-badge { border: 1rpx solid rgba(var(--xia-deep-rgb), 0.1); background: rgba(255,255,255,0.8); font-weight: 500; color: var(--xia-deep); }
.mpt-third-badge { border: 1rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(255,255,255,0.6); color: rgba(var(--xia-deep-rgb), 0.7); }
.mpt-four-type-wrap { width: 100%; max-width: 100%; padding: 24rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(255,255,255,0.8); box-sizing: border-box; }
.mpt-four-type-title { font-size: 24rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.6); margin-bottom: 16rpx; }
.mpt-four-type-grid { display: flex; flex-wrap: wrap; gap: 16rpx; }
.mpt-four-cell { display: flex; align-items: center; gap: 12rpx; padding: 12rpx 16rpx; border-radius: 16rpx; font-size: 24rpx; background: rgba(var(--xia-deep-rgb), 0.06); color: rgba(var(--xia-deep-rgb), 0.7); flex: 1 1 45%; min-width: 140rpx; box-sizing: border-box; }
.mpt-four-cell.mpt-four-primary { border: 2rpx solid rgba(var(--xia-aqua-rgb), 0.5); background: rgba(var(--xia-mint-rgb), 0.5); font-weight: 700; color: var(--xia-deep); }
.mpt-four-cell.mpt-four-secondary { background: rgba(var(--xia-haze-rgb), 0.6); font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.9); }
.mpt-four-main-tag { padding: 4rpx 8rpx; border-radius: 6rpx; background: rgba(var(--xia-aqua-rgb), 0.4); font-size: 16rpx; font-weight: 700; color: var(--xia-teal); flex-shrink: 0; }
.mpt-four-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; }
.mpt-four-name { flex: 1; min-width: 0; }
.mpt-four-pct { font-weight: 500; flex-shrink: 0; }
.mpt-body { padding: 48rpx 32rpx; }
.mpt-section { margin-bottom: 64rpx; }
.mpt-section:last-of-type { margin-bottom: 0; }
.mpt-section-title-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; margin-bottom: 24rpx; }
.mpt-section-icon { width: 40rpx; height: 40rpx; }
.mpt-section-title { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }
.mpt-section-badge { padding: 8rpx 16rpx; border-radius: 999rpx; background: rgba(var(--xia-aqua-rgb), 0.2); font-size: 22rpx; font-weight: 600; color: var(--xia-teal); }
.mpt-conclusion-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-mint-rgb), 0.2); box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.mpt-conclusion-summary { font-size: 26rpx; line-height: 1.7; color: rgba(var(--xia-deep-rgb), 0.9); }
.mpt-conclusion-note { font-size: 28rpx; font-weight: 600; color: var(--xia-teal); margin-top: 16rpx; }
.mpt-conclusion-disclaimer { font-size: 20rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 24rpx; }
.mpt-pattern-card { margin-top: 32rpx; padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: #fff; }
.mpt-pattern-title { font-size: 26rpx; font-weight: 600; color: var(--xia-teal); margin-bottom: 16rpx; }
.mpt-pattern-analysis { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); }
.mpt-pattern-implication { font-size: 26rpx; font-weight: 500; color: var(--xia-teal); margin-top: 16rpx; }
.mpt-combo-card { margin-top: 32rpx; padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-mint-rgb), 0.2); }
.mpt-combo-title { font-size: 26rpx; font-weight: 600; color: var(--xia-teal); margin-bottom: 16rpx; }
.mpt-combo-text { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); }
.mpt-extended-card { margin-top: 32rpx; padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-cream-rgb), 0.5); }
.mpt-extended-title { font-size: 26rpx; font-weight: 600; color: var(--xia-teal); margin-bottom: 16rpx; }
.mpt-extended-row { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); margin-bottom: 12rpx; }
.mpt-extended-label { font-weight: 500; color: var(--xia-teal); }
.mpt-extended-val { color: rgba(var(--xia-deep-rgb), 0.9); }
.mpt-radar-wrap, .mpt-bar-wrap { width: 100%; min-height: 320rpx; padding: 24rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.mpt-dim-grid { display: flex; flex-wrap: wrap; gap: 24rpx; }
.mpt-dim-card { flex: 1 1 45%; min-width: 280rpx; padding: 32rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); overflow: hidden; background: #fff; box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.mpt-dim-card.mpt-dim-dominant { border-color: rgba(var(--xia-aqua-rgb), 0.5); background: rgba(var(--xia-mint-rgb), 0.3); }
.mpt-dim-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; flex-wrap: wrap; gap: 12rpx; }
.mpt-dim-name-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; flex: 1; }
.mpt-dim-icon { width: 40rpx; height: 40rpx; flex-shrink: 0; }
.mpt-dim-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.mpt-dim-dominant-badge { padding: 8rpx 16rpx; border-radius: 999rpx; background: rgba(var(--xia-aqua-rgb), 0.3); font-size: 20rpx; font-weight: 500; color: var(--xia-teal); }
.mpt-dim-meta { font-size: 28rpx; font-weight: 700; color: var(--xia-teal); flex-shrink: 0; }
.mpt-dim-bar { height: 12rpx; width: 100%; overflow: hidden; border-radius: 999rpx; background: rgba(var(--xia-haze-rgb), 0.5); margin-bottom: 16rpx; }
.mpt-dim-fill { height: 100%; border-radius: 999rpx; background: var(--xia-teal); }
.mpt-level-box { display: flex; gap: 16rpx; padding: 16rpx 24rpx; border-radius: 16rpx; background: rgba(var(--xia-cream-rgb), 0.5); margin-bottom: 16rpx; }
.mpt-level-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.mpt-level-text { font-size: 26rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.9); line-height: 1.5; flex: 1; }
.mpt-dim-suggestion { font-size: 26rpx; font-weight: 500; color: var(--xia-teal); margin-bottom: 12rpx; }
.mpt-dim-desc { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); }
.mpt-tips-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-mint-rgb), 0.2); }
.mpt-tip-item { display: flex; align-items: flex-start; gap: 16rpx; margin-bottom: 16rpx; }
.mpt-tip-item:last-child { margin-bottom: 0; }
.mpt-tip-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.mpt-tip-text { font-size: 28rpx; line-height: 1.5; color: rgba(var(--xia-deep-rgb), 0.9); flex: 1; }
.mpt-growth-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-cream-rgb), 0.5); }
.mpt-growth-direction { font-size: 30rpx; font-weight: 600; color: var(--xia-teal); }
.mpt-growth-reason { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); margin-top: 12rpx; }
.mpt-growth-item { display: flex; align-items: flex-start; gap: 16rpx; margin-top: 12rpx; }
.mpt-growth-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.mpt-growth-text { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); flex: 1; }
.mpt-cautions-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-sky-rgb), 0.5); background: rgba(var(--xia-sky-rgb), 0.15); }
.mpt-cautions-title { font-size: 30rpx; font-weight: 600; color: var(--xia-teal); }
.mpt-caution-item { display: flex; align-items: flex-start; gap: 16rpx; margin-top: 16rpx; }
.mpt-caution-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.mpt-caution-text { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); flex: 1; }
.mpt-partner-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-cream-rgb), 0.5); }
.mpt-partner-lbl { font-size: 26rpx; font-weight: 600; color: var(--xia-teal); margin-top: 24rpx; }
.mpt-partner-lbl:first-child { margin-top: 0; }
.mpt-partner-match { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); margin-top: 12rpx; }
.mpt-partner-item { display: flex; align-items: flex-start; gap: 16rpx; margin-top: 12rpx; }
.mpt-partner-icon { width: 28rpx; height: 28rpx; flex-shrink: 0; margin-top: 4rpx; }
.mpt-partner-text { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); flex: 1; }
.mpt-disclaimer-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); }
.mpt-disclaimer-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.mpt-disclaimer-text { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); margin-top: 16rpx; }
.mpt-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.mpt-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.mpt-footer-links { display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: 16rpx 32rpx; margin-top: 24rpx; }
.mpt-footer-link { font-size: 28rpx; font-weight: 500; color: var(--xia-teal); text-decoration: underline; }
.mpt-footer-dot { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.3); }

/* ========== VBT 易被欺负报告 - 严格移植 VBTResult.tsx ========== */
.vbt-report-wrap { padding: 24rpx; }
.vbt-report-paper { border-radius: 24rpx; overflow: hidden; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08); }
.vbt-header { background: linear-gradient(135deg, rgba(var(--xia-mint-rgb), 0.5) 0%, rgba(var(--xia-cream-rgb), 0.8) 50%, #fff 100%); padding: 48rpx 32rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.vbt-header-inner { display: flex; flex-direction: column; align-items: center; gap: 32rpx; text-align: center; }
.vbt-icon-wrap { width: 96rpx; height: 96rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(var(--xia-mint-rgb), 0.6); box-shadow: 0 4rpx 16rpx rgba(var(--xia-aqua-rgb), 0.2); border: 2rpx solid rgba(var(--xia-aqua-rgb), 0.5); }
.vbt-primary-icon { width: 48rpx; height: 48rpx; }
.vbt-header-text { display: flex; flex-direction: column; align-items: center; gap: 12rpx; }
.vbt-meta { font-size: 20rpx; font-weight: 600; letter-spacing: 0.1em; color: rgba(var(--xia-deep-rgb), 0.5); }
.vbt-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); }
.vbt-zone-badge { display: inline-flex; align-items: center; gap: 12rpx; flex-wrap: wrap; justify-content: center; padding: 12rpx 24rpx; border-radius: 999rpx; border: 1rpx solid rgba(var(--xia-teal-rgb), 0.4); }
.vbt-zone-badge.vbt-zone-low { background: rgba(var(--xia-mint-rgb), 0.5); color: var(--xia-teal); }
.vbt-zone-badge.vbt-zone-low-mid { background: rgba(var(--xia-aqua-rgb), 0.2); color: var(--xia-teal); }
.vbt-zone-badge.vbt-zone-mid { background: rgba(var(--xia-sky-rgb), 0.2); color: var(--xia-teal); }
.vbt-zone-badge.vbt-zone-mid-high { background: rgba(var(--xia-sky-rgb), 0.3); color: var(--xia-deep); border-color: rgba(var(--xia-sky-rgb), 0.5); }
.vbt-zone-badge.vbt-zone-high { background: rgba(var(--xia-deep-rgb), 0.1); color: var(--xia-deep); border-color: rgba(var(--xia-teal-rgb), 0.5); }
.vbt-badge-lbl { font-size: 20rpx; font-weight: 700; letter-spacing: 0.05em; }
.vbt-badge-num { font-size: 28rpx; font-weight: 700; }
.vbt-badge-range { font-size: 22rpx; opacity: 0.9; }
.vbt-zone-analysis { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.5; margin-top: 16rpx; }
.vbt-four-type-wrap { width: 100%; max-width: 100%; padding: 24rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 0.6); background: rgba(255,255,255,0.8); box-sizing: border-box; }
.vbt-four-type-title { font-size: 24rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.6); margin-bottom: 16rpx; }
.vbt-four-type-grid { display: flex; flex-wrap: wrap; gap: 16rpx; }
.vbt-four-cell { display: flex; align-items: center; gap: 12rpx; padding: 12rpx 16rpx; border-radius: 16rpx; font-size: 24rpx; background: rgba(var(--xia-haze-rgb), 0.3); color: rgba(var(--xia-deep-rgb), 0.7); flex: 1 1 45%; min-width: 140rpx; box-sizing: border-box; }
.vbt-four-cell.vbt-four-strong { border: 2rpx solid rgba(var(--xia-aqua-rgb), 0.5); background: rgba(var(--xia-mint-rgb), 0.5); font-weight: 700; color: var(--xia-deep); }
.vbt-four-cell.vbt-four-weak { background: rgba(var(--xia-sky-rgb), 0.3); font-weight: 600; color: var(--xia-deep); }
.vbt-four-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; }
.vbt-four-name { flex: 1; min-width: 0; }
.vbt-four-pct { font-weight: 500; flex-shrink: 0; }
.vbt-body { padding: 48rpx 32rpx; }
.vbt-section { margin-bottom: 64rpx; }
.vbt-section:last-of-type { margin-bottom: 0; }
.vbt-section-title-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; margin-bottom: 24rpx; }
.vbt-section-icon { width: 40rpx; height: 40rpx; }
.vbt-section-title { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }
.vbt-section-badge { padding: 8rpx 16rpx; border-radius: 999rpx; background: rgba(var(--xia-aqua-rgb), 0.2); font-size: 22rpx; font-weight: 600; color: var(--xia-teal); }
.vbt-conclusion-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-mint-rgb), 0.2); box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.vbt-conclusion-summary { font-size: 26rpx; line-height: 1.7; color: rgba(var(--xia-deep-rgb), 0.9); }
.vbt-conclusion-note { font-size: 28rpx; font-weight: 600; color: var(--xia-teal); margin-top: 16rpx; }
.vbt-conclusion-disclaimer { font-size: 20rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 24rpx; }
.vbt-zone-card { margin-top: 32rpx; padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: #fff; }
.vbt-zone-card-title { font-size: 26rpx; font-weight: 600; color: var(--xia-teal); margin-bottom: 16rpx; }
.vbt-zone-card-analysis { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); }
.vbt-zone-card-implication { font-size: 26rpx; font-weight: 500; color: var(--xia-teal); margin-top: 16rpx; }
.vbt-weakest-card { margin-top: 32rpx; padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-cream-rgb), 0.5); }
.vbt-weakest-title { font-size: 26rpx; font-weight: 600; color: var(--xia-teal); margin-bottom: 16rpx; }
.vbt-weakest-row { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); margin-bottom: 12rpx; }
.vbt-weakest-label { font-weight: 500; color: var(--xia-teal); }
.vbt-weakest-val { color: rgba(var(--xia-deep-rgb), 0.9); }
.vbt-radar-wrap, .vbt-bar-wrap { width: 100%; min-height: 320rpx; padding: 24rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.vbt-dim-grid { display: flex; flex-wrap: wrap; gap: 24rpx; }
.vbt-dim-card { flex: 1 1 45%; min-width: 280rpx; padding: 32rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-haze-rgb), 1); overflow: hidden; background: #fff; box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.vbt-dim-card.vbt-dim-weak { border-color: rgba(var(--xia-sky-rgb), 0.5); background: rgba(var(--xia-sky-rgb), 0.15); }
.vbt-dim-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; flex-wrap: wrap; gap: 12rpx; }
.vbt-dim-name-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; flex: 1; }
.vbt-dim-icon { width: 40rpx; height: 40rpx; flex-shrink: 0; }
.vbt-dim-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.vbt-dim-weak-badge { padding: 8rpx 16rpx; border-radius: 999rpx; background: rgba(var(--xia-sky-rgb), 0.4); font-size: 20rpx; font-weight: 500; color: var(--xia-teal); }
.vbt-dim-meta { font-size: 28rpx; font-weight: 700; color: var(--xia-teal); flex-shrink: 0; }
.vbt-dim-bar { height: 12rpx; width: 100%; overflow: hidden; border-radius: 999rpx; background: rgba(var(--xia-haze-rgb), 0.5); margin-bottom: 16rpx; }
.vbt-dim-fill { height: 100%; border-radius: 999rpx; }
.vbt-dim-fill.vbt-bar-teal { background: var(--xia-teal); }
.vbt-dim-fill.vbt-bar-sky { background: var(--xia-sky); }
.vbt-dim-fill.vbt-bar-aqua { background: var(--xia-aqua); }
.vbt-level-box { display: flex; gap: 16rpx; padding: 16rpx 24rpx; border-radius: 16rpx; background: rgba(var(--xia-cream-rgb), 0.5); margin-bottom: 16rpx; }
.vbt-level-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.vbt-level-text { font-size: 26rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.9); line-height: 1.5; flex: 1; }
.vbt-dim-suggestion { font-size: 26rpx; font-weight: 500; color: var(--xia-teal); margin-bottom: 12rpx; }
.vbt-dim-desc { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); }
.vbt-tips-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-mint-rgb), 0.2); }
.vbt-tip-item { display: flex; align-items: flex-start; gap: 16rpx; margin-bottom: 16rpx; }
.vbt-tip-item:last-child { margin-bottom: 0; }
.vbt-tip-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.vbt-tip-text { font-size: 28rpx; line-height: 1.5; color: rgba(var(--xia-deep-rgb), 0.9); flex: 1; }
.vbt-growth-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-cream-rgb), 0.5); }
.vbt-growth-direction { font-size: 30rpx; font-weight: 600; color: var(--xia-teal); }
.vbt-growth-reason { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); margin-top: 12rpx; }
.vbt-cautions-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-sky-rgb), 0.5); background: rgba(var(--xia-sky-rgb), 0.15); }
.vbt-caution-item { display: flex; align-items: flex-start; gap: 16rpx; margin-top: 16rpx; }
.vbt-caution-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.vbt-caution-text { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.9); flex: 1; }
.vbt-disclaimer-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); }
.vbt-disclaimer-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.vbt-disclaimer-text { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); margin-top: 16rpx; }
.vbt-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.vbt-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.vbt-footer-links { display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: 16rpx 32rpx; margin-top: 24rpx; }
.vbt-footer-link { font-size: 28rpx; font-weight: 500; color: var(--xia-teal); text-decoration: underline; }
.vbt-footer-dot { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.3); }

/* ========== City 宜居城市报告 - 严格移植 CityResult.tsx ========== */
.city-report-wrap { padding: 24rpx; }
.city-report-paper { border-radius: 24rpx; overflow: hidden; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08); }
.city-header { background: linear-gradient(135deg, rgba(var(--xia-mint-rgb), 0.5) 0%, rgba(var(--xia-cream-rgb), 0.8) 50%, #fff 100%); padding: 48rpx 32rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.city-header-inner { display: flex; flex-direction: column; align-items: center; gap: 32rpx; text-align: center; }
.city-icon-wrap { width: 96rpx; height: 96rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(var(--xia-mint-rgb), 0.6); box-shadow: 0 4rpx 16rpx rgba(var(--xia-aqua-rgb), 0.2); border: 2rpx solid rgba(var(--xia-aqua-rgb), 0.5); }
.city-primary-icon { width: 48rpx; height: 48rpx; }
.city-header-text { display: flex; flex-direction: column; align-items: center; gap: 12rpx; }
.city-meta { font-size: 20rpx; font-weight: 600; letter-spacing: 0.1em; color: rgba(var(--xia-deep-rgb), 0.5); }
.city-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); }
.city-top-badges { display: flex; flex-wrap: wrap; justify-content: center; gap: 24rpx; margin-top: 24rpx; }
.city-badge { display: flex; align-items: center; justify-content: center; flex-wrap: wrap; gap: 8rpx; padding: 16rpx 24rpx; border-radius: 24rpx; overflow: hidden; min-height: 80rpx; box-sizing: border-box; }
.city-badge.city-badge-first { border: 2rpx solid rgba(var(--xia-aqua-rgb), 0.5); background: rgba(var(--xia-mint-rgb), 0.6); box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.06); }
.city-badge.city-badge-second { border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(255,255,255,0.9); }
.city-badge-name { font-size: 28rpx; font-weight: 700; flex-shrink: 0; }
.city-badge.city-badge-first .city-badge-name { color: var(--xia-teal); }
.city-badge.city-badge-second .city-badge-name { color: var(--xia-deep); }
.city-badge-province { font-size: 20rpx; color: rgba(var(--xia-deep-rgb), 0.6); flex-shrink: 0; }
.city-badge-match { padding: 4rpx 12rpx; border-radius: 999rpx; background: rgba(var(--xia-aqua-rgb), 0.3); font-size: 20rpx; font-weight: 700; color: var(--xia-teal); flex-shrink: 0; }
.city-profile-line { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.5; margin-top: 24rpx; }
.city-five-type-wrap { width: 100%; max-width: 100%; padding: 24rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 0.6); background: rgba(255,255,255,0.8); box-sizing: border-box; }
.city-five-type-title { font-size: 24rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.6); margin-bottom: 16rpx; }
.city-five-type-grid { display: flex; flex-wrap: wrap; gap: 16rpx; }
.city-five-cell { display: flex; align-items: center; gap: 12rpx; padding: 12rpx 16rpx; border-radius: 16rpx; font-size: 24rpx; background: rgba(var(--xia-haze-rgb), 0.3); color: rgba(var(--xia-deep-rgb), 0.7); flex: 1 1 28%; min-width: 120rpx; box-sizing: border-box; }
.city-five-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; }
.city-five-name { flex: 1; min-width: 0; }
.city-five-pct { font-weight: 500; flex-shrink: 0; }
.city-body { padding: 48rpx 32rpx; }
.city-section { margin-bottom: 64rpx; }
.city-section:last-of-type { margin-bottom: 0; }
.city-section-title-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; margin-bottom: 24rpx; }
.city-section-icon { width: 40rpx; height: 40rpx; }
.city-section-title { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); margin-bottom: 24rpx; }
.city-detail-cards { display: flex; flex-direction: column; gap: 32rpx; }
.city-detail-card { padding: 32rpx; border-radius: 24rpx; overflow: hidden; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: #fff; box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.city-detail-card.city-detail-first { border-color: rgba(var(--xia-aqua-rgb), 0.5); background: rgba(var(--xia-mint-rgb), 0.3); }
.city-detail-card.city-detail-second { border-color: rgba(var(--xia-haze-rgb), 1); }
.city-detail-row { display: flex; flex-direction: column; gap: 24rpx; align-items: stretch; }
.city-detail-row .city-detail-right { align-self: flex-end; }
.city-detail-left { flex: 1; min-width: 0; }
.city-detail-header { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; }
.city-rec-badge { display: inline-flex; align-items: center; justify-content: center; padding: 8rpx 20rpx; border-radius: 999rpx; font-size: 24rpx; font-weight: 700; }
.city-rec-badge.city-rec-first { background: var(--xia-teal); color: #fff; }
.city-rec-badge.city-rec-second { background: rgba(var(--xia-mint-rgb), 0.5); color: var(--xia-teal); border: 1rpx solid rgba(var(--xia-teal-rgb), 0.4); }
.city-detail-name { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); }
.city-detail-province { font-size: 28rpx; font-weight: 400; color: rgba(var(--xia-deep-rgb), 0.6); margin-left: 8rpx; }
.city-detail-brief { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.8); line-height: 1.6; margin-top: 12rpx; }
.city-detail-reason { font-size: 26rpx; font-weight: 500; color: var(--xia-teal); margin-top: 16rpx; }
.city-detail-right { flex-shrink: 0; text-align: right; }
.city-detail-percent { font-size: 48rpx; font-weight: 700; color: var(--xia-teal); }
.city-detail-percent-lbl { font-size: 20rpx; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 4rpx; }
.city-dim-tags { display: flex; flex-wrap: wrap; gap: 12rpx; margin-top: 24rpx; }
.city-dim-tag { padding: 8rpx 16rpx; border-radius: 12rpx; background: rgba(var(--xia-haze-rgb), 0.5); font-size: 20rpx; color: rgba(var(--xia-deep-rgb), 0.8); }
.city-methodology-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-cream-rgb), 0.3); }
.city-methodology-step { display: flex; gap: 24rpx; margin-bottom: 24rpx; }
.city-methodology-step:last-of-type { margin-bottom: 0; }
.city-methodology-num { width: 56rpx; height: 56rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(var(--xia-aqua-rgb), 0.4); font-size: 24rpx; font-weight: 700; color: var(--xia-teal); }
.city-methodology-content { flex: 1; min-width: 0; }
.city-methodology-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.city-methodology-desc { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.85); line-height: 1.6; margin-top: 8rpx; }
.city-methodology-disclaimer { font-size: 20rpx; color: rgba(var(--xia-deep-rgb), 0.6); line-height: 1.6; margin-top: 24rpx; padding-top: 24rpx; border-top: 1rpx solid rgba(var(--xia-haze-rgb), 0.5); }
.city-radar-wrap, .city-bar-wrap { width: 100%; min-height: 320rpx; padding: 24rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.city-dim-grid { display: flex; flex-wrap: wrap; gap: 24rpx; }
.city-dim-card { flex: 1 1 45%; min-width: 280rpx; padding: 32rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-haze-rgb), 1); overflow: hidden; background: #fff; box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.city-dim-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; flex-wrap: wrap; gap: 12rpx; }
.city-dim-name-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; flex: 1; }
.city-dim-icon { width: 40rpx; height: 40rpx; flex-shrink: 0; }
.city-dim-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.city-dim-meta { font-size: 28rpx; font-weight: 700; color: var(--xia-teal); flex-shrink: 0; }
.city-dim-bar { height: 12rpx; width: 100%; overflow: hidden; border-radius: 999rpx; background: rgba(var(--xia-haze-rgb), 0.5); margin-bottom: 16rpx; }
.city-dim-fill { height: 100%; border-radius: 999rpx; background: var(--xia-teal); }
.city-level-box { display: flex; gap: 16rpx; padding: 16rpx 24rpx; border-radius: 16rpx; background: rgba(var(--xia-cream-rgb), 0.5); margin-bottom: 16rpx; }
.city-level-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.city-level-text { font-size: 26rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.9); line-height: 1.5; flex: 1; }
.city-dim-desc { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); }
.city-typical-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-haze-rgb), 1); background: rgba(var(--xia-mint-rgb), 0.2); }
.city-typical-focus { font-size: 28rpx; font-weight: 500; color: var(--xia-deep); }
.city-typical-cities { font-size: 26rpx; color: rgba(var(--xia-deep-rgb), 0.7); line-height: 1.5; margin-top: 12rpx; }
.city-disclaimer-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); }
.city-disclaimer-title { font-size: 28rpx; font-weight: 600; color: var(--xia-deep); }
.city-disclaimer-text { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); margin-top: 16rpx; }
.city-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.city-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.city-footer-links { display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: 16rpx 32rpx; margin-top: 24rpx; }
.city-footer-link { font-size: 28rpx; font-weight: 500; color: var(--xia-teal); text-decoration: underline; }
.city-footer-dot { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.3); }

/* ========== LBT 恋爱脑报告 - 严格移植 LBTResult.tsx ========== */
.lbt-report-wrap { padding: 24rpx; }
.lbt-report-paper { border-radius: 24rpx; overflow: hidden; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.12); background: #fff; box-shadow: 0 8rpx 32rpx rgba(var(--xia-deep-rgb), 0.08); }
.lbt-header { background: linear-gradient(135deg, rgba(251,207,232,0.3) 0%, rgba(var(--xia-cream-rgb), 0.4) 50%, #fff 100%); padding: 48rpx 32rpx; border-bottom: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); }
.lbt-header-inner { display: flex; flex-direction: column; align-items: center; gap: 32rpx; text-align: center; }
.lbt-icon-wrap { width: 96rpx; height: 96rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(255,255,255,0.95); box-shadow: 0 4rpx 16rpx rgba(236,72,153,0.2); border: 2rpx solid rgba(236,72,153,0.4); }
.lbt-header-icon { width: 48rpx; height: 48rpx; }
.lbt-header-text { display: flex; flex-direction: column; align-items: center; gap: 12rpx; }
.lbt-meta { font-size: 20rpx; font-weight: 600; letter-spacing: 0.1em; color: rgba(var(--xia-deep-rgb), 0.5); }
.lbt-title { font-size: 36rpx; font-weight: 700; color: var(--xia-deep); }
.lbt-overall-badge { display: inline-flex; align-items: center; gap: 12rpx; padding: 12rpx 24rpx; border-radius: 999rpx; background: rgba(236,72,153,0.15); border: 1rpx solid rgba(236,72,153,0.4); }
.lbt-badge-lbl { font-size: 20rpx; font-weight: 700; letter-spacing: 0.05em; color: rgb(190,24,93); }
.lbt-badge-val { font-size: 28rpx; font-weight: 600; color: rgb(157,23,77); }
.lbt-badge-pct { font-size: 28rpx; font-weight: 700; color: rgb(190,24,93); }
.lbt-three-type-wrap { width: 100%; max-width: 100%; padding: 24rpx; border-radius: 24rpx; border: 1rpx solid rgba(var(--xia-deep-rgb), 0.12); background: rgba(255,255,255,0.8); box-sizing: border-box; }
.lbt-three-type-title { font-size: 24rpx; font-weight: 600; color: rgba(var(--xia-deep-rgb), 0.6); margin-bottom: 20rpx; }
.lbt-three-type-grid { display: flex; flex-wrap: wrap; gap: 16rpx; }
.lbt-three-cell { display: flex; align-items: center; gap: 12rpx; padding: 16rpx 20rpx; border-radius: 16rpx; font-size: 26rpx; background: rgba(var(--xia-deep-rgb), 0.06); color: rgba(var(--xia-deep-rgb), 0.85); flex: 1 1 45%; min-width: 200rpx; box-sizing: border-box; }
.lbt-three-cell.lbt-three-highest { border: 2rpx solid rgba(236,72,153,0.6); background: rgba(236,72,153,0.2); font-weight: 700; color: rgb(157,23,77); }
.lbt-three-icon { width: 36rpx; height: 36rpx; flex-shrink: 0; }
.lbt-three-name { flex: 1; min-width: 0; font-size: 26rpx; word-break: keep-all; }
.lbt-three-pct { font-weight: 600; flex-shrink: 0; font-size: 26rpx; }
.lbt-body { padding: 48rpx 32rpx; }
.lbt-section { margin-bottom: 64rpx; }
.lbt-section:last-of-type { margin-bottom: 0; }
.lbt-section-title-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; margin-bottom: 24rpx; }
.lbt-section-icon { width: 40rpx; height: 40rpx; }
.lbt-section-title { font-size: 32rpx; font-weight: 700; color: var(--xia-deep); }
.lbt-conclusion-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(252,231,243,0.8); background: rgba(253,242,248,0.6); box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.lbt-conclusion-summary { font-size: 26rpx; line-height: 1.7; color: rgba(var(--xia-deep-rgb), 0.9); }
.lbt-conclusion-note { font-size: 28rpx; font-weight: 600; color: rgb(190,24,93); margin-top: 16rpx; }
.lbt-conclusion-disclaimer { font-size: 20rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.6); margin-top: 24rpx; }
.lbt-radar-wrap, .lbt-bar-wrap { width: 100%; min-height: 320rpx; padding: 24rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); background: #fff; }
.lbt-dim-card { padding: 32rpx; border-radius: 24rpx; border: 2rpx solid rgba(var(--xia-deep-rgb), 0.1); margin-bottom: 24rpx; overflow: hidden; background: #fff; box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04); }
.lbt-dim-card:last-child { margin-bottom: 0; }
.lbt-dim-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; flex-wrap: wrap; gap: 12rpx; }
.lbt-dim-name-row { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; flex: 1; }
.lbt-dim-icon { width: 40rpx; height: 40rpx; flex-shrink: 0; }
.lbt-dim-name { font-size: 30rpx; font-weight: 600; color: var(--xia-deep); }
.lbt-dim-meta { font-size: 28rpx; font-weight: 700; color: var(--xia-teal); flex-shrink: 0; }
.lbt-dim-bar { height: 12rpx; width: 100%; overflow: hidden; border-radius: 999rpx; background: rgba(var(--xia-deep-rgb), 0.12); margin-bottom: 16rpx; }
.lbt-dim-fill { height: 100%; border-radius: 999rpx; background: rgb(219, 39, 119); }
.lbt-dim-fill.lbt-dim-fill-teal { background: var(--xia-teal); }
.lbt-level-box { display: flex; gap: 16rpx; padding: 16rpx 24rpx; border-radius: 16rpx; background: rgba(var(--xia-cream-rgb), 0.5); margin-bottom: 16rpx; }
.lbt-level-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.lbt-level-text { font-size: 26rpx; font-weight: 500; color: rgba(var(--xia-deep-rgb), 0.9); line-height: 1.5; flex: 1; }
.lbt-dim-desc { font-size: 26rpx; line-height: 1.6; color: rgba(var(--xia-deep-rgb), 0.8); }
.lbt-tips-card { padding: 32rpx; border-radius: 24rpx; border: 1rpx solid rgba(252,231,243,0.8); background: rgba(253,242,248,0.6); }
.lbt-tip-item { display: flex; align-items: flex-start; gap: 16rpx; margin-bottom: 16rpx; }
.lbt-tip-item:last-child { margin-bottom: 0; }
.lbt-tip-icon { width: 32rpx; height: 32rpx; flex-shrink: 0; margin-top: 4rpx; }
.lbt-tip-text { font-size: 28rpx; line-height: 1.5; color: rgba(var(--xia-deep-rgb), 0.9); flex: 1; }
.lbt-footer { border-top: 2rpx solid rgba(var(--xia-deep-rgb), 0.08); background: rgba(var(--xia-cream-rgb), 0.2); padding: 48rpx 32rpx; text-align: center; }
.lbt-footer-text { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.5); }
.lbt-footer-links { display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: 16rpx 32rpx; margin-top: 24rpx; }
.lbt-footer-link { font-size: 28rpx; font-weight: 500; color: var(--xia-teal); text-decoration: underline; }
.lbt-footer-dot { font-size: 24rpx; color: rgba(var(--xia-deep-rgb), 0.3); }
